# JOURNAL — platform-contracts
Continuity ledger per CTO charter §6/§6bis. Every entry is evidence-grounded.

Format per entry:
## <date> · <slice/WO id> · <status: in-progress | in-review | done | blocked-on-founder>
- What was done (with the tool result / test output that proves it)
- Decisions made · safest-defaults applied on open ⏳ (flagged) · founder overrides
- Pending / next

---

## 2026-07-09 · E0 bootstrap · done
- Arranged the founder-supplied kickoff set: `e0-kickoff/docs/` → `/docs` (7 canon documents), `CLAUDE.md`/`AGENTS.md`/`JOURNAL.md` → repo root, four work orders → `/WORK-ORDERS/`, `e0-kickoff/` (incl. its README) deleted. Commit `f72dba5` "chore(e0): bootstrap canon, charter, work orders", pushed to `main`.
- Canon completeness check: Execution Contract + 3 Build Specs + 3 Building Plans all present in `/docs`. North Stars + prototype not yet supplied — expected per WO-0 §B6 ("…when the founder supplies them"); the manifest covers the seven present documents.

## 2026-07-09 · Founder Decision — RoundingLaw v1 · CLOSED ✅
- **Ruling (founder, 2026-07-09):** RoundingLaw v1 CONFIRMED; the WO-0 §B2 ⏳ and its PENDING_FOUNDER_DECISION block are lifted. The law: `sellerPlatformFee = floor(0.05 × B)` · `resellerPlatformFee = floor(0.20 × (C + M))` · `sellerNet = B − C − sellerPlatformFee` · `resellerNet = (C + M) − resellerPlatformFee` · `platformProductFeeRevenue = sellerPlatformFee + resellerPlatformFee`. All Quote money fields integer FCFA; rounding exists ONLY on the two fees; nets are defined by subtraction, never independent multiplications.
- Applied as the single named module `packages/contracts/src/money/rounding-law.ts` (`ROUNDING_LAW_VERSION = 'v1'`) — no marker, no skipped tests. Fees computed in exact integer arithmetic (no floating-point money).
- Suite contains, all passing: the reconciliation property tests (fast-check, 2,000 runs each, both identities exact ∀ integer FCFA inputs in realistic ranges) · the §5.4 worked baseline asserted literally (10,000/1,000/1,500/1,000 → 11,500 · 12,500 · 8,500 · 2,000 · 1,000) · the founder's fixed non-divisible regression asserted exactly (B=10,001 · C=333 · M=778 · D=600 → fees 500 · 222, sellerNet 9,168, resellerNet 889, platform 722, productSubtotal 10,779, buyerTotal 11,379).

## 2026-07-09 · WO-0 · in-review (built + verified; tag/merge reserved to the founder's reviewer)
- **Built on branch `e0/wo-0`**, small conventional commits:
  - B1: pnpm + Turborepo + TypeScript-strict workspace; packages `@platform/contracts` · `@platform/kernel-types` · `@platform/i18n` · `@platform/ui-tokens`, all versioned 0.1.0.
  - B2: E1-critical §5.6 shapes as TS types + zod strict schemas (frozen Quote verbatim — `supplyMode`/`handlingClass`/`kittingSealId` are a tested parse failure on the Quote); four secrets as branded, non-assignable types; pure money functions (`computeWaterfall`, `assertQuoteReconciles`, RoundingLaw v1); byte-stable canonical JSON; event envelope + E1 event-name union with all `packlab.*`/`cercle.*`/`campaign.*`/`referral.*`/`review.*` names excluded.
  - B3: kernel types (phone-alias identity, no-address Location — rejects `streetAddress`, offline queued-=-pending semantics, media refs). Types + schemas only, no services.
  - B4: i18n catalog schema (`register` + `screenClass` on every entry), copy-lint CLI implementing exactly the four §10.5 failure conditions with token lists/budgets as maintained data files; seed catalog = the eight canonical Shop+ §6.1 checkout strings (register: money); negative fixture catalog.
  - B5: ui-tokens — color/type/spacing/radius/elevation/motion, one family DNA, three themes (`boutik-plus` supply-green · `shop-plus` warm commerce · `sera` road-and-custody), shared verified-badge color; tokens only, no components.
  - B6: `docs.manifest.json` (sha256 × 7 docs + packageVersion 0.1.0, root + package copy) + `drift-check` CLI shipped in `@platform/contracts`.
  - B7: CI workflow `.github/workflows/ci.yml` + `scripts/run-gates.sh`.
- **Evidence (tool outputs in this session; copies in `_review/WO-0-review.zip` → `logs/`):**
  - `pnpm build` 4/4 green · `pnpm typecheck` 5/5 green (incl. secret-separation type tests) · **60/60 tests passing** with assertions visible (verbose output captured).
  - Every gate shown failing once on its negative fixture: copy-lint fixture with « Veuillez patienter » + « séquestre » fails with all four conditions reported (exit 1) · tampered consumer doc fails drift-check (exit 1) · pristine copy passes · secret-substitution fixture fails tsc (TS2322 both ways) · independent-multiplication quote throws QuoteReconciliationError (10,778 ≠ 10,779) · tampered snapshot (kittingSealId on Quote) fails shape-freeze.
- **Safest defaults applied (maintained data, not canon):** copy-lint token lists (administrative-formal set beyond the canonical six; marketing/urgency; finance jargon; Mooré/Dioula) and reading-level budgets are data files seeded so the canonical §6.1 strings pass; screen-class list is a lint parameter. All tunable without code changes.
- **Canon nit surfaced to the founder (no action taken):** `EvidenceBundle` carries `coarseLocation` in the Séra spec §5.6 but not in Boutik+/Shop+ §5.6 — modeled as optional here; the specs should be reconciled at source.
- **Founder overrides recorded:** WO-0 §B8/DoD calls for the `v0.1.0` git tag, but the founder's session instruction reserves tagging and merging to his reviewer — versions are set to 0.1.0 and the tag is intentionally NOT created. Ecosystem-Engineering session ruling: RoundingLaw v1 confirmed (see Decision entry above), so the §B2 PENDING_FOUNDER_DECISION path was never entered.
- **Fresh-context verifier (mandatory for money/contracts paths, charter §6bis): VERDICT PASS, zero blocking findings.** The verifier — given only the SPEC AUTHORITY quotes, the founder RoundingLaw ruling, the full `main...e0/wo-0` diff, and the DoD — independently re-ran build/typecheck/tests/gates with caches bypassed (60/60 at the time, 0 skipped), read the actual assertions, confirmed the frozen Quote is field-exact §5.6 with gated fields a tested parse failure, confirmed no float money and no independent-multiplication nets anywhere in src, confirmed the event union carries zero gated names, and confirmed nothing OUT-OF-SCOPE was built. Findings: 1 NON-BLOCKING (a same-version snapshot regeneration would pass CI — freeze-with-version-bump was convention, not machine) + 7 NOTEs.
- **Fixes applied after the verdict (commit `5a5c191`), re-verified:** (1) new CI gate `scripts/check-snapshot-version-bump.sh` — a changed API-surface snapshot without a `@platform/contracts` version bump now fails CI (initial addition allowed); demonstrated failing once on a tampered snapshot (exit 1, output in evidence). (2) copy-lint condition (b) now scans `moore`/`dioula` variant texts, not only `fr` (+ regression test). (3) gate runner uses `mktemp` for the drift-consumer fixture (no fixed /tmp path). Post-fix full re-run: build green, gates ALL GREEN, **61/61 tests passing** (verbose output in evidence).
- **Verifier re-check of the delta: RE-VERDICT PASS.** All four branches of the new snapshot-version gate exercised live in a scratch clone (tamper-without-bump fails exit 1, tamper-with-bump passes, missing base skips, unchanged passes); lint variant coverage confirmed with the regression test; 61/61 tests and ALL GATES GREEN reproduced with caches bypassed; JOURNAL wording checked for overclaim — none found. Remaining findings: 3 cosmetic NOTEs (working-tree version read in dirty local trees, un-cleaned mktemp dir, evidence archive location).
- **Review packet:** `_review/WO-0-review.zip` — full `main...e0/wo-0` diff as `diff.txt`, all test/CI/gate outputs incl. every negative-fixture failure and both verifier verdicts under `logs/`, complete source tree under `source/` (node_modules and .git excluded).
- **Pending / next:** founder's reviewer verdict on `e0/wo-0` (merge + tag `v0.1.0`); then WO-B0.1 / WO-SP0.1 / WO-SE0.1 pin the tag.

## 2026-07-09 · Conduct (founder correction, binding)
- Every number in any report to the founder must be copyable from a log in the evidence set; adopted permanently — reports are now written against the logs, not from memory.

## 2026-07-09 · WO-0 · done (founder review PASS) + WO-0B consumability · done
- **Founder review verdict on `e0/wo-0`: PASS.** Merged into `main` with merge commit `24d400a` ("merge(e0): WO-0 platform-contracts v0.1.0 — founder review passed"), pushed.
- **Consumability gap closed (blocked the tag):** `dist/` was gitignored and no outside consumer had ever installed the packages. Mechanism chosen: **per-package `prepare` builds + pnpm git-dependency `path:` syntax** (commits `41d7bfd`, `5bf4a4a`) — each package builds from source on install; `@platform/contracts`'s dep on kernel-types changed `workspace:*` → `0.1.0` (workspace protocol does not resolve for outside consumers — verified failing: `ERR_PNPM_WORKSPACE_PKG_NOT_FOUND`) with `linkWorkspacePackages: true` keeping local sibling links (lockfile: `specifier: 0.1.0, version: link:../kernel-types`). Consumers need two `pnpm-workspace.yaml` blocks: `onlyBuiltDependencies` (pnpm 10 blocks dependency build scripts by default) and an `overrides` entry routing transitive `@platform/kernel-types@0.1.0` to the same git ref. Exact install line (app repos): `pnpm add "git+https://github.com/beurni2/platform-contracts.git#v0.1.0&path:packages/contracts"` (same pattern for the other three) — full recipe in `/CONSUMING.md`.
- **Acceptance evidence (`consumer-proof.txt` in the WO-0B packet):** from a scratch directory outside the repo, all four packages installed pinned to main sha `5bf4a4a`; `baseline-check.mjs` imported `computeWaterfall` + `assertQuoteReconciles` and asserted the §5.4 baseline — output: productSubtotal 11500 · buyerTotal 12500 · sellerNet 8500 · resellerNet 2000 · platformProductFeeRevenue 1000 · "assertQuoteReconciles: no throw" · OK; `imports-check.mjs` resolved and imported kernel-types, i18n, and ui-tokens from the same ref. Workspace re-verified after the change: 61/61 tests passing.
- **Tag `v0.1.0`: created, push BLOCKED by the environment (blocked-on-founder/infra).** Annotated tag cut on the final main commit (`git rev-parse v0.1.0` → `5a6813f`, `v0.1.0^{commit}` → `b10f482`). Pushing it fails deterministically — `error: RPC failed; HTTP 403` from the sandbox git proxy on every tag-push variant (3 forms tried with 2s/4s/8s backoff); branch pushes succeed, so this is a tag-ref permission of the proxy, not a network fault. `git ls-remote --tags origin` is empty (full evidence: `tag-status.txt` in the WO-0B packet). The documented `#v0.1.0` install line was proven verbatim against the tag from a scratch consumer (`consumer-tag-proof.txt`: §5.4 baseline OK). **Unblock (either):** (a) from any authorized clone: `git fetch origin main && git tag -a v0.1.0 -m "platform-contracts v0.1.0 — E0 canon package set (WO-0, founder review passed)" b10f482 && git push origin v0.1.0`; or (b) grant tag-push on the proxy and I push the existing tag object. Until then app repos pin sha `b10f482` (interim note in `/CONSUMING.md`).

## 2026-07-09 · WO-0C · platform-contracts v0.2.0 — in-review (built + verified on `canon/v0.2.0`; merge and tag are the founder's, not mine)
- No failures to report: every suite, every gate, and the scratch-consumer E2E are green; the fresh-context verifier's verdict is attached below the evidence bullets.
- **1. RN-safe entrypoints (the one structural change).** Each package's `exports` `"."` entry now exposes only shapes, schemas, pure functions, tokens, and catalog data. `@platform/contracts` root no longer re-exports the drift-check module (node:crypto/fs/path); it moved to subpaths `@platform/contracts/drift-check` (lib) and `@platform/contracts/drift-cli` (CLI module), bin `drift-check` unchanged. `@platform/i18n` root no longer re-exports the data loader (node:fs/path/url); it moved to `@platform/i18n/data-loader` and `@platform/i18n/lint-cli`, bin `copy-lint` unchanged, plus `@platform/i18n/catalog.json` exposing the seed catalog as importable JSON data. Additive for existing consumers: all three app repos import only root entries or type-only imports (verified by grep over their `origin/main`; the CLIs are consumed via `pnpm exec` bins only), and every package carries a `./dist/*` passthrough so any legacy deep import keeps resolving.
- **2. Gate.** New scanner `scripts/scan-rn-safe-entry.mjs` walks each `"."` static import graph, follows `@platform/*` siblings, and fails on ANY node builtin (full `builtinModules` set, `node:`-prefixed or bare) or unvetted external (allowlist: `zod`). Pass output (log-copied): contracts 19 files · kernel-types 5 · i18n 3 · ui-tokens 3, all "no node builtins", externals `[zod]` or none. Negative fixture `scripts/show-rn-safe-negative.sh`: hardlinked copy of the built contracts package (`cp -al`), planted `import { readFileSync } from "node:fs"` into `dist/enums.js` on the copy (rm-before-write so the shared inode is untouched, verified pristine afterwards) — scanner output "✗ @platform/contracts: root entry graph is NOT RN-safe … node builtin 'node:fs' imported", exit rc=1. Both wired into `scripts/run-gates.sh` alongside a new export-map gate (`scripts/check-export-maps.mjs`: every subpath target exists post-build, node-only subpaths present, versions 0.2.0, `./dist/*` present).
- **3. CONSUMING.md.** Added the CI-ONLY auth step verbatim from the deployed app-repo workflows (read from `boutik-plus` `origin/main:.github/workflows/ci.yml` lines 19–20, identical in shop-plus): the `git config --global url."https://x-access-token:${{ secrets.PLATFORM_CONTRACTS_READ_TOKEN }}@github.com/beurni2/platform-contracts".insteadOf` rewrite before `pnpm install`, requiring the fine-grained read-only PAT secret per app repo, ephemeral runner config only. All install lines now pin `#v0.2.0`; the interim-sha note is replaced by the tag process (founder cuts tags in the GitHub Releases UI at the reviewed final sha — sandbox tag pushes are 403, known). New section documents the RN-safe roots + node-only subpaths and that RN apps may now import canon directly (type-only workaround optional).
- **4. Versions.** All four packages + root → 0.2.0; `@platform/contracts` dep on kernel-types → `0.2.0`; `docs.manifest.json` regenerated (packageVersion 0.2.0, 7 docs, root + package copies identical); run-gates `--pinned-version` → 0.2.0. **Shape-freeze snapshot updated deliberately in this PR** — snapshot packageVersion 0.2.0, 91 exports (was 93: `checkDocsDrift` and `sha256Hex` left the root surface), and the version-bump gate confirms: "check-snapshot-version-bump: snapshot changed with version bump 0.1.0 -> 0.2.0 — OK". Untouched suites stayed green untouched: reconciliation, secrets, copy-lint, no-gated-shapes — full run 42+9+5+5 = 61/61 tests, 0 skipped; `run-gates.sh` → "ALL GATES GREEN (positive suites pass; every negative fixture demonstrably fails)".
- **Scratch-consumer E2E (outside the repo, pinned to sha `d1bc628`):** all four packages installed at 0.2.0; root imports asserted the §5.4 baseline (11500 · 12500 · 8500 · 2000 · 1000, "assertQuoteReconciles" no throw); subpaths `@platform/contracts/drift-check`, `@platform/i18n/data-loader`, and `@platform/i18n/catalog.json` resolved and worked (seed catalog: 8 entries, 0 violations); output in the packet (`logs/consumer-e2e-proof.txt`).
- **5. Deferred, with reasons (OUT OF SCOPE this release):** (a) North Stars + prototype into `/docs` — founder has not supplied the files; they join the manifest at v0.3.0. (b) `PERF-BUDGETS.md` — Decision D17 silent; budgets are named at E0 by founder numbers, not invented ones. (c) copy-lint label-class rule — Decision D18 silent; the lint's screen-class set stays as maintained data until ruled. (d) `EvidenceBundle.coarseLocation` spec-text alignment — D19: founder edits the spec at source; the schema keeps the optional-field bridge until the canon text changes.
- **Fresh-context verifier: VERDICT PASS** (report attached verbatim in the packet, `logs/verifier-verdict.txt`). It independently reproduced every figure (61/61 tests · scanner counts 19/5/3/3 · snapshot 93→91 exports, removed set exactly `{checkDocsDrift, sha256Hex}` · consumer install at 0.2.0 with the §5.4 baseline exact) and confirmed the CONSUMING.md auth step byte-identical to the deployed boutik-plus workflow. Findings: 1 NON-BLOCKING (the scanner mapped `@platform/pkg/subpath` imports to the sibling's ROOT entry — a future node-only subpath import could have evaded the gate; no such import exists today) + 3 NOTEs (packet must be committed before merge — done in the packet commit that closes this entry, after both verifier passes; a missing dist could have made the fixture "fail" vacuously; app repos still pin `#v0.1.0`, which is expected — they move in their own PR). Transparency: the verifier's first run was terminated mid-verification by a platform credit limit and was resumed to completion; both the partial and final outputs are preserved in the verdict file.
- **Post-verdict hardening (commit `9e855b6`), re-verified:** scanner now resolves `@platform/*` subpaths through the sibling's real exports map (wildcards included); the negative fixture gains scenario 2 — planted `import "@platform/i18n/data-loader"` in a hardlinked root graph → scanner surfaces `node:fs/promises`, `node:path`, `node:url` from `data-loader.js`, rc=1 — plus a missing-dist guard (exit 0 = undemonstrated, never counted as the expected failure). Full re-run at final sha `9e855b6`: "ALL GATES GREEN", 61/61 tests, snapshot-version-bump gate "0.1.0 -> 0.2.0 — OK", scratch-consumer E2E re-pinned to `9e855b6` green (all root entries + subpaths + catalog.json).
- **Verifier delta re-check: RE-VERDICT PASS** (verbatim in the packet, `logs/verifier-re-verdict.txt`). It re-ran everything at the delta tip (scanner 19/5/3/3 rc=0 · 61/61 tests · all 12 gates green · both fixture scenarios rc=1 with the real dist pristine · missing-dist guard exercised by parking dist: "BUG: … build before running the fixture", exit 0 · its own wildcard probe caught with the same three builtins · consumer E2E re-pinned green). Findings: 1 NON-BLOCKING — a JOURNAL clause here claimed the packet was already committed while it was still anticipatory; the wording is corrected above and the packet commit closes this entry — plus 2 NOTEs (a CI-side wildcard fixture scenario is optional, the verifier probed that branch functional itself; the credit-limit interruption disclosure is consistent but not independently verifiable by it).
- **Conduct:** every figure above is copied from a log in `_review/WO-0C-review.zip`; the verifier verdict is attached verbatim in the packet and below.
- **Founder review verdict: PASS.** Guard re-issued after the first guard's self-contradiction (it demanded both the pre-packet head `ad6253c` and the packet in-tree, which cannot both hold) was caught and reported rather than forced through. Re-issued guard verified empirically before merge: `canon/v0.2.0` head `933aa0d`, log contains `ad6253c`/`9e855b6`/`d1bc628` · `git diff --stat ad6253c..933aa0d` touches only `_review/WO-0C-review.zip` (Bin 0 -> 52970 bytes, the packet commit and nothing else) · `WO-0C-review.zip` tracked at head · scanner exit 0 (19/5/3/3 files, no node builtins). **Merged into `main`: `558e007..efabb77`** ("merge(canon): v0.2.0 — RN-safe entrypoints, builtin scanner, CONSUMING v0.2.0 — founder review passed"), pushed. `git ls-remote --heads origin`: `main` → `efabb7726e8c58df6730d3fe8d7fbc82a31adbe9`. WO-0C status: **done**.
- **Tag `v0.2.0`:** founder creates it at the merge commit via GitHub Releases (sandbox tag pushes 403, known — see the WO-0/WO-0B precedent above). Consumers bump per `CONSUMING.md`: shop-plus's WO-1.1 pre-flight self-adjusts; sera and boutik-plus bump inside their first E1 slices.

## 2026-07-10 · WO-1.0 · platform-contracts v0.3.0 — in-review (built + verified on `e1/wo-1.0`; do NOT merge — founder's reviewer decides)
- No failures to report: 87/87 tests (48 contracts + 9 i18n + 5 kernel-types + 5 ui-tokens + 20 certification, 0 skipped), all gates green, every negative fixture demonstrably fails; the verifier verdict is attached below, verbatim, and ships in the packet committed by the closing commit of this entry.
- **1. `@platform/certification` (new package, node tooling — README states it must never enter an app's runtime graph; exempt from the RN-safe roots by design, tracked by the export-map gate).** (a) The §3 suite: a standard `MockAdapter` interface per E1 domain (payment-provider · eligibility · supply-projection · readiness) and `certifyAdapter` running the eight §3 behaviors as executable checks with a per-behavior scorecard — certification is 8/8, no partial passes. (b) Envelope conformance: every event must parse via the pinned `PlatformEventSchema` and carry `{command_id, correlation_id, aggregateVersion, actor, serverTime, version}`. (c) The §2.3 fifteen-step chain runner over pluggable `ChainAdapters`, validating ONE correlation chain across the nine §6 ids, the step-6 Quote against `QuoteSchema` + `assertQuoteReconciles`, and the step-8 Order against the five-state enum; emits text + static-HTML chain report (the E1 dashboard seed). (d) Four bundled §3-complete reference adapters self-test the suite and runner end-to-end; the chain reference adapters compose them.
- **2. Canon addition (the deliberate part of this bump):** `ORDER_STATUSES = quote_issued | reserved | payment_pending | paid | confirmed` and `OrderSchema.status` tightened from bare string to the enum (WO-1.1 verifier NB-6; §2.2 single-definition). Terminal/failure states are E2's and were NOT added. Snapshot updated deliberately: packageVersion 0.3.0, 93 exports (was 91: + `ORDER_STATUSES`, `OrderStatusSchema`); gate output: "check-snapshot-version-bump: snapshot changed with version bump 0.2.0 -> 0.3.0 — OK". Versions 0.3.0 across root + all five packages; docs manifest regenerated (7 docs, packageVersion 0.3.0).
- **Evidence (all in `_review/WO-1.0-review.zip` → `logs/`, committed by the closing commit of this entry):** reference adapters certify **4/4 domains, 8/8 behaviors each** (scorecards in `certification-positive.txt`) · deficient mock (duplicates behavior removed) **REFUSED, 7/8, exit 1** · envelope negative (missing correlation_id) fails exit 1 · chain runner: **"15-STEP CHAIN COMPLETE — correlation corr_e1demo — 27 events"**, all nine ids minted and distinct (`quote_e1demo → rsv_e1demo → payatt_e1demo → order_e1demo → pkg_e1demo → dtask_e1demo → val_e1demo → sob_supplier_e1demo → payout_e1demo`), `chain-report.html` generated · broken-link negative (validation_id dropped) fails exit 1 · order-status negative: `'shipped'` refused with `expected one of "quote_issued"|"reserved"|"payment_pending"|"paid"|"confirmed"`, exit 1 · all pre-existing suites green untouched (money, secrets, copy-lint, RN-safe 19/5/3/3, shape-freeze, drift) · typecheck 8/8 tasks successful (uncached; the 6/6 figure first written here was stale — corrected per the verifier's finding 2).
- **Flake caught and fixed during build (evidence-grounded):** the delay-behavior check intermittently failed under parallel turbo load — Node timers measured with `Date.now()` can quantize ~1ms "early" (observed 39ms for a 40ms sleep). Fixed with a documented 2ms clock-quantization tolerance (a deficient mock observes ~0ms, so the tolerance cannot mask a missing behavior); full suite then ran **6/6 consecutive uncached passes**.
- **3. Deferred founder items, re-recorded at v0.3.0 (unchanged, awaiting founder inputs):** (a) North Stars + prototype into `/docs` — files still not supplied; join the manifest at their arrival. (b) `PERF-BUDGETS.md` — D17 silent; device budgets are founder numbers, not invented ones. (c) copy-lint label-class rule — D18 silent; screen-class set remains maintained lint data. (d) `EvidenceBundle.coarseLocation` spec-text alignment — D19: founder edits the canon at source; the optional-field bridge stays until then.
- Not merged. After founder review, main advances and the founder cuts `v0.3.0` via GitHub Releases at the final sha (sandbox tag pushes 403, known).
- **Fresh-context verifier: VERDICT PASS** (report verbatim in the packet, `logs/verifier-verdict.txt`). It re-ran every DoD item itself, uncached: 4/4 domains at 8/8 with scorecards observed · deficient mock "NOT CERTIFIED — payment-provider — 7/8", only `emits_duplicates` ✗, exit 1 · chain "15-STEP CHAIN COMPLETE — corr_e1demo — 27 events" with the nine ids pairwise distinct and the HTML verdict "CHAÎNE COMPLÈTE" · snapshot delta independently confirmed (91→93, added exactly `ORDER_STATUSES` + `OrderStatusSchema`) · its own probes beyond the shipped tests: a 1-FCFA buyerTotal tamper at step 6 → `QuoteReconciliationError` flagged; `status:'shipped'` at step 8 → parse failure flagged; deficient delay observes 0ms vs the 38ms threshold, so the 2ms tolerance cannot mask · no network calls in the package · 87/87 tests, "Tasks: 8 successful, 8 total" typecheck. Findings, titles quoted verbatim: 1 **NON-BLOCKING (fix before merge)** — "JOURNAL.md:71,74 claims 'the verifier verdict is attached below and in the packet, verbatim' … but `_review/` contains only WO-0/WO-0B/WO-0C zips — no WO-1.0-review.zip exists at HEAD"; 2 **NON-BLOCKING** — "JOURNAL.md:74 'typecheck 6/6 clean' is stale … 'Tasks: 8 successful, 8 total'"; 3 **NOTE** — "the delay check measures the adapter's self-reported `deliveredAt`… acceptable for cooperative mocks/live siblings under §3"; 4 **NOTE** — "without `--out`, the runner writes `chain-report.html` into the cwd… Cosmetic"; 5 **NOTE** — "pairwise-distinctness covers exactly the nine §6 chain ids (what the Contract requires)".
- **Findings resolved before merge (docs-only delta, no code touched):** finding 1 — the two anticipatory clauses are reworded above to name the closing packet commit, and the packet (with this verdict inside) is committed by that closing commit, per the WO-0C precedent; finding 2 — the stale "6/6" corrected to the log-copyable "8/8 tasks successful". NOTEs 3–5 acknowledged, no action (cooperative-adapter measurement is the §3 certification model; the gate always passes `--out`; distinctness scope is the Contract's).

## 2026-07-10 · Conduct (founder correction #2, binding, all repos)
- Identity headers are command-generated: the final message shows `git rev-parse --short HEAD` and its output together — a typed sha is prose, and prose is not evidence. (Root cause of the 4c1f2ae guard failure: a typed header sha, not command output. The self-anchoring guard pattern replaces named-sha guards.)

## 2026-07-10 · WO-1.0 · done (founder review PASS — merged)
- Self-anchoring guards verified empirically before merge: local `e1/wo-1.0` head equal to origin's (`2f4e9e657a616cc997c70a45dd7675f932a0003b` both sides, ls-remote log-copied) · log contains `50e1bc5`/`3c64cd8`/`506ba7a`/`47406e1` · `git diff --stat 50e1bc5..HEAD` touches only `_review/WO-1.0-review.zip` (Bin 0 -> 71455 bytes) · `run-gates.sh` at head exit 0, "ALL GATES GREEN".
- **Merged into `main`: `b076ff3..9308c64`** ("merge(e1): WO-1.0 certification suite + 15-step chain runner + order-status canon (v0.3.0) — founder review passed"), pushed; `git rev-parse HEAD` → `9308c641c368ea56d141867d667b122e10db682d`.
- **Release ref = the merge-commit sha `9308c64`** (pins use the sha, per the founder tag ruling). The founder re-cuts lowercase `v0.3.0` at the NEW main after this merge — the prior `V0.3.0`/`BOSSY`/`boss` tags are founder artifacts being deleted (report-only). Observed during the guard fetch, report-only: a lowercase `v0.3.0` also appeared on origin pointing at the PRE-merge main `b076ff3` (v0.2.0-era content) — it needs the same re-cut at `9308c64`.
- App repos converge on this sha at their next E1 slices; certification of shop-plus's in-repo WO-1.1 mocks happens at E1 assembly, per the work order.

## 2026-07-10 · Founder Decisions D17 + D18 · CLOSED ✅ (founder-signed 2026-07-10)
- **D17 (performance & device budgets):** reference profile = Android Go class, 2GB RAM (Tecno Spark Go / Itel A-series, Android 12–13 Go), 3G-class network (1.5 Mbps, 300ms RTT), 360×800. Signed budgets: cold start < 5s · touch-to-feedback < 100ms · touch-to-result < 250ms · no frozen scroll > 200ms · initial PWA payload < 300KB compressed · queued offline actions survive app-kill and reboot · French long-text never truncates meaning. Landed as `/docs/PERF-BUDGETS.md` with the Contract §7.3 named list completed by conservative values, each marked "CTO-derived — tune at E4 telemetry".
- **D18 (copy-lint label rule):** reading-level budgets apply only to sentences of four or more words; label screen-classes are checked for banned tokens + register only. Landed as lint rule + data (`minWordsForBudget: 4`, `labelClasses: ["label"]`, new `label` screen class).

## 2026-07-10 · WO-0D · platform-contracts v0.4.0 — in-review (built + verified on `canon/v0.4.0`; do NOT merge — founder's reviewer decides)
- No failures to report: 97/97 tests (53 contracts + 14 i18n + 5 kernel-types + 5 ui-tokens + 20 certification, 0 skipped), "ALL GATES GREEN (positive suites pass; every negative fixture demonstrably fails)", typecheck clean; the verifier verdict is attached below and ships in the packet committed by the closing commit of this entry.
- **1. PERF-BUDGETS.md** committed to `/docs`; manifest regenerated — 8 docs, packageVersion 0.4.0, `PERF-BUDGETS.md` sha256 `bffcb5328f994540c97a400c7494d2947c8304f287db8c8809fa94be8546b358` present in `docs.manifest.json`.
- **2. D18 lint rule** implemented as rule + data; tests log-copied: « Espace revendeur » PASSES as label · « Repère » PASSES as label (the collision instance) · a 5+-word administrative sentence still FAILS the budget on a non-label class · « séquestre » in a label still FAILS the banned-token check · a sub-4-word high-syllable sentence is budget-exempt even outside labels. The pre-existing negative fixture still fails with all four §10.5 conditions.
- **3. Supply-projection contract promoted** into `@platform/contracts` as `SupplyProjectionSchema` (strict; §2.2 single definition; B4.2/SP-I03 — no supplier identity/contact/pickup fields); `@platform/certification` re-imports it (`SupplyProjectionEventPayloadSchema` aliased for compatibility; the behavior-8 identity check now anchors on the canon schema). Negative gate log-copied: `supplierName`/`supplierPhone`/`pickupAddress` → `unrecognized_keys`, exit 1. Positive parses in tests (4 leak variants refused).
- **4. CONSUMING.md**: overrides section now the ≥0.3.0 full block — `@platform/contracts`, `@platform/kernel-types`, AND `@platform/ui-tokens` all routed to the pinned ref (certification's version-deps); install lines reference `<RELEASE_SHA>` with the current worked example (v0.3.0 release = `9308c641c368ea56d141867d667b122e10db682d`); `@platform/certification` added to the consumer build-script allowlist.
- **5. Versions 0.4.0** across root + all five packages; deliberate snapshot bump — 94 exports (was 93: + `SupplyProjectionSchema`), gate output: "check-snapshot-version-bump: snapshot changed with version bump 0.3.0 -> 0.4.0 — OK".
- **Deferred, re-recorded at v0.4.0:** (a) North Stars + prototype into `/docs` — founder files still not supplied. (b) D19 `EvidenceBundle.coarseLocation` spec-text alignment — founder edits the canon at source; the optional-field bridge stays until then. (D17 and D18 leave the deferral list — closed above.)
- Not merged. After founder review, main advances; the release ref is the merge-commit sha per the standing tag ruling.

### WO-0D verifier round (continues the in-review entry above)
- **Fresh-context verifier: VERDICT PASS at `a968d0b`** (brief = the WO-0D order verbatim + §2.2/§7.3/§3 quotes + the full diff; no build-thread context). Finding titles quoted verbatim (standing rule): 1 "DIFF AUTHENTIC AND FULLY IN SCOPE" (NOTE) · 2 "FORBIDDEN LIST CLEAN" (NOTE) · 3 "D18 IMPLEMENTED EXACTLY" (NOTE — verifier probed boundaries beyond the shipped tests: 4-word boundary binds, 3-word exempt everywhere, conditions a/b/d still fire on labels) · 4 "§2.2 SINGLE DEFINITION HOLDS" (NOTE — alias is reference-identical; behavior-8 anchors on the canon object) · 5 "SNAPSHOT DELTA IS EXACTLY THE PROMOTION" (NOTE) · 6 "LABEL EXEMPTION FOR ≥4-WORD LABEL STRINGS HAS NO TEST" (**MINOR**) · 7 "JOURNAL FORWARD-REFERENCES A VERDICT NOT YET IN THE DIFF" (NOTE) · 8 "STALE ILLUSTRATIVE EXAMPLE IN CONSUMING.md" (NOTE) · 9 "VERSION COHERENCE COMPLETE" (NOTE).
- **Finding 6 fixed in `804b65b`** — one committed test: « Coordonnées géolocalisées vérification communautaire » (4 words, avg 4.50 syllables/word by the shipped estimators — the min-words clause cannot exempt it) PASSES as `label` AND FAILS `reading_level` on `status`. i18n suite now 15 tests; workspace total **98/98** (53 contracts + 15 i18n + 5 kernel-types + 5 ui-tokens + 20 certification — `tests-after-finding6.txt`, log-copied). **Verifier delta re-check: "DELTA VERDICT — CONFIRMED FIXED"** at `804b65b`; its stat check confirms the commit touches only `packages/i18n/test/copy-lint.test.ts` (+13 lines).
- **Finding 7 resolved by this entry's closing commit** — the packet committed there contains the verdict file (initial + delta, verbatim); the forward-looking wording above becomes true at that commit, not before. **Finding 8 deferred, recorded:** the illustrative `@platform/kernel-types@0.2.0` pin in CONSUMING.md's Mechanism section is stale (actual pins 0.4.0) — outside WO-0D scope per the verifier's own reading; queued for the next doc pass.
- The count line in the opening paragraph above ("97/97 tests … 14 i18n") was true when written (pre-`804b65b`); the of-record count after the verifier round is **98/98**, per this addendum.

## 2026-07-10 · WO-0D · done (founder review PASS — merged)
- Self-anchoring guards verified empirically before merge: local `canon/v0.4.0` head equal to origin's (`c487d16a49b870cded579205f1f55614d0bd2936` both sides, ls-remote log-copied) · log contains `965b566`/`804b65b`/`a968d0b`/`b4301d7`/`67591c3`/`efb92c1`/`c9569ef` (7/7) · `git diff --stat 965b566..HEAD` touches only `_review/WO-0D-review.zip` (Bin 0 -> 58272 bytes) · `run-gates.sh` at head exit 0, "ALL GATES GREEN (positive suites pass; every negative fixture demonstrably fails)".
- **Merged into `main`: `4e751c3..be1df7a`** ("merge(canon): v0.4.0 — PERF-BUDGETS (D17), copy-lint label rule (D18), supply-projection promotion — founder review passed"), pushed; `git rev-parse HEAD` → `be1df7ad79a09d43182a638cc8acdd0a9081302e`.
- **Release ref = the merge-commit sha `be1df7a`**; apps converge on it at E1 assembly (sera may re-add « Repère » to its catalogs at that bump — the D18 label rule now admits it). Founder noted the finding-6 catch (label exemption previously unpinned by any committed test) with approval.
- **D17 and D18 leave the open-decisions board.** Remaining founder items: North Star files + prototype into `/docs` · D19 `coarseLocation` spec edit at source · stale CONSUMING.md `@0.2.0` mechanism example at the next doc pass.

## 2026-07-10 · E1 ASSEMBLY PHASE 2 · the fifteen steps LIVE — in-review (built + verified on `e1/assembly`; do NOT merge — founder's reviewer decides)
- No failures to report. The Contract E1 exit is met on live wiring, every figure below log-copied: **`15-STEP CHAIN COMPLETE — correlation corr_e1live — 31 events`, exit 0** (`logs/e1-run.txt`) · nine ids minted and distinct (`quote_e1live → rsv_e1live → payatt_e1live → order_e1live → pkg_e1live → dtask_e1live → val-order_e1live → sob_supplier_e1live → payout_e1live`) · **all four §3 pairs 8/8 mock AND live side by side** (`logs/conformance-pairs.txt`) · dashboard `assembly/chain-report.html` + exit doc `assembly/E1-EXIT.md` committed · live-wiring negative (validation_id dropped) **exit 1** · **cold proof green from committed bytes** at `340026e` (`logs/cold-proof.txt`) · root suites **98/98**, root gates "ALL GATES GREEN", typecheck 8/8 root + assembly clean.
- **Pinned world verified on origin before anything** (`logs/pinned-world.txt`): canon `be1df7a` (ancestor of main, asserted) · sera `77f12bc` · boutik-plus `76ef6aa` · shop-plus `0a8068f` — all three app repos had already converged their canon pins to `be1df7a` at their "assembly phase 1" heads.
- **Consumption mechanism (WO item 1):** `assembly/` is a standalone pnpm workspace consuming 16 app service packages as `git+https…#<pinned-main>&path:` deps. Two app-repo gaps closed ASSEMBLY-SIDE (app repos untouched, per FORBIDDEN): (a) `workspace:*` intra-repo leaf deps → pinned to the same repo+sha by `.pnpmfile.cjs`; (b) no `prepare` scripts + gitignored `dist/` → `scripts/build-consumed.mjs` compiles each consumed package in the pnpm store against `consumed-base-tsconfig.json`, whose sha256 equals all three repos' `tsconfig.base.json` at the pinned shas (`472ba1c6…777d`, all four identical — `logs/consumed-base-tsconfig-provenance.txt`). Lockfile regenerated then normalized to https-form URLs (standing guard; 96 scp-form entries → 0) and frozen-install-proven. Cold-proof round 1 caught the `file:`-linked certification packing only `dist/` → bootstrap order documented + fail-fast check (`340026e`); round 2 green.
- **Live adapters (WO item 2), imports only:** boutik SupplierRegistry/ProductCatalog/buildPremiumFrameAssets/OfferBook/buildSupplyProjection/FulfillmentBook · shop issueQuote (kill-switch consulted FIRST, live)/ImmutableQuoteStore/OrderSpine/LedgerRecords/attribution sign+verify · sera RiderRegistry/ReadyQueue/AssignmentBook/CustodySpine/acceptSellerReadinessEvidence. Both app DOs run under Miniflare/workerd from their COMMITTED bundles (boutik stock, shop reservation) — step 7 is atomic on both sides. Séra's intake projections read REAL cross-repo state (funding ← shop escrow record; readiness ← boutik FulfillmentBook). Payment provider = certified sandbox mock (no real provider, no credentials). Where boutik services return plain outcomes, the adapter wraps REAL outcome data in envelopes — plumbing, not domain logic. Step 4 composes the canon-owned `ResellerListingSchema` document (no listing CRUD exists in any app at E1 — §2.2: the shape's single definition lives in contracts). ADR-002 reading, journaled: "Séra/Boutik+ never import commerce-core" binds the APPS; this harness is the E1 assembly rig, not an app.
- **⚠ FLAGGED FOR FOUNDER — two §3 payload-registry alignments in `@platform/certification` (`78a88c0`), certification-package scaffolding only, `contracts/` and the money waterfall untouched (root snapshot/drift gates unchanged):** (1) **eligibility**: the WO-1.0 scaffold payload carried a SettlementObligation WITH an amount inside a Séra-produced event — the founder-reviewed live producer (WO-1.3 CustodySpine) emits the amount-free SE-I09 signal `{order_id, task_id, validation_id, result, settlement_eligibility}`; certifying live eligibility (WO item 3) against the amount-bearing scaffold was impossible without violating SE-I09/Ten-Laws-2, so the registry now pins the LIVE shape. (2) **payment-provider**: the scaffold's nested `{orderId, paymentAttemptId, leg}` shape is parsed by NO deployed consumer — the founder-reviewed OrderSpine (WO-1.1) parses top-level `amount`/`status`, the shape shop's provider mock emits; the registry now pins the deployed contract. Both reference mocks re-certified 8/8 after alignment; certification tests 20/20; reference chain still 15/15. This is the §3 discipline working as designed — the mocks had drifted from the deployed truth; easy revert if the founder rules otherwise.
- **§3 pairs (WO item 3), each 8/8, log-copied side by side:** supply-projection (LIVE boutik offer-service) · readiness (LIVE boutik fulfillment-service) · eligibility (LIVE sera custody-service — three real spines; evidence resubmission, a real at-least-once behavior, gives the signals distinct aggregate versions) · payment-provider (sandbox composition over shop's MockPaymentProvider — §3 misbehaviors driven through the mock's OWN knobs: webhookCopies, timeoutFirstNInitiates, staleStatusReads, and its real idempotency-key amount-mismatch refusal). Live invalid-transition refusals are REAL service refusals (expired offer, expired readiness challenge, drop-without-validation, amount-mismatch idempotency).
- **Flag service (WO item 4), live:** `assembly/worker/flag-worker.ts` — DO-backed workerd worker, remotely togglable (`GET /flags`, `POST /kill/:switch`), snapshot parsed by shop's own `parseSnapshot`. In-run demo log-copied: kill ON → `issueQuote` → `reason=checkout_killed` (refused closed) → kill OFF → the fifteen steps complete. **Remaining E0 deferral re-recorded: staging-environment isolation — enters at E2.**
- **The run (WO item 5) extras, all in-run and log-copied:** exactly-once eligibility proven live (drop-code replay absorbs with zero events; duplicate injection into shop's OrderSpine absorbs as `duplicate:true`) · obligations recorded COPIED `supplier:supplier-1 8500` / `reseller:rs_e1live 2000` · step-15 reconciliation to the franc (escrow leg 12500 = amountPaidAtCheckout; payouts 8500+2000 = sellerNet+resellerNet) · step-6 Quote validated live by `assertQuoteReconciles`, step-8 Order on the canon five-state enum · drop code LAST (step 13).
- The fresh-context verifier verdict is attached below and ships in the packet committed by the closing commit of this entry. Not merged; after founder review, main advances and the three app repos take up E2.

### E1 assembly verifier round (continues the in-review entry above)
- **Fresh-context verifier: VERDICT PASS at `cf0dd3f`** — it re-established the cold world itself (wiped node_modules/dist/caches, README bootstrap), re-ran every DoD item, and executed the founder's tamper mandate. Finding titles quoted verbatim (standing rule): 1 "PINNED WORLD VERIFIED ON ORIGIN" (PASS; NOTE on the canon-pin-is-ancestor nuance) · 2 "COLD PROOF RE-ESTABLISHED FROM COMMITTED BYTES" (PASS) · 3 "FIFTEEN STEPS LIVE" (PASS — committed chain-report.html byte-identical to its fresh output) · 4 "KILL-SWITCH DEMONSTRATION" (PASS) · 5 "NEGATIVE ON LIVE WIRING" (PASS; NOTE: future CI must assert exit==1 exactly, never "non-zero means failure") · 6 "CONFORMANCE PAIRS" (PASS — plus its OWN certifyAdapter re-run on both flagged domains, 8/8×4) · 7 "THE TWO FLAGGED §3 REGISTRY ALIGNMENTS ARE ALIGNMENTS, NOT WEAKENINGS" (PASS — "correct and mandatory under SE-I09"; certify.ts behaviors untouched, verified) · 8 "FORBIDDEN LIST — CLEAN" · 9 "HARNESS-COMPOSED ENVELOPES AND IDS" (NOTE — steps 9/14/15 envelopes and sob_*/payout_* ids are rig-minted labels over real ledger records; no settlement/payout service exists at E1) · 10 "IN-PROCESS QUOTE REFERENCE SHARING" (NOTE — harness-topology observation; the immutability boundary held under tamper).
- **Tamper results, log-copied from the verifier:** 1-FCFA quote tampers (buyerTotal +1, sellerNet +1) → chain BROKEN at step 6 by the canon reconciliation check; tampered bytes → `ImmutableQuoteStore` refused `bytes_do_not_match_quote`; direct `assertQuoteReconciles` threw. Duplicate eligibility → drop-code replay `{ok:true,duplicate:true,events:[]}`; exact re-injection AND re-enveloped copy into OrderSpine both absorbed `duplicate:true`; obligations stayed exactly 2 at 8500/2000. "It bit everywhere."
- **Three NOTEs ride to the founder** with the two flagged §3 alignments (finding 7 above). No fixes required; no code changed after `cf0dd3f`. The verdict ships verbatim in the packet committed by the closing commit of this entry.

## 2026-07-10 · E1 ASSEMBLY · done (founder review PASS — merged; E1 EXITED)
- Self-anchoring guards verified empirically before merge: local `e1/assembly` head equal to origin's (`9d74a2d3e229ab6cb5c6dfe276d76551b5f19e3a` both sides, ls-remote log-copied) · log contains `dc6b2d1`/`cf0dd3f`/`340026e`/`37cb6ac`/`78a88c0` (5/5) · `git diff --stat dc6b2d1..HEAD` touches only `_review/E1-assembly-review.zip` (Bin 0 -> 74154 bytes) · `run-gates.sh` at head exit 0, "ALL GATES GREEN (positive suites pass; every negative fixture demonstrably fails)".
- **Merged into `main`: merge commit `f637188`** ("merge(e1): E1 assembly — the fifteen steps live — founder review passed; §3 registry alignments ratified (SE-I09)"), pushed; `git rev-parse HEAD` → `f6371888de9ba2c00620a9b89770b0efbe123ab6`.
- **FOUNDER RULING, CLOSED (2026-07-10): the §3 payload-registry alignments (`78a88c0`) are RATIFIED under SE-I09** — the eligibility conformance contract is the amount-free live signal; the payment-provider contract is the shape the deployed OrderSpine parses. The registries now carry founder-ratified truth, not scaffolds.
- **E1 is EXITED against the Contract's three binaries:** the 15-step transaction completes with one correlation chain across all nine §6 ids · the first mocks pass §3 AND their live siblings pass the same suite (four pairs, 8/8 each) · the chain is visible on a basic dashboard. Evidence on main: `assembly/E1-EXIT.md` + `assembly/chain-report.html`.
- **Carried to E2 (tracked, not self-started):** ① CI wiring asserts the live negative's exit == 1 EXACTLY (standing rule — never "non-zero means failure") ② settlement/payout producers become real services at E2/E3 — the rig-minted `sob_*`/`payout_*` labels retire then ③ staging-environment isolation (the last E0 deferral) ④ the WO-1.3 verifier's E2 list, evidence-to-chain binding FIRST ⑤ dwell posture (record-vs-enforce) needs the founder at E2 scoping.

## 2026-07-10 · WO-2.0 · platform-contracts v0.5.0 — the failure-state taxonomy — in-review (built + verified on `canon/v0.5.0`; do NOT merge — founder's reviewer decides)
- No failures to report: **59/59 contracts tests** (104 workspace-wide: 59+20+15+5+5), "ALL GATES GREEN (positive suites pass; every negative fixture demonstrably fails)", typecheck 8/8, "check-snapshot-version-bump: snapshot changed with version bump 0.4.0 -> 0.5.0 — OK" — all log-copied.
- **PRIME DIRECTIVE HONORED — every name derived, none invented:** `docs/derivations/E2-taxonomy.md` carries a verbatim spec quote (document + line) for every enum member, reason code, fault class, and event name added. The table is repo governance, deliberately outside the drift-checked manifest (the generator sweeps `docs/` root only — asserted).
- **1. Order enum** (deliberate bump, NB-6 pattern): `+payment_failed` (Contract E2 "reservation-held-after-payment-fail" / §6 "a reservation stays held after payment failure") · `+cancelled` (SE-I10 / "refund/cancellation consume reason + evidence") · `+refunded` (B+7 "buyer refunded" / B+I-13). Now eight members; negative updated — the SE-I10-banned generic `'failed'` refuses at parse, exit 1 (log-copied). No `released` order state and no success-terminal added (not derivable / not mandated — scope note in the table).
- **2. EscrowTxn.status promoted** from bare string to `collect | hold | split | payout | refunded` — the aggregator flow named identically in both specs ("BCEAO-licensed aggregator (collect→hold→split→payout…)", Boutik l.78 / Shop l.71) + leg vocabulary. **⚠ RATIFICATION FLAG 1:** the WO's hint `'released'` has **no spec quote anywhere** (the only "released" in the specs is gated Cercle vocabulary) — under DERIVE-NEVER-INVENT it did not enter canon; a `'released'` leg negative proves the refusal, exit 1. `PaymentLegStatusSchema` was already the spec-verbatim `held|captured|refunded` — unchanged.
- **3. DeliveryOutcome** (new, strict): family `retry|reschedule|return|incident` (SE6.1 verbatim, the WO's cited authority) · reasonCode = the §6.4 six (`honest_absence|unusable_location|insufficient_balance|change_of_mind|repeated_abuse|fraud`) + `provider_failure` ("Honest absence / provider failure do NOT escalate") · `humanReasonRef` i18n key (Law 6 — text never inline) · `faultClass` ("fault-attributed") · attempt `{number, at, windowExpiresAt?}` ("One retry window (~15 min…)"). **Bare `'failed'` is UNREPRESENTABLE** — no member exists and the negative proves parse refusal, exit 1. **⚠ RATIFICATION FLAG 2:** Sera spec prose l.121 says `return_required` where SE6.1 (plan) + the WO say `return` — the WO's citation wins; the divergence is quoted in the table for ratification.
- **4. FaultClass verified, not extended:** the six members are spec-ENUMERATED verbatim (Boutik §5.6 l.148); routing quotes per member in the table (B+I-12 seller→Fund · SE buyer-fault ladder · "Séra-caused… = CustodyLiabilityClaim, not a fund payout" · provider). Claim shapes already carry exactly the §5.6 fields; claim `state` stays a spec-bare string (**no enumeration exists to derive — none invented**). Non-canonical faultClass negative refuses, exit 1.
- **5. Ops events:** `+reconciliation.alert.v1 · +saga.stuck.v1 · +dlq.parked.v1` (Contract E2 exit + §6 Standards, quoted). **The specs list NO refund/reversal event names** (the refund/earning-reversal saga is E3, shop plan l.23) — **none invented**; recorded as an E3 deferral.
- **6. D20 RECORDED (founder ruling 2026-07-10):** dwell is recorded and console-surfaced; **no enforcement fields exist** — doc comments on `PickupVerificationSchema` + `InspectionPolicySchema.dwellTargetSec`; no schema change.
- **7. Doc pass:** CONSUMING.md stale `@0.2.0` mechanism example → 0.5.0 (closes the WO-0D verifier's NOTE 8). Deferrals re-recorded: North Stars + prototype into /docs (founder files) · D19 coarseLocation spec edit (founder-side) · refund/reversal event names at E3.
- **8. Versions 0.5.0** ×6; deliberate snapshot bump — **101 exports** (was 94: +7 taxonomy exports). **The reference world still stands at 0.5.0:** all four reference mocks re-certify **8/8** and the reference chain runs **15/15** (log-copied). **No retro-breakage possible on E1 assembly:** it pins canon at `be1df7a` everywhere (grep-asserted: 1 pin line in `assembly/package.json` + 4 override lines in `assembly/pnpm-workspace.yaml`) — v0.5.0 reaches the apps only when they deliberately re-pin. RN-safe roots intact (scanner gate green in the full run).
- The fresh-context verifier verdict — mandated to spot-check three derivation quotes against /docs itself — is attached below and ships in the packet committed by the closing commit of this entry. Not merged.

### WO-2.0 verifier round (continues the in-review entry above)
- **Fresh-context verifier: VERDICT PASS at `80390f9`** (brief = prime directive + spec authority with line numbers + DoD; mandated to spot-check derivation quotes against /docs itself). Its three mandated spot-checks (`payment_failed` · the §6.4 reason codes · the ops events) all confirmed VERBATIM at the cited lines — and it went beyond the mandate, verifying **every remaining cited quote** and auditing the snapshot delta export-by-export ("ADDED = exactly the 7 taxonomy exports … No smuggled surface"). Finding titles quoted verbatim (standing rule): 1 "SHOP CITATION OVER-REACH IN TABLE §2" (**MINOR** — the leg enumeration exists only in Boutik l.143; Shop l.108 carries a bare `status`) · 2 "`dlq.parked.v1` VERB NOT SPEC TEXT" (NOTE) · 3 "STALE DESCRIBE TITLE" (NOTE) · 4 "PACKAGE.JSON REWRITE ARTIFACTS" (NOTE). Both ratification flags judged "honestly presented"; its own /docs grep confirmed `released` occurs only in gated Cercle vocabulary.
- **All four findings fixed in `6a0e773`** (Shop-citation corrected + the Boutik/Shop §5.6 textual non-identity flagged for the founder's spec fix list · dlq verb marked EDITORIAL in the table · describe title refreshed · six package.json restored to UTF-8 + trailing newlines, content JSON-identical). **Verifier delta re-check: "DELTA VERDICT — CONFIRMED FIXED"** at `6a0e773` — "The original PASS stands, now with zero open MINOR/NOTE items."
- **Rides to the founder with this review:** ⚠ ratification flag 1 (escrow `released` — no spec quote, excluded) · ⚠ ratification flag 2 (`return` vs `return_required` spec/plan divergence) · the newly-exposed Boutik/Shop §5.6 leg-status textual non-identity (spec fix list). The verdicts ship verbatim in the packet committed by the closing commit of this entry.

## 2026-07-10 · WO-2.0 · done (founder review PASS — merged)
- Self-anchoring guards verified empirically before merge: local `canon/v0.5.0` head equal to origin's (`b7f1f5f906ff2ba65f3c2feb7db1b2d67b9cd91f` both sides, ls-remote log-copied) · log contains all 7 anchors (`939abb3`/`6a0e773`/`80390f9`/`0e08c72`/`1d3fa22`/`c1e497f`/`9983c50`) · `git diff --stat 939abb3..HEAD` touches only `_review/WO-2.0-review.zip` (Bin 0 -> 48075 bytes) · `run-gates.sh` at head exit 0, "ALL GATES GREEN (positive suites pass; every negative fixture demonstrably fails)".
- **Merged into `main`: merge commit `0ff6696`** ("merge(canon): v0.5.0 — E2 failure-state taxonomy, derived and ratified — founder review passed"), pushed; `git rev-parse HEAD` → `0ff6696a4d0d89bef24822711e452c9a0ff07f9f`. **This merge sha is the pin for the three failure-flow work orders.**
- **FOUNDER RULINGS, CLOSED (2026-07-10):** ① escrow `released` stays EXCLUDED from canon — the derivation was right, the WO prose was wrong; the escrow-status vocabulary is the aggregator flow `collect|hold|split|payout|refunded`. ② `return` is the canonical DeliveryOutcome family (plan SE6.1 verbatim; the spec-prose `return_required` yields). ③ the Boutik/Shop §5.6 leg-status textual non-identity joins the founder's source-edit list.
- **E3-entry item recorded:** refund/earning-reversal event names enter canon at the E3 bump **by named derivation** — no spec currently names them; they do not exist in canon until a spec or founder ruling names them.
- **Founder's source-edit list (three items, founder-side):** Boutik/Shop §5.6 leg-status non-identity · D19 `coarseLocation` spec edit · Séra l.121 (`return_required` → aligned with the ratified `return`).
- **Standing rule acknowledged (adopted 2026-07-10, one line):** every review packet is attached in the founder chat at done-report time, in addition to the committed `_review/` copy.

## 2026-07-10 · WO-2.8 · E2 ASSEMBLY — the eight adversarial scenarios, live — in-review (built + verified on `e2/assembly`; do NOT merge — founder's reviewer decides)
- **PRE-FLIGHT (founder addendum applied):** all six content anchors fetched and log-copied BEFORE branching — sera main `6213d41` (contains `eff4d39` WO-2.7 merge) · boutik-plus main `7e4901d` (contains `92059cb` WO-2.7 merge) · shop-plus main `74913d7` (contains `af4e7fc` WO-2.5 merge) · platform-contracts main contains `0ff6696`. These anchored mains ARE the pins.
- No failures to report, every figure log-copied: **fifteen-step E1 regression GREEN at the new pins on the FIRST run** (nine ids, money reconciled to the franc: 8 500 + 2 000 payouts, kill-switch demo intact — E2 cost E1 nothing); **all §3 pairs re-certified 8/8** (mock and live, four domains); **8/8 scenarios live** (driver exit 0, `E2-SCENARIOS.md` written by the run, never by hand); root suites 94/94 (15 i18n + 59 contracts + 20 certification incl. the reference chain); root gates ALL GREEN under the NEW strict convention; assembly typecheck clean; staging gate positive + collision negative (exit 1, both collisions named); **cold proof green from committed bytes at `4faca31`** (fresh clone → root bootstrap → assembly install → build → fifteen steps → 8/8 pairs → 8/8 scenarios → staging both arms).
- **1. RE-PIN:** all 16 app pins + canon → the anchored shas; **the `.pnpmfile.cjs` workspace-shim carried its own sha table — the missed pin site, found when the first e1 run loaded flags-client at the OLD sha**; lockfile by regeneration (a clean `node_modules` purge was required — pnpm re-resolved stale hook output otherwise); zero old shas (scripted assert).
- **Integration cost surfaced by the re-pin, adapted (assembly-side only):** the E1 eligibility live-certifiable minted version variance by RESUBMITTING evidence — the post-2.7 sera pin refuses that (`evidence_already_submitted`, probed live) and accepts at most one bundle per order. Replaced with the REAL corrective verification round-trip (WO-2.7 cycles: refused attempt with a failed qty check → `openNewVerificationCycle` under its own cycle-2 code → accepted) — validated lands at v8 vs v6, the §3 ordered/out-of-order checks hold, and the certified surface exercises the hardened machinery instead of the retired trick.
- **2. THE SCENARIO MATRIX (Contract E2 exit verbatim, one row each in `assembly/E2-SCENARIOS.md`):** ① reservation-held-after-payment-fail — alert `reservation_held_after_payment_failure`, recovery = release through the LIVE reservation DO, idempotent replay proven, no escrow exists; ② paid-order-no-supplier-decision — boutik desk alert past 120 min + B+I-13 `refund_required` record (amount COPIED, buyerPriority) + seller-fault claim, silent under deadline, exactly once; ③ ready-package-no-task — alert past 60 min, silent when a task exists, package stays READY (dispatch is the runbook action); ④ impossible-custody — TWO live spines each holding one courier custody of the same package (the conflict is store-seam-visible), OpsMonitor alert, NO auto-release (wrong-code drop refuses closed); ⑤ evidence-not-validated — silent at −5 min, alert past the 30-min policy, recovery `decideValidation` → validated; ⑥ settlement-eligible-not-submitted (**built here**) — both REAL chain obligations alert past the TTL, never re-fire, obligations byte-identical; ⑦ payout-not-reconciled (**built here**) — the certified provider's own responses reconcile CLEAN (0 alerts); dropping the supplier `payout.paid` → exactly one alert naming `submitted_without_paid`, escrow + obligations byte-identical (no double-count); ⑧ offline-backlog — 30 real sera spines queued offline (queued = pending proven live: pre-flush validation refused on every spine), threshold alert, recovery = `flushOfflineEvidence` 30/30 through the server_confirmed binding, drained evidence validates, empty queue observes clean. **⏳ POLICY VALUES flagged founder-tunable, all versioned:** `settlement-submission-ttl.v1` (60 min) · `offline-backlog-policy.v1` (25 bundles / 30 min) — no spec values exist; conservative defaults.
- **3. DLQ + stuck-saga LIVE:** a REAL certified-provider webhook torn at 70% parks byte-exact (sha256 on the park event) while the intact webhook passes; `saga.stuck.v1` silent under TTL, exactly once past it, never again.
- **5. Shop PWA queue honesty anchored at the pin:** `order.offline.queued` at `74913d7` verbatim (« C'est noté sur votre téléphone. Envoi dès que le réseau revient. ») — log-copied via `git show`, no working-tree trust.
- **6. Runbooks tranche 1 (Contract §5 format, versioned v1, French, owner: fondateur):** `assembly/runbooks/01–08 + README` — détection (the exact alert payload) · diagnostic (chain ids) · action de reprise referencing ONLY real commands (`release` DO command, `decideValidation`, `flushOfflineEvidence`, `ReadyQueue.onTaskReady`/`AssignmentBook.assign`, `verifyChain`) · E3 boundaries stated honestly (no refund execution, no payout submission — the records/alerts ARE the trace until E3).
- **7. Staging isolation (the last E0 deferral, HONEST SCOPE):** `config/env.{default,staging}.json` + `EnvProfileSchema` loader + `assertProfilesIsolated` (equality AND prefix-collision law, because storage keys are `prefix+id`); gate positive + planted collision negative (shared flag namespace + `resv:` prefix-colliding) → exit 1 naming both. Documented plainly: this is the isolation MECHANISM; deployed staging infra arrives with real cloud accounts at E3/E4 — `providerMode` is schema-locked to `sandbox` (a real mode is unrepresentable until the Real-Money Gate).
- **8. exit==1 convention echoed:** the root `run-gates.sh` accepted ANY non-zero for negatives — the strict form immediately CAUGHT a non-conformer: the type-level secret-substitution negative exits with tsc's 2; normalized behind `show-secret-separation-negative.mjs` (compile-refused → exit 1; compiled → exit 0 so the harness flags the vanished type wall). Root gates re-run green under the strict convention.
- **CROSS-APP INTEGRATION NOTE (flagged, no action taken — founder's WO-2.4/2.5 seam review):** sera's `consumeDoorPaidSignal` (WO-2.4/2.7) consumes the PROVIDER event `payment.door_leg_confirmed.v1` with actor provenance, while shop's WO-2.5 door-paid signal is its own `order.status_projection_updated.v1` projection. No canon conflict — SE-I11 gates custody on provider-confirmed payment and provider webhooks are the only payment truth, so the E3 door assembly wires the provider webhook to BOTH consumers; shop's projection remains a buyer-surface event. Flagged so the E3 door work order names the wiring explicitly.
- FORBIDDEN respected: no provider submission/refund execution (the two seam monitors detect and alert only — nothing marked paid, nothing released) · no new canon (every event emitted is a v0.5.0 name; the policy objects are app/assembly data) · no §3 behavior weakened (the eligibility adaptation exercises MORE hardened machinery, all pairs still 8/8) · every runbook action references a command proven in this run's evidence.
- The fresh-context verifier verdict — mandated to drive two scenarios of its own choosing end-to-end, attempt one recovery-state bypass and one alert suppression, and re-run the fifteen steps — is attached below and ships in the packet committed by the closing commit of this entry. Not merged.
- **VERIFIER VERDICT (fresh-context; full text verbatim in `_review/WO-2.8-review.zip` → `verifier-verdict.txt`): WO-2.8 VERIFIER VERDICT — PASS, verified at `99c8893`**, diff byte-identical, every DoD command re-run by the verifier's own hands (fifteen steps 15/15 to the franc, all §3 pairs 8/8, 8/8 scenarios with E2-SCENARIOS.md regenerating byte-identical, root suites 104/104, root gates green under strict exit==1, cold proof ALL ARMS GREEN). **The founder's mandate executed in full:** two self-driven scenarios with the verifier's OWN probes — scenario 1 proved the DO truly held via a real `expire→not_expired` refusal, scenario 7 caught **two one-franc payout tampers the committed driver never tried** (`paid_amount_8499_diverges_from_submitted_8500` · `submitted_8501_diverges_from_obligation_8500`) with obligations byte-identical throughout; the impossible-custody recovery-state bypass refused closed five escalating ways (including the CORRECT drop code without the proof path); four alert-suppression games all defeated (clock reset via re-registration, episode burning, under-TTL spam, wrong-party submissions) — no silence while any condition held.
- **Findings (titles verbatim): 1. CROSS-SPINE CUSTODY FREEZE IS PROCEDURAL, NOT CODE-ENFORCED — MINOR** (a conflicted spine completing its OWN full proof path still emits eligibility while the store-seam conflict stands; committed claims honestly scoped, `custodian_conflict` exists in the spine → **carried by name to the E3 door/infra work order: store-level single-ledger-per-package wiring, NON-OPTIONAL**). **2. LOCKFILE RESOLUTION URLS REGENERATED IN SSH-STYLE FORM — MINOR → FIXED on this branch (`a841b82`: 78 hybrid + 18 scp → 0, zero duplicate keys, frozen install + e1 + scenarios re-proven) — the verifier's DELTA RE-CHECK, same fresh context: "WO-2.8 DELTA RE-CHECK — CLEARED … Regressions: none", verified at `a841b82`, with a line-level purity proof (every changed line is exactly the URL rewrite; zero pins, specifiers, or non-lockfile bytes moved).** 3. DRIVER'S SCENARIO-1 HELD VIEW IS HAND-BUILT — NOTE (behavior unaffected; the verifier's own probe supplied the stronger DO-held proof; noted for a future driver strengthening). 4. JOURNAL "root suites 94/94" NAMES THREE OF FIVE PACKAGES — NOTE (accurate as scoped; the full count is 104/104 incl. ui-tokens 5 + kernel-types 5). **No blocking findings; 2 closed on-branch and re-verified; 1 carried by name to E3; 3/4 no action.**

## 2026-07-10 · WO-2.8 · E2 ASSEMBLY — DONE (founder review PASSED; merged to main; **E2 EXITED**)
- **Merge: `62d7284ec45ee2f5fe073c9f59250b87c870dd6b`** — "merge(e2): E2 assembly — the eight adversarial scenarios live — founder review passed; E2 EXITED" (branch `e2/assembly`: 4faca31 build · 99c8893 journal · a841b82 lockfile fix · c8f4067 closing commit + packet). Pushed; ls-remote confirms origin/main at the merge sha. Merge-time guards all passed, log-copied: heads equal; three anchors present; `a841b82..HEAD` = JOURNAL.md +2/−0 additive + `_review/` only, nine quoted verdict strings content-matched to the packet's `verifier-verdict.txt` (1 hit each); root gates exit 0 at head.
- **E2 IS EXITED against the Contract's exit binaries (l.31), evidence on main:** each of the eight adversarial scenarios produces the defined recovery state + a reconciliation alert — `assembly/E2-SCENARIOS.md` (written by the live run, per-row recovery/alert/evidence) · the first-tranche runbooks exist and are versioned — `assembly/runbooks/01–08 + README` (Contract §5 format: détection · diagnostic · action de reprise · owner: fondateur · v1) · DLQ + stuck-saga detection live (torn real-producer webhook parked byte-exact; `saga.stuck.v1` exactly once past TTL). The fifteen-step E1 chain runs green at the same pins — E2 cost E1 nothing.
- **FOUNDER RULINGS, closed 2026-07-10:** ① `settlement-submission-ttl.v1` (60 min) + `offline-backlog-policy.v1` (25 bundles / 30 min) **ACCEPTED as CTO defaults — versioned data, tune at E4 telemetry.** ② Verifier finding 1 **RATIFIED as a named, non-optional E3 door/infra item: store-level single-ledger-per-package, in the SAME E3 work order that wires the provider door webhook to BOTH consumers** (this entry's WO-2.4/2.5 seam flag). ③ **THE E3-ENTRY LEDGER IS FIXED AT SEVEN NAMED ITEMS:** refund/reversal event derivation · transport webhook authenticity · late-webhook alert · single-ledger-per-package · door-webhook wiring · ISO-normalization boundary guards · real settlement/payout producers.
- **The two document-class carries restated (source: the boutik-plus/sera WO-2.7 done-entries at the anchored mains `7e4901d`/`6213d41`, verbatim):** "the strict-> same-millisecond comment (one line, next touch of protection.ts) · the ISO-normalization guard at the E3 boundary" — per the founder's WO-2.7 ruling ④: "the same-millisecond strict-> choice is CORRECT (one-line comment, nothing more); the lexicographic ISO comparison gets a normalization guard when timestamps first arrive from outside the process (E3 boundary work, noted)."
- Verifier NOTEs carried without action: the scenario-1 driver's hand-built held view (the verifier's own probe supplied the stronger DO-held proof — a driver strengthening candidate at the next assembly touch); the 94/104 root-suite scoping clarification.

## 2026-07-11 · WO-0E · canon v0.6.0 — the Signature Design Doctrine enters canon — in-review (built + verified on `canon/v0.6.0`; do NOT merge — founder's reviewer decides)
- **BASE (self-anchored):** branched off origin/main `82b22ef`; log-verified to contain `f637188` (the E1-assembly merge — the order's prose labeled it "the E2-assembly merge"; both it AND the true E2-assembly merge `62d7284` verified present, so no stop — label correction recorded per the relay-prose discipline) plus `0ff6696`, all log-copied.
- No failures to report, every figure log-copied: suites **111/111** (12 ui-tokens + 5 kernel-types + 15 i18n + 59 contracts + 20 certification — the reference world rides in certification: mocks 8/8, chain 15/15), typecheck clean, **ALL GATES GREEN** (drift at 0.6.0 over the 9-doc manifest; shape-freeze at the new snapshot; RN-safe scan: ui-tokens root graph 3 files, no node builtins, externals none — the new groups are data only), **cold proof green from committed bytes at `cc4607c`** (fresh clone → install → build → 111/111 → all gates → doctrine sha identical).
- **1. `docs/DESIGN-LANGUAGE.md` committed VERBATIM** — founder-signed (« design: OK », 2026-07-11), **sha256 `e478db70771fac8d5a9993a8530db4573d08b04548e70aa1860d3ccb039fa9b7`** (3 103 bytes); the manifest regenerated over it (9 docs, packageVersion 0.6.0, the SAME sha embedded — byte-match by construction). It binds every UX slice like the French Voice; work-order DoDs will carry « signatures applicables démontrées · test 5 secondes énoncé · budgets D17 prouvés sur l'export ».
- **2. ui-tokens v2 (TOKENS ONLY — the no-components law stands, RN-safe scan green):** `motion` v2 — doctrine durations 150/250 ms on quick/standard, `spring-soft` easing name + **⏳ CTO-default spring params (damping 20 / stiffness 250 / mass 1 — the doctrine names the curve family, not the physics)**, hard ceilings `celebrationMaxMs 800` / `countUpMaxMs 600`, `reducedMotionFlag 'prefers_reduced_motion'`. `celebration` per theme — the three named moments (`produit_pret`/`premiere_vente`/`course_validee`, no fourth: const-tuple), each motif palette `[primary, primaryStrong, primarySoft]` + halo `primarySoft` FROM ITS OWN THEME, timing REFS into the motion law (three themes celebrate in three different palettes, asserted). `money` — hero amount **⏳ CTO default 40/46/800** (« Vous recevrez X F » the hero of its screen), `tabularNumerals: true`, count-up as a REF (never a second clock), receipt emphasis with the reconciled badge pointing at the shared `verifiedBadge` token. `landmark` — hierarchy repère→indications→zone descending (asserted), **⏳ CTO-default icon-name slots** (assets are app work). 12 tests incl. the TYPE-LEVEL per-theme resolution test (`expectTypeOf` on celebration/motion through each theme, plus a runtime anchor).
- **3. Versions 0.6.0** across root + five packages + intra-deps (`certification→contracts/ui-tokens`, `contracts→kernel-types`); CONSUMING.md example version bumped; **api-surface snapshot updated DELIBERATELY** (`UPDATE_API_SNAPSHOT=1`; delta = packageVersion ONLY — exports and schemas byte-identical, 1 insertion/1 deletion); lockfile regenerated (0 scp/ssh forms, 0 duplicate keys — scripted asserts); root drift args 0.5.0→0.6.0.
- **DEFERRALS RE-RECORDED (reasons unchanged, riding to v0.7.0):** `conformity_mismatch` + the Séra l.121 / Shop l.108 / D19 derivations remain **BLOCKED ON THE FOUNDER'S FOUR SOURCE EDITS** (they enter at v0.7.0 when the edits land) · `probationLimits` tightening + protection-claim-state enumeration ride the same bump · North Stars still awaited. Nothing founder-blocked ships in this bump.
- FORBIDDEN respected: no components/JSX/runtime code in ui-tokens (data + one pure token-composition function, RN-safe-scanned) · money/secrets/taxonomy untouched (snapshot delta proves it: packageVersion only) · no founder-blocked derivation shipped · the doctrine text unaltered (sha-anchored in the manifest and this entry).
- The fresh-context verifier verdict — mandated to byte-verify the doctrine against the order's text, spot the snapshot delta export-by-export, and attempt a doctrine edit smuggled as "formatting" (must be zero-diff) — is attached below and ships in the packet committed by the closing commit of this entry. Not merged.
- **VERIFIER VERDICT (fresh-context; full text verbatim in `_review/WO-0E-review.zip` → `verifier-verdict.txt`): WO-0E VERIFIER VERDICT — PASS, verified at `523cbea`**, every DoD command re-run by the verifier's own hands (111/111, typecheck, ALL GATES GREEN, RN-safe all four packages, cold proof from a fresh clone with the doctrine sha identical). **The founder's mandate executed in full:** ① doctrine byte-verified in TWO layers — cmp against the order copy byte-identical AND an independent line-by-line content audit of every required element (sha `e478db70…` on the file, the copy, AND the HEAD blob; both manifests carry it); ② the snapshot delta parsed export-by-export — 101/101 exports, 67/67 schemas, added/removed/changed all empty, the ONLY delta `packageVersion 0.5.0→0.6.0`; ③ the formatting smuggle CAUGHT four variants at three layers (drift-check exit 1 naming the sha ×4; the full committed gate run fails on a tampered clone; manifest regeneration exposes the sha change as a tracked diff) — no reformatting of this file exists that the pipeline tolerates.
- **Findings (titles verbatim, all NOTE, all no-action): 1. STALE "v0.1.0" PROSE IN UI-TOKENS** (the no-components LAW holds and is enforced; the version reference in two comments is stale — cosmetic, next touch). **2. MOTION DURATIONS OUTSIDE THE 150–250 BAND** (instant 80 ms and celebrate 420 ms — the CTO owns the reading the verifier recorded: quick/standard carry the law exactly, celebrate is governed by the separate 800 ms celebration ceiling, instant is sub-band feedback consistent with D17's <100 ms). **3. TWO PURE FUNCTIONS, NOT ONE** (the non-exported `makeCelebration` — compliant data assembly). **4. COLD PROOF RUN AT HEAD, JOURNAL CLAIMS IT AT cc4607c** (trees differ by JOURNAL.md only; the proof covers the claim). **5. "DELIBERATE" SNAPSHOT UPDATE IS PROCESS TESTIMONY** (the substance is verified: packageVersion-only delta + shape-freeze green + the tampered-snapshot negative demonstrably fails). **No blocking findings; nothing to fix on this branch.**

## 2026-07-11 · WO-0E · canon v0.6.0 — DONE (founder review PASSED; merged to main)
- **Merge: `539dbc8ac8372d0d98606428258f79ad0f1ceaaa`** — "merge(canon): v0.6.0 — the Signature Design Doctrine enters canon — founder review passed" (branch `canon/v0.6.0`: cc4607c build · 523cbea journal · 705d3d2 closing commit + packet). Pushed; ls-remote confirms origin/main at the merge sha. Merge-time guards all passed, log-copied: heads equal; both anchors present; `523cbea..HEAD` = JOURNAL.md +2/−0 additive + `_review/` only, seven quoted verdict strings (incl. the doctrine sha) content-matched to the packet's `verifier-verdict.txt` (1 hit each); gates exit 0 at head.
- **FOUNDER RULINGS, closed 2026-07-11:** ① the three ⏳ CTO-default token groups (spring-soft physics · money hero sizes · landmark icon slots) **ACCEPTED — versioned tokens, tuned by the founder's eye at gallery reviews from the next UX slice onward.** ② The finding-2 motion-band reading **RATIFIED as the verifier recorded it** (quick/standard carry the 150–250 law exactly; celebrate rides the separate 800 ms celebration ceiling; instant is sub-band D17 feedback). ③ Finding 1 (stale v0.1.0 prose) rides to the next ui-tokens touch.
- **The merge sha is the canon pin for every UX slice from la capture guidée onward; the doctrine binds their DoDs like the French Voice; the founder-blocked derivations ride to v0.7.0 on his four source edits.**

## 2026-07-11 · WO-0F · the founder's Master Reference — VERIFICATION PASS STOPPED (5 flagged rows; founder arbitrates; the document is NOT committed)
- **BASE (self-anchored):** branched `canon/master-reference` off origin/main `8e1f80d`; log-verified to contain `539dbc8` (the v0.6.0 canon merge), log-copied.
- **The founder's file, staged byte-exact and untouched:** sha256 `8f94051609eddc4bc378469aab69f76b7415e25b559021ad624c0b02803eb8ad` · 58 755 bytes · 721 lines. Not one byte edited; NOT committed to `/docs` — the WO forbids shipping anything the audit flags.
- **Verification pass complete over all nine `/docs` canon documents** (Contract · three Build Specs · three Building Plans · PERF-BUDGETS · DESIGN-LANGUAGE), claim → source line: **`docs/derivations/MASTER-REFERENCE-AUDIT.md`** (committed on this branch). The money spine verifies exactly — the §2.2 waterfall formula-identical to spec §5.4 in all three specs, the §2.3 baseline to the franc, the §3.2 boundary step-for-step, the fleet gates value-exact, the §6.1 inspection disclaimer and the Contract §10.5 North-Star line verbatim.
- **THE FIVE FLAGS (full quotes in the audit; per the WO: STOP, do NOT edit the founder's document, the founder arbitrates):** ① MR §3.1's "four secrets" table lists the **custody seal** as the fourth secret — canon's CI-enforced list (identical in all three specs) is readiness challenge · pickup code · drop code · **`HandoffAuthorization`**. ② Option-B eligibility given as a complete three-condition list ("all must hold") — canon's §6.1 gate has **five** (category-inspectable and network-reliable-zone omitted; cap is "pilot ~25 000 F", not flat). ③ Free delivery "subtotal ≥ 15 000 F in dense zones" — **no such number anywhere in `/docs`** (grep-verified) and the fee-table/zones Decision is ⏳ open (Séra §13; the MR's own Part 15). ④ The refusal-ladder tiers are more permissive than canon §6.4 (canon escalates at the 1st buyer-fault, prepay-only at the 2nd, then suspension; the MR starts at "repeated", omits suspension). ⑤ Two normative lists abridged under their canonical names: B+I-01 (3 of 5 conditions) and the Cercle build gate (5 of 10 items).
- **Report-only (no arbitration needed):** Part 12 numbered 9.x / Part 13 numbered 10.x (founder-side cosmetic, pre-classified by the WO) · PackLab "~50% waypoint" has no `/docs` source (North Star absent; values ⏳ open, gate closed) · Séra spec §9 already homes «Fonds de protection» + «Éligibilité acheteur» desks in the dispatch/ops console — material input to D22 · wording nuances each stated precisely elsewhere in the MR itself (audit N4–N6).
- **HELD pending arbitration (the WO's step-2/3 items, untouched):** the byte-exact `/docs` commit + manifest regeneration · the "North Stars awaited → SUPERSEDED" deferral update · the D22 register entry (ops-console home; CTO recommendation stands: fifth repo `platform-ops`) · cold-clone byte-proof. Gates/suites on this branch are unaffected (docs/derivations is manifest-exempt by design — the manifest covers the 9 canon docs, unchanged).
- **VERIFIER VERDICT (fresh-context; full text verbatim in `_review/WO-0F-review.zip` → `verifier-verdict.txt`): "WO-0F STOP-STATE VERIFIER VERDICT — PASS"** — "All five flags re-verified by my own hands and CONFIRMED as real contradictions with verbatim-accurate quotes; the stop-state is clean (founder file byte-untouched, only the audit + an additive JOURNAL entry on the branch, manifests unchanged, gates/tests/typecheck green)". The WO's formatting-smuggle item recorded as "NOT EXECUTABLE at stop-state" (the target `/docs/ECOSYSTEM-MASTER-REFERENCE.md` was never committed — the consequence of the STOP, not an omission), with a drift-check tamper proxy executed instead: one trailing space on a scratch copy's DESIGN-LANGUAGE.md → refusal naming expected/got sha256, exit 1.
- **Verifier findings absorbed on-branch (delta commit `40f5107`, amended from `a90363e` for committer identity only — trees identical `cbc2738…`, same parent `548da74`):** finding 6 (missed MINOR, FLAG 3's class): the "16h–18h" Standard-delivery window is likewise unsourced and rides the same ⏳ scheduled-window-pricing Decision — added to FLAG 3 as a companion row so both are arbitrated together; finding 7: the audit's "field-exact" ladder-shape adjective corrected to six-of-seven fields (`buyerRef` omitted, harmless); finding 8: the SE-I12 MAY-NOT abridgment noted in N4; finding 1's precision note: FLAG-1 canon attribution reworded (LIST identical in all three specs, sentence quoted from Boutik:154's annotated form). **DELTA RE-CHECK, same fresh context: "WO-0F DELTA RE-CHECK — CLEARED", verified at `40f5107`, "Regressions: none."** — every hunk one of the four fixes, founder file still `8f94051…`, manifests unchanged, gates exit 0.
- **BLOCKED ON THE FOUNDER: arbitration of the five flags (+ the 16h–18h companion).** The WO's step-2/3 remain held: no byte-exact `/docs` commit, no manifest regeneration, no deferral supersession, no D22 register entry until he rules. Recommendation on record in the audit per flag; the four-secrets table (FLAG 1) and the refusal-ladder tiers (FLAG 4) misstate CI-enforced canon and should be corrected at source; FLAGS 2/5 can alternatively be ruled acceptable-as-abridgment under the audit's header note ("the specs remain normative on conflict"); FLAG 3 (+16h–18h) is the founder's number to make a Decision or an illustration. Not merged; nothing shipped that the audit flags.

## 2026-07-11 · WO-0F · ARBITRATION RECEIVED — canon wins on all five flags; execution HELD on the missing founder edit text
- **Founder ruling recorded verbatim in `docs/derivations/MASTER-REFERENCE-AUDIT.md` (Arbitration section):** canon wins on all five flags · the five corrections apply "EXACTLY as the founder's relayed text (they are founder-labeled edits)" · amended document enters as `docs/ECOSYSTEM-MASTER-REFERENCE.md` **v1.1** (sha in journal, manifest regenerated) · N-items accepted as wording nuances (closed) · then STEP 2–3 of the original order.
- **HELD, empirically:** the relayed correction text is absent from this session's record — the ruling message body carries no passages, and `ls` of the uploads directory shows only the original attachment (58 755 bytes, sha `8f94051…` re-verified). Per the untouchable-bytes law and the standing relay-content template (anchors/content from the session's own command-generated record, never from relay prose), the CTO does not draft founder-labeled edits. Requested from the founder: the five corrected passages (or the amended v1.1 file). Everything else is staged; on arrival: re-verify each passage against the canon lines quoted in FLAGS 1–5 → byte-exact commit as v1.1 → manifest (10 docs) → gates/suites/typecheck → cold-clone sha proof → journal deferral updates ("North Stars awaited" SUPERSEDED · D22 recorded · numbering nit report-only, now closed as accepted nuance).

## 2026-07-11 · WO-0F · the Master Reference enters /docs as v1.1 — arbitration executed (built + verified on `canon/master-reference`; do NOT merge — founder's reviewer decides)
- **The five founder corrections received relayed verbatim, re-verified against the audit's canon lines (all five PASS), and applied by script with per-line assertions against the byte-exact original (source sha `8f94051…` asserted; v1.0→v1.1 diff = exactly 8 replaced + 2 inserted lines).** Full record — passages verbatim + per-correction canon re-verification — in `docs/derivations/MASTER-REFERENCE-AUDIT.md`, "Arbitration executed".
- **`docs/ECOSYSTEM-MASTER-REFERENCE.md` (v1.1) committed — sha256 `f9b148b2063c7db847d6f0c9444558b30a7347e7a028b36017addca6d72d9dbd` (59 738 bytes).** Manifest regenerated: **10 docs, packageVersion 0.6.0**, the same sha embedded in BOTH copies (root + packages/contracts), byte-match by construction. Decision **D6 stays open** per the founder's explicit ruling — the document now says so in its own delivery section ("illustrative, never canon").
- **Deferral updates per the order's step 3:** the "North Stars awaited" deferral is **SUPERSEDED by the Master Reference** (the ecosystem-wide reference now lives in canon; the North Stars remain the authoritative feature specs for their gated layers, still absent from /docs, still gated) · **D22 recorded:** the platform Ops console's home — founder decision PENDING; CTO recommendation stands: a fifth repo `platform-ops` consuming the same pinned package, never writing another domain's truth (audit N3: Séra's console already homes «Fonds de protection» + «Éligibilité acheteur» desks — input to the ruling) · the Part-12 (9.x) / Part-13 (10.x) internal numbering: **closed as an accepted wording nuance** per the arbitration (founder-side cosmetic).
- N-items N1–N6 + the verifier's SE-I12/buyerRef notes: **closed as accepted wording nuances** (founder ruling).
- **FINAL VERIFIER VERDICT (same fresh-context verifier, full text verbatim in `_review/WO-0F-review.zip` → `final-verdict.txt`): "WO-0F FINAL VERIFIER VERDICT — PASS", verified at `85b0ff4`** (remote-equal by its own ls-remote). **"DIFF PURITY — PASS"** — the founder-original ↔ committed-v1.1 diff is exactly 8 replaced + 2 inserted lines, every hunk mapped 1:1 to the five relayed corrections, nothing else moved in 721 lines. Each correction confirmed FAITHFUL and flag-closing against the canon lines (C1→FLAG 1 · C2→FLAG 2 · C3→FLAG 3 + the 16h–18h companion, "D6 STAYS OPEN" · C4→FLAG 4 · C5→FLAG 5). **The WO's formatting-smuggle mandate executed on the committed doc: "all three refused"** (trailing space · guillemet swap · em-dash swap → drift-check exit 1 naming expected/got shas, three distinct got-shas) **+ manifest regeneration over a tampered doc flips the recorded sha in BOTH tracked copies — "No smuggle vector open."** State at `85b0ff4`: gates exit 0 · 111/111 · typecheck clean · branch surface = exactly the allowed files, 1 025 insertions, 0 deletions.
- **Findings: 1 MINOR + 2 NOTEs, none blocking, none touching the founder's bytes without his order:** MINOR — the C3 prose line (his exact relayed text) replaces a table row *mid-table*, splitting the delivery table (header + Standard row / paragraph / orphaned Cercle row) — **carried as a v1.2 cosmetic candidate, founder's edit to make**; NOTE — the C1 middle cell reads as a description under the "Held by" header (his verbatim text, cosmetic); NOTE — "D6" is unnumbered in the Séra spec itself but register-consistent with the founder's D-register (D17/D18/D19/D22 precedents) and came verbatim in his relayed C3.
- Cold proof (full arm, fresh clone at `85b0ff4`): install → build → 111/111 → gates exit 0, doc byte-sha `f9b148b2…` reproduced from committed bytes. Do NOT merge — the founder's reviewer decides.

## 2026-07-12 · WO-0F · the Ecosystem Master Reference — DONE (founder review PASSED; merged to main)
- **Merge: `ea1791b78be3b88e7e3088783b522237d4170a90`** — "merge(canon): WO-0F — Ecosystem Master Reference v1.1 (founder-arbitrated) enters /docs — founder review passed" (branch `canon/master-reference`: 548da74 audit/STOP · 40f5107 verifier deltas · 4fd501d stop-state closing · 150296d arbitration recorded/held · 85b0ff4 v1.1 + manifest · 3bdce07 final closing + packet). Pushed; ls-remote confirms origin/main at the merge sha. Merge-time guards all passed, log-copied: heads equal; anchors 150296d + 85b0ff4 present; the delta beyond the final journal commit = JOURNAL.md +3/−0 additive + `_review/` only, five quoted verdict strings content-matched to the packet's `final-verdict.txt` (1 hit each); gates exit 0 at head.
- **The arbitration chain, one line:** five flags confirmed (fresh-context verifier, every quote re-derived from the sources) → execution held at the edit-text boundary under the untouchable-bytes law (no founder-labeled bytes authored by the CTO) → the founder's five passages relayed verbatim → script-applied with per-line assertions against the audited original → **v1.1 sha `f9b148b2063c7db847d6f0c9444558b30a7347e7a028b36017addca6d72d9dbd`**, diff purity 8 replaced + 2 inserted lines, smuggle battery refused 3/3.
- **"North Stars awaited" is formally SUPERSEDED** by the Master Reference on main (the North Stars remain the authoritative feature specs for their gated layers; still gated).
- **TEMPLATE LAW RESTATED (founder, standing):** any ruling authorizing text changes carries the full replacement text in the same paste — a reference to relayed text that is not itself in the paste is a hold, never a license to draft.
- **Founder's report-only nit, carried to the next packet by name:** packets carry `logs/branch-log.txt` by name — `evidence.txt` substituted it this once; future packets restore the canonical filename.
- The founder's approval of the script-application discipline (byte-asserted line replacement against the audited original) and the executed smuggle battery is on record. Open items riding forward: **D22** (ops-console home, founder pending; CTO rec: fifth repo `platform-ops`) · the **v1.2 cosmetic candidate** (the C3 prose line splits the delivery table — the founder's edit to make at his next touch) · **D6 open** (the document itself now says its delivery figures are illustrative).

## 2026-07-12 · WO-0G · canon v0.7.0 — the accumulated docket — in review (built + verified on `canon/v0.7.0`; do NOT merge — founder's reviewer decides)
- **BASE (self-anchored):** off origin/main `e6e50c2` (the WO-0F merge lineage), log-copied; gates green at head before any work.
- **DERIVE-NEVER-INVENT INVESTIGATION (by the CTO's own greps against /docs; the heart is `docs/derivations/V0.7.0-DOCKET.md`, every shipped name quote-anchored):** two items ship, one gap-journaled, one blocked-absent — stated plainly per branch.
- **SHIPS — Item 1, ui-tokens v3 THE INTERACTION GROUP (founder-docketed BY NAME at WO-4.2R):** `interaction` = four family-level constants (`pressScale 0.98` · `pressedOpacity 0.92` · `disabledOpacity 0.4` · `skeletonPulseFloor 0.4`) under « la loi du mouvement »; **`androidElevation` per-theme** (« Élévation visuelle par thème » — the doctrine differentiates elevation by theme; structurally per-theme via `makeTheme`'s third param, ⏳ CTO-default value `2` uniform until gallery tuning — differentiating the numbers now would invent a differentiation the doctrine gives only as direction). **All VALUES ⏳ CTO defaults** sourced from the WO-4.2R verifier's recorded laundered kit constants (the honest default equals what the kits already ship; the kits migrate off their token-laundering at the apps' next touches). RN-safe: ui-tokens root graph still 3 files, externals none (pure data).
- **SHIPS — Item 2, ICON SLOTS:** `cadenas` + `moto` join `landmark.iconNames` — **names only**, assets app-side. **Naming authority regrounded on the spec (verifier finding 1): Shop-Plus-Build-Spec.md:135** directly names *"lock/scooter icons"* (lock → `cadenas`, scooter → `moto`); the WO-0G order's "WO-4.4 docket" reference could not be located in any repo journal, so the slots rest on the spec quote, not the unverifiable reference (corroborated by Master Reference §6.2 « Paiement protégé » + Sera §7.2 `Motorcycle`).
- **SHIPS — Item 3, probationLimits TIGHTENING (contracts/ change, founder-ordered; maximum rigor + mandatory verifier):** `SellerTrustState.probationLimits` closed from `z.record(z.string(), z.unknown())` to the STRICT `ProbationLimitsSchema` of six spec-named limit keys (`maxActiveOrders` · `maxOrderValueFcfa` · `maxOrderQuantity` · `approvedCategoriesOnly` · `fullPrepayOnly` · `everyPickupVerified`), each derived from Boutik-Plus-Build-Spec.md:31–35's tier text (quotes in the docket), all optional. **The zero-deposit law (B+I-12) now holds at the type boundary:** a deposit-class money key (`requiredDeposit`/`reserve`/`caution`/`bond`/`sellerNet`) REFUSES at `.parse` — asserted (negative). `maxOrderValueFcfa` is the spec's named order-value CEILING, never a held/deposit amount (flagged for the founder).
- **NOT SHIPPED — Item 4, CLAIM-STATE ENUMERATION (GAP, do not invent):** no spec sentence anywhere in /docs enumerates `ProtectionClaim`'s lifecycle states (canon spells out SettlementObligation's `Locked→…→Paid|Held|Failed` but not the claim's; the only nearby `(state)` tokens are fund SOLVENCY and campaign state). `ProtectionClaim.state` stays `z.string().min(1)`; the boutik local vocabulary stays local. **Gap recorded for the founder's source-edit list.**
- **NOT SHIPPED — Item 5, conformity_mismatch (BLOCKED-ABSENT; the ABSENT branch ran):** the founder's four source edits have NOT landed — `conformity_mismatch` appears NOWHERE in /docs (grep exit 1); Séra l.121 still reads `return_required` (not renamed to `return`); Shop l.108 EscrowTxn legs still carry a bare `status`; `coarseLocation` is unchanged/pre-existing. The v0.5.0 derivation table is NOT extended; the reason code does NOT enter the §6.4 enum. **Journaled blocked on the founder's edits; rides to a later bump.**
- **Versions 0.7.0** across root + five packages + intra-deps; CONSUMING example bumped; **api-surface snapshot regenerated DELIBERATELY** — delta = EXACTLY `ProbationLimitsSchema` added + `SellerTrustStateSchema` changed, nothing else (proven export-by-export; no money/taxonomy schema moved); manifest regenerated (10 docs, packageVersion 0.7.0); run-gates drift args 0.6.0→0.7.0.
- **REFERENCE WORLD STANDS + assembly pins untouched BY CONSTRUCTION:** mock certification 4/4 adapters CERTIFIED 8/8 · the fifteen-step chain 15/15 with the nine-id correlation · assembly/ has ZERO changed files and pins `@platform/contracts` to git SHA `0ff6696` (v0.5.0), immune to this branch's working-tree bump.
- **EVIDENCE (all figures log-copied):** suites **123/123** (ui-tokens 20 + kernel-types 5 + i18n 15 + contracts 63 + certification 20 = 123; the earlier "106" was an arithmetic slip, verifier finding 2) · typecheck 0 · **ALL GATES GREEN** (drift 10 docs @0.7.0; shape-freeze at the new snapshot; RN-safe all four packages; certification 8/8×4 + chain 15/15; every negative fixture demonstrably fails) · cold proof follows with the verifier verdict.
- FORBIDDEN respected: every shipped name carries a derivation quote · no components in ui-tokens (data + the existing pure token-composition function) · no money/taxonomy touched beyond the docketed probationLimits close (snapshot delta proves it) · no app repos touched.
- Do NOT merge — founder review pending; the verifier verdict lands below.
- **VERIFIER VERDICT (fresh-context; full text verbatim in `_review/WO-0G-review.zip` → `verifier-verdict.txt`): "WO-0G VERIFIER VERDICT — PASS", verified at `fd68e6f`.** Every derivation quote spot-checked against /docs by the verifier's OWN hands and CONFIRMED (all six probationLimits keys trace verbatim to Boutik:31–35; interaction group + icon slots grounded; the negative test genuinely asserts a deposit key REFUSES). Both non-shipped branches confirmed CORRECTLY withheld — item 4 claim-state ("no `/docs` sentence enumerates ProtectionClaim's lifecycle … GAP determination correct"; corroborated by the prior E2-taxonomy derivation) and item 5 conformity_mismatch ("all four founder edits genuinely absent … ABSENT branch ran"). **The snapshot smuggle caught at BOTH gates:** a stray `SellerDepositSchema` export → shape-freeze exit 1; a same-version snapshot change → version-bump gate exit 1; the legit 0.6.0→0.7.0 bump accepted. Reference world stands (4/4 adapters 8/8, chain 15/15), assembly pins untouched (git SHA `0ff6696`), RN-safe ui-tokens 3 files externals none.
- **Findings (all MINOR/NOTE, none blocking): 1. MINOR — the "WO-4.4 docket" naming reference for the icon slots could not be located in any repo journal → FIXED on-branch (`de34fcd`): regrounded on the verifiable spec quote Shop-Plus-Build-Spec.md:135 "lock/scooter icons" (the verifier's own stronger-authority find), both docket + journal noting the WO-4.4 reference was unlocatable — the derive-never-invent discipline applied even to a founder-referenced name. 2. MINOR — the "106" test total was an arithmetic slip → FIXED to 123 (20+5+15+63+20). 3. NOTE — elevation cite l.16 heading / l.17 content → FIXED. 4. NOTE — ruling location "all three app repos" → FIXED to sera/JOURNAL.md:293 (the last-merged of the trio). 5. NOTE (strengthening) — cadenas/moto now cite Shop:135 directly. DELTA RE-CHECK, same fresh context: "WO-0G DELTA RE-CHECK — CLEARED", verified at `de34fcd`, "Regressions: none" — code/snapshot/versions/manifest byte-identical to fd68e6f, gates exit 0.**

## 2026-07-12 · WO-0G · canon v0.7.0 — the docket — DONE (founder review PASSED; merged to main)
- **Merge: `d56fd33a040e9e74c9e91fe1933757ac26ed5f24`** — "merge(canon): v0.7.0 — the docket (interaction tokens, icon slots, probationLimits strict close) — founder review passed" (branch `canon/v0.7.0`: 0db4952 build · fd68e6f lockfile · de34fcd verifier deltas · 87342e9 verdict appendix · 60a7c60 closing + packet). Pushed; ls-remote confirms origin/main at the merge sha. Guards passed, log-copied: heads equal (`60a7c60` both) · main parity pre-merge (`e6e50c2` both) · anchors 87342e9/de34fcd/fd68e6f/0db4952 all in the log (4/4) · delta beyond the final journal commit = only `_review/WO-0G-review.zip` (canonical closing-commit template) · three verdict strings content-matched to the packet + delta CLEARED · gates exit 0 at head.
- **RULINGS, closed 2026-07-12 (founder):** ① **the interaction group + per-theme androidElevation (uniform ⏳ default) ACCEPTED** — values tune at gallery reviews; the apps migrate off token-laundering at their next touches. ② **The icon-slot regrounding on Shop:135 is RATIFIED — derive-never-invent applies to founder-relayed references too** (the WO-4.4 reference was unlocatable; the slots rest on the spec quote "lock/scooter icons"); noted with approval. ③ **probationLimits' strict close ACCEPTED, with `maxOrderValueFcfa` confirmed an order-value CEILING, never a held amount** — B+I-12 now holds at the type boundary. ④ **The claim-state GAP and the conformity_mismatch BLOCKED-ABSENT branch are both correct; they ride to the bump after the founder's four source edits land.**
- **The merge sha is the pin for the apps' next touches** (they migrate off token-laundering there); **conformity_mismatch + claim-state enumeration remain BLOCKED on the founder's four source edits — the only canon items he still gates.**

## 2026-07-12 · WO-5.0 · canon v0.8.0 — GRAND TEINT enters canon (the founder's signed design system becomes token law) — in review (do NOT merge)
- **BASE (self-anchored):** off origin/main `d56fd33` (the v0.7.0 merge), log-copied; gates green at head before any work. The founder's design bundle (`design_handoff_grand_teint`, direction 1b, locked 2026-07) is the source.
- **VALUE FIDELITY — machine-enforced, zero values altered:** `docs/design/tokens.json` committed BYTE-EXACT into the repo as the source of truth, and a new gate **`scripts/check-token-fidelity.mjs`** deep-equals the BUILT `@platform/ui-tokens` against it — **all 17 groups match value-for-value, including the U+202F narrow-no-break-space money separator** (transcription verified exact). A negative fixture (`show-token-fidelity-negative.sh`) proves the gate catches a one-hex-digit tamper (#C2571B→#C2571C, exit 1). No structural impossibility was hit — every designer value expressed as-is (rgba translucency for hairlines/scrim; cubic-bezier easing strings; unitless `lh` line-heights) — nothing forced, nothing "improved", no STOP needed.
- **ui-tokens v0.8.0 (family.ts + themes.ts fully rebuilt as Grand Teint):** the ten value groups take their real designer values (every prior provisional default replaced — a gate asserts no ⏳/CTO-default marker survives) + **the four NEW groups land as the designer named them: `band` · `ribbon` · `skeleton` · `statusbar`.** Per-theme resolution: `boutikPlusTheme`/`shopPlusTheme`/`seraTheme` compose `sharedColour` + one app palette (Séra leads with amber accent on ink, carries no primarySoft — faithful). Canon export/theme names kept (`boutik-plus` etc., Law #10); the designer's boutik/shop/sera keys map 1:1. No components/JSX — data only, RN-safe root intact (3 files, externals none). The certification consumer (`report-html.ts`) updated to the new token names to keep the reference world green (surface→paper, line→hairline, `.colors`→`.colours`); no value invented.
- **Byte-exact canon commits (the DESIGN-LANGUAGE pattern):** `docs/GRAND-TEINT.md` = the bundle's design-system.md, **sha256 `b1a1bf6a982591da114b47bbe8970c6a41c69eb6d0191c93404f1fc7c1aa8834`** (manifest regenerated → **11 docs**, packageVersion 0.8.0, the sha embedded in both copies). DESIGN-LANGUAGE.md STAYS (the doctrine is the law; Grand Teint is its expression). `docs/design/` = components · motion · copy · flows · budget · proposals + tokens.json, byte-exact reference (manifest-exempt subdir). `assets/icons/` = the **26 canonical SVGs byte-exact + `icons.manifest.json` (name→sha256)** — a gate asserts every icon uses `currentColor`, names == `landmark.iconNames`, and **total raw bytes = 7356 (~7.2 KB)**; one source for all four repos. `assets/share-cards/` = the 5 templates + README, byte-exact.
- **Versions 0.8.0** across root + five packages + intra-deps; CONSUMING example bumped; **api-surface snapshot regenerated DELIBERATELY — delta = packageVersion 0.7.0→0.8.0 ONLY** (contracts shapes untouched, proven export-by-export/schema-by-schema); drift args 0.7.0→0.8.0.
- **REFERENCE WORLD STANDS + assembly pins untouched BY CONSTRUCTION:** mock certification 4/4 adapters CERTIFIED 8/8 · the fifteen-step chain 15/15 with the nine-id correlation · assembly/ has ZERO changed files and pins `@platform/contracts` to git SHA `0ff6696` (v0.5.0), immune to this branch's bump.
- **DEFERRALS re-recorded (unchanged):** `conformity_mismatch` + the `ProtectionClaim` claim-state enumeration remain **BLOCKED on the founder's four source edits** — nothing founder-blocked ships here.
- **EVIDENCE (all figures log-copied):** suites **121/121** (kernel-types 5 + ui-tokens 18 + i18n 15 + contracts 63 + certification 20) · typecheck 0 · **ALL GATES GREEN** (token-fidelity OK 17 groups + its negative caught · icon-manifest OK 26/7356 · drift 11 docs @0.8.0 · certification 8/8×4 · chain 15/15 · RN-safe all packages · every negative fixture demonstrably fails) · cold proof follows with the verifier verdict.
- FORBIDDEN respected: no components/JSX in ui-tokens (data only) · not one designer value altered (fidelity gate proves it) · money/taxonomy/secrets untouched (snapshot delta = packageVersion only) · no founder-blocked derivation shipped.
- Do NOT merge — founder review pending; the verifier verdict lands below.
- **VERIFIER VERDICT (fresh-context; full text verbatim in `_review/WO-5.0-review.zip` → `verifier-verdict.txt`): "WO-5.0 VERIFIER VERDICT — PASS", verified at `471e89f`.** The founder's mandate executed in full by the verifier's OWN hands: **VALUE FIDELITY — 16 values spot-checked bundle-vs-built, all MATCH, incl. `money.groupSeparator` proven U+202F at the codepoint (0x202f on family.ts source, dist, and tokens.json — "the genuine narrow no-break space", NOT 0x20)**; the fidelity gate covers all 17 groups (nothing escapes); its negative catches a one-digit tamper. **FORMATTING-SMUGGLE CAUGHT:** a /tmp copy re-indenting family.ts + flipping one colour digit + "whitespace-normalizing" U+202F→space → the gate exited 1 naming both — "No value edit can hide as formatting." **BYTE-EXACT:** GRAND-TEINT.md ≡ bundle design-system.md (sha `b1a1bf6a…` matches exactly), tokens.json + 6 design docs + 26 icons + README + 5 share-cards all IDENTICAL (icon shas recomputed against the manifest); icon-manifest OK 26/7356 all currentColor. **SNAPSHOT DELTA = exactly ONE leaf, packageVersion 0.7.0→0.8.0** (102 exports both sides, contracts shapes untouched). Reference world stands (8/8×4, chain 15/15), assembly pins git SHA `0ff6696`, DESIGN-LANGUAGE.md unchanged, RN-safe ui-tokens 3 files externals none, suites 121/121, typecheck 0.
- **Findings (3, all NOTE, none blocking): 1. the certification `report-html.ts` consumer followed the ui-tokens public rename** (`neutralColors`→`sharedColour`, `.colors`→`.colours`) to keep compiling — Node-only chain-report tooling, never app runtime, in scope for a ui-tokens bump, still certifies 8/8×4. **2. ui-tokens' public API is a breaking rename vs v0.7.0** — expected for a signed design-system replacement; downstream apps pin by git SHA and adopt on their next touch. **3. the deferred items (conformity_mismatch, claim-state) correctly ABSENT** from shipped shapes (only the JOURNAL re-affirmation of their deferral; the snapshot's zero new exports confirms nothing founder-blocked shipped). No BLOCKING findings; no altered value, no byte mismatch, no smuggled export.
- **BUILD-TIME CORRECTION (honest note):** the build commit was first made on `main` (a skipped branch-checkout); caught and corrected BEFORE any push — the commit was moved to `canon/v0.8.0` and `main` restored to `c46293c` (= origin/main, never touched). Zero content impact; the branch base, anchor `d56fd33`, and commit are all correct.
- Cold proof green (fresh clone at `471e89f`: install/build/test 0, gates 0, GRAND-TEINT sha `b1a1bf6a…` reproduced from committed bytes). Do NOT merge — the founder's reviewer decides.

## 2026-07-12 · WO-5.0 · canon v0.8.0 — GRAND TEINT enters canon — DONE (founder review PASSED; merged to main)
- **Merge: `2472e7ae61a2ded70f4b4e8caad3b8fcb2d5df55`** — "merge(canon): v0.8.0 — GRAND TEINT enters canon; tokens are the designer's, byte-exact and fidelity-gated — founder review passed" (branch `canon/v0.8.0`: 036a04d build · 471e89f lockfile · f855be2 verdict appendix · f75800b closing + packet). Pushed; ls-remote confirms origin/main at the merge sha. Guards passed, log-copied: heads equal (`f75800b` both) · main parity pre-merge (`c46293c` both) · anchors 036a04d/471e89f/f855be2 all in the log (3/3) · delta beyond the final journal commit = only `_review/WO-5.0-review.zip` (canonical closing-commit template) · three verdict strings content-matched to the packet · gates exit 0 at head (token-fidelity + icon-manifest inside).
- **RULINGS, closed 2026-07-12 (founder):** ① **the ui-tokens PUBLIC API RENAME (neutralColors→sharedColour, .colors→.colours) ACCEPTED as a deliberate breaking change** — the design system was replaced, not patched; the apps pin by sha and adopt at their Grand Teint touches, where they are rewritten anyway. ② **The certification report-html.ts consumer update ACCEPTED** (Node-only tooling, in scope for a ui-tokens bump, still certifies 8/8×4). ③ **The token-fidelity gate is now STANDING LAW: no future slice may alter a designer value without a founder ruling — the gate is the enforcement, and it stays.**
- **The merge sha is the pin for every Grand Teint UI slice; the apps adopt the new token API at their next touches; conformity_mismatch + claim-state remain BLOCKED on the founder's four source edits.**

## 2026-07-12 · WO-5.2 (the founder's spec edits + the attribution/conformity canon they unblock) · in review — branch `canon/attribution`
- **Anchoring:** branch off main `7a6824d` (v0.8.0 merge `2472e7a` present in the log). **DERIVE-NEVER-INVENT governed PART B absolutely.**
- **PART A `47dbd73` — the ten founder edits (relayed verbatim, located by content, pre-edit asserted):** A1 Sera DeliveryOutcome `return_required`→`return` · A2 Shop §5.6 EscrowTxn leg `status: 'held'|'captured'|'refunded'` · A3 Sera-Plan SE5.1 append (« Un refus valide à l'inspection porte le code `conformity_mismatch`. ») · A4 Shop §6.4 reason enum += `conformity_mismatch` · A5 Sera EvidenceBundle `coarseLocation?` · A6 §4.1 « Attribution a deux portées » (product|identity) · A7 `ResellerShortCode` · A8 `AttributionArrival` + 30-day versioned TTL · A9 new invariant SP-I09b (precedence) · A10 « La vitrine est une surface acheteuse ». The /docs diff is EXACTLY these ten (verifier-confirmed, no smuggle); the only non-founder token is the structural `### 4.1 Attribution` heading. Outer relay « » treated as delimiters; inner « Livré par Séra »/« Paiement protégé » kept as content.
- **TWO REVIEWER DRAFTING SLIPS caught against canon BEFORE any derivation — the derivation discipline caught both:**
  - **A2 relayed as `released`; caught against the ratified E2-taxonomy exclusion (« no spec names `released` for escrow or payment legs ») and Boutik §5.6 l.143 `status(held|captured|refunded)`; corrected to `captured` by founder ruling before any derivation** (`e8615e7`). Shop §5.6 is now value-identical to Boutik §5.6 — the entire purpose of A2 (the flagged Boutik/Shop non-identity). The `released`-refuses-at-parse negative stays LIVE.
  - **A7's « ambiguity-free charset » clause contradicted its own example `AICHA-4821` (which carries `I` and `1`); caught before derivation; corrected to a STRUCTURAL schema by founder ruling** (`8efac60`). Position (`PRENOM-NNNN`) is the disambiguator, not a character-set restriction.
- **PART B `034608e` — canon v0.9.0, every name traced to a quoted spec sentence (`docs/derivations/ATTRIBUTION-AND-CONFORMITY.md`):** `conformity_mismatch` → `DELIVERY_FAILURE_REASONS` (A3+A4); DeliveryOutcome family `return` + Shop leg `captured` now MATCH their specs (asserted). `AttributionScope = product|identity` (A6) — the v0.8.0 `AttributionScope` ({kind:listing|store|campaign}) target INLINED into `AttributionToken`, byte-identical parse (E1/E2 pins hold, asserted), freeing the canonical name. `AttributionRef` discriminated: offerId REQUIRED on product, FORBIDDEN on identity (`.strict()` refuses at parse). `ResellerShortCode` `/^[A-Z]{2,12}-[0-9]{4}$/` + forgiving `normalizeShortCode` (hyphen at the unique letter/digit boundary — normalization proven unambiguous) + `shortCodeToSlug` lowercase (A7). `AttributionArrival` + `ARRIVAL_TTL_POLICY` (versioned 30-day policy DATA, never a bare constant) (A8). `resolveAttribution` (SP-I09b/A9): **locked > explicit code > most-recent unexpired arrival > NONE**; NONE never falls back to the platform (no such branch). **`claim-state` STILL DEFERRED — no spec names the lifecycle; not invented** (WO-0G ruling ④ unchanged).
- **Evidence (my own runs):** typecheck 0 (5 packages) · **contracts 84** (incl. `attribution.test.ts` **+21** negatives/arms: offerId-on-identity refuses · no-digits/5-digits/non-ASCII short-code refuse · the four resolver arms · expired-arrival · conformity_mismatch parse-positive + unknown refuses · AttributionToken byte-compat) · **certification 20** (chain **15/15**, mocks **8/8**) · ui-tokens 18 · i18n 15 · kernel-types 5 = **142 tests** · **`run-gates.sh` EXIT 0** (ALL GATES GREEN; every negative fixture fails as required) · snapshot + docs-manifest regen deliberate (v0.9.0, 11 docs) · **cold proof green** (fresh `--local` clone `034608e`, frozen install 0, build 0, run-gates EXIT 0).
- **Fresh-context verifier (7 mandated probes) verdict, verbatim: `OVERALL: PASS`** — "Only the authorized /docs edits landed (A1–A10 with A2=`captured` and A7=structural schema, plus the one derivations file; no smuggled edits). Every derivation carries a governing spec quote and matches code. All five hostile probes hold: locked-wins immutable, identity-forbids-offerId, no platform fallback, byte-compat token scope, `released` unrepresentable as a leg status. Deliverable green (run-gates exit 0; 142 tests pass)."
- **⚠ flag (verifier catch, non-blocking):** `docs/ECOSYSTEM-MASTER-REFERENCE.md:490` still reads `return_required` — a non-drift-checked governance doc A1 did not touch (A1's scope was the Séra spec). NOT edited here (it is beyond the founder's ten edits — editing it would compose). A master-reference parity sweep is the founder's call.
- Do NOT merge — founder review pending.

## 2026-07-12 · WO-5.2 done (founder review PASS) — attribution identity scope + conformity_mismatch on main (v0.9.0)
- **Founder verdict on `canon/attribution`: PASS — merged.** Merge commit `fa2ff24` ("merge(canon): v0.9.0 — attribution identity scope + conformity_mismatch — founder review passed"; branch head at merge `5db25ce`), pushed. Guard passed: branch local == origin (`5db25ce` both, ls-remote log-copied) · anchors `47dbd73` (PART A) + `e8615e7`/`8efac60` (the two correction commits) + `034608e` (PART B) in the log · delta beyond the final journal commit = only `_review/WO-5.2-review.zip` (canonical closing-commit template) · run-gates.sh at head `fa2ff24` exit 0 (ALL GATES GREEN; 142 tests; chain 15/15; mocks 8/8; drift-check 11 docs @ 0.9.0).
- **RULINGS, closed 2026-07-12 (founder):**
  - **① The arrival TTL (30 days) and the `PRENOM-NNNN` structural schema STAND as founder-ruled versioned policy.**
  - **② claim-state enumeration REMAINS DEFERRED — no spec names its lifecycle; boutik's local vocabulary stays local. Correct call; not invented.**
  - **③ The vitrine-as-buyer-surface law (A10) BINDS every future vitrine slice: SP-I03 applies there in full** (commission never shown · supplier identity never shown · reseller price · « Livré par Séra »/« Paiement protégé »).
- **The two reviewer drafting slips (A2 `released`, A7's self-contradicting charset) were caught by the derivation discipline BEFORE any canon byte moved — the loop working upward, journaled as such** (founder noted the A2 STOP-before-deriving over a §5 money-adjacent conflict and the attempt to construct an ambiguous normalizer input rather than assert absence, both with approval).
- **`conformity_mismatch` is CLOSED after being blocked since E2; the attribution identity scope makes the share hub buildable; the merge sha `fa2ff24` is the pin for the shop-plus vitrine slice.**
- **Attribution identity scope + conformity_mismatch = the founder's canon.**

## 2026-07-12 · Master-Reference parity sweep (WO-6.1 RIDER-FIRST, founder-authorized) — v1.2
- **Authorized as WO-6.1's docs-only first step.** Swept `docs/ECOSYSTEM-MASTER-REFERENCE.md` against the v0.9.0 specs for every token the WO-5.2 ten edits changed. **Report (each hit + governing spec quote):**
  - **`return_required` → `return` — ONE hit, l.490.** Governing quote: Sera-Build-Spec l.121 (post-A1) « dispatcher applies retry/reschedule/**return**/incident »; the code enum `DELIVERY_OUTCOME_FAMILIES = ['retry','reschedule','return','incident']`; AND the Master Reference's OWN five other uses already read `return` (l.386/454/507/608). A stale token, an internal inconsistency — corrected. Not a contradiction.
  - **EscrowTxn leg statuses (held|captured|refunded):** 0 hits — the doc does not enumerate them (l.602 « reservation released » is an unrelated reservation state). No divergence.
  - **`coarseLocation` optionality (A5):** 0 hits — « coarse progress » prose (l.371/585) is unrelated to the `EvidenceBundle.coarseLocation` field. No divergence.
  - **§6.4 reason enum / `conformity_mismatch` (A3/A4):** 0 hits — conformity is described as SE-I12 verification prose; the reason codes are not enumerated in this doc. No divergence.
- **Applied ONLY the one quote-matched correction; invented nothing; no genuine contradiction found (no STOP needed).** Doc → **v1.2**, sha256 **`2bafe3eaded062ef2cf43dfa6e0b32e37122b1494a1005bbc7f2e5d2c29fb83a`**; docs-manifest regenerated (11 docs, packageVersion 0.9.0). Gates green.

## 2026-07-12 · WO-5.4 · in-review (canon v0.9.1 — icon set 26 → 29; do NOT merge)
- **Base:** branched `canon/v0.9.1` off `origin/main` (log contains `fa2ff24`, the v0.9.0 merge). **Why:** WO-6.0 hit a real canon gap — the 26-icon Grand Teint set drew for screens, with no bottom-nav glyph for Accueil/Produits. The founder took it to the designer; three glyphs came back.
- **Three new icons committed BYTE-EXACT from the design bundle** (`cmp` clean against `_review/WO-5.4/bundle-icons/`): `accueil.svg` 262 B `34d8e573…a4d8` · `produits.svg` 297 B `17ff2ff7…2724` · `vitrine.svg` 269 B `0aeae055…3e24`. The **26 existing icons are byte-UNCHANGED** (`git diff origin/main…HEAD -- assets/icons/` shows only the 3 additions + the manifest edit).
- **`icons.manifest.json` regenerated:** 29 icons, **totalRawBytes 8184 MEASURED** (7356 + 262 + 297 + 269 = 8184); the diff is exactly `count 26→29`, `totalRawBytes 7356→8184`, and the three added blocks — no existing entry touched.
- **`landmark.iconNames` → 29** in `packages/ui-tokens/src/family.ts` + `docs/design/tokens.json` (appended accueil/produits/vitrine). ui-tokens suite **19/19** (length assertion 26→29; new "three nav glyphs present" test). **token-fidelity gate green** — 17 groups deep-equal tokens.json, no ⏳; no designer VALUE touched (standing law).
- **Icon gate extended (item 4) + green:** now asserts, permanently, `zero ns0: prefixes` (the WO-5.1 invisible-icon regression) and `every root <svg> carries xmlns`, alongside currentColor + `names == iconNames`. Gate output: *"29 icons, 8184 raw bytes, all currentColor, zero ns0: prefixes, every root <svg> carries xmlns, names == landmark.iconNames"*.
- **ns0 regression PROVEN NON-VACUOUS:** injected an `ns0:` prefix into a manifested icon (sha patched so ONLY the ns0 check could fire) → `check-icon-manifest.mjs` EXIT 1: *"accueil.svg: carries an 'ns0:' namespace prefix (WO-5.1 invisible-icon regression)"* → restored → green. The regression catches the real defect, in isolation.
- **Versions 0.9.1** across root + 5 packages + 3 intra-deps (certification→contracts/ui-tokens, contracts→kernel-types) + both docs manifests + 2 run-gates drift args; **lockfile regenerated** (intra-deps `specifier: 0.9.1`). **api-surface snapshot regenerated DELIBERATELY — delta = `packageVersion 0.9.0→0.9.1` ONLY** (exports + schemas byte-identical, proven by structural diff). The "delta = three names + packageVersion" holds precisely: names in family.ts/tokens.json, packageVersion in the snapshot, nothing else.
- **Evidence (my own runs):** typecheck 0 (5 packages) · **`run-gates.sh` EXIT 0** (143 suite tests: ui-tokens 19 + kernel-types 5 + contracts 84 + i18n 15 + certification 20; mocks 4/4 domains 8/8; every negative fixture fails as required) · **reference chain 15/15** (nine-id chain reconciles quote→payout) · **cold proof EXIT 0** (fresh `--local` clone @ `1b81ab2`, frozen install, build, ALL GATES GREEN). The cold proof CAUGHT a real gap first — the intra-dep bump needed the lockfile regenerated; fixed and re-proven.
- **FORBIDDEN respected:** no existing icon altered · no token VALUE touched (fidelity gate proves it) · no money/taxonomy/secrets/contracts-shape/event path touched · assembly pins untouched · no founder-blocked derivation shipped.
- **NOTE (pre-existing, out of scope):** `CONSUMING.md:12` still reads `@platform/kernel-types@0.8.0` — stale from the WO-5.2 (v0.9.0) bump, not from this WO; left untouched per the emphatic "SCOPE (nothing else)". Flagged for a future doc-tidy.
- **Verifier (fresh-context, MANDATORY contracts path) — VERDICT: PASS, BLOCKING: none** (full text verbatim in `_review/WO-5.4/verifier-verdict.md`). All seven mandate items executed by the verifier's OWN hands: **the ns0 regression independently PROVEN non-vacuous** (it injected `<ns0:path>` into `coche.svg`, patched the manifest sha/bytes/totalRawBytes so ONLY the ns0 check could fire → `check-icon-manifest.mjs` EXIT 1 naming the regression → reverted → green); three new icons `cmp`-identical to the bundle (262/297/269 B); 26 existing byte-unchanged; **snapshot delta = exactly ONE leaf, `packageVersion 0.9.0→0.9.1`** (deep leaf-by-leaf compare); manifest 29/8184 all sha match disk; iconNames parity 29 across family.ts/tokens.json/manifest/disk; no money/taxonomy/secrets/shape path touched, assembly pins untouched, `run-gates.sh` EXIT 0 with every negative fixture firing. Non-blocking note: HEAD advanced `1b81ab2`→`fdb2f3f` mid-review (the docs-only packet commit); the verifier re-ran items 2/4 against the new HEAD — all code-level findings hold.

## 2026-07-12 · WO-5.4 · done (merged `4440ce0`)
- **Merged to main — `merge(canon): v0.9.1 — the icon set closes the nav gap (29 glyphs) — founder review passed`**, merge sha `4440ce0f217372051d9876cf6aaefda8d006a3ba`. Founder review PASS; the ns0 non-vacuity proof (inject the defect AND patch the manifest so ONLY the ns0 check can fire) is now the STANDARD — a permanent regression test for the WO-5.1 invisible-icon class.
- **The nav-icon gap was found by a build session that REFUSED to substitute a lookalike; the designer drew accueil/produits/vitrine; nothing was approximated. The merge sha is the pin for WO-6.0's TabBar.**

## 2026-07-13 · WO-5.6 · in-review (canon v0.9.2 — TIER AMBER; do NOT merge; impl `43529c0`)
- **Base:** branched `canon/v0.9.2` off `origin/main` (log contains `4440ce0`, the v0.9.1 merge — GUARD satisfied). Four parts, all landed in commit `43529c0`.
- **PART A — the Contract amendment.** `docs/Ecosystem-Engineering-Execution-Contract.md` gains **§3.1 Tiered review — RED · AMBER · GREEN** (founder-ruled "tiered review: OK", 2026-07-12), placed as a decimal section beside §3 (review discipline) to avoid renumbering §4–§11. Verbatim tier definitions; the standing rule **"Every work order declares its TIER in its first line. The CTO assigns it; the session obeys it. A session may NEVER self-assign a tier."**; and the **UNCHANGED-AND-ABSOLUTE-IN-ALL-TIERS** list (packet-primacy · derive-never-invent · self-anchoring guards · isolation-evidence law · no state dropped · STOP-over-guess).
- **PART B — nine design-dimension tokens, each DERIVED AND QUOTED (`docs/derivations/DESIGN-DIMENSIONS.md`).** The derivation table pins each value to a **byte-exact** canon design-doc line (verified `grep -F`, all nine present):
  - `celebration.haloPx = 220` ← `motion.md:28` "halo (220px circle, theme tint)"; `celebration.ringPx = 132` ← `motion.md:29` "ring (132px, theme colour)".
  - `band.priceBand.noteWidth = 118` ← `components.md:53` "right-column honesty note (`caption` `primarySoft`, w 118)".
  - `dimension.controlHeightPx` {`primaryButton 56` ← `components.md:36` "h 56" · `searchField 50` ← `components.md:81` "box h 50" (the clean single value; the SecondaryButton "50–56" is a **range** and was deliberately NOT the source) · `listRow 44` ← `components.md:30` "fixed h 44 (list virtualization law)" · `offlineBanner 30` ← `components.md:71` "Ink band h 30"}.
  - `dimension.iconSizePx` {`listRow 17` ← `components.md:28` "icon 17" · `emptyState 28` ← `components.md:62` "Icon 28"}.
- **PART B — the one STOP (derive-never-invent, held).** WO-6.0's kit carries a third icon-size prop **12 px**. No canon line states a **12 px icon** — 12 exists only as the spacing step `4/8/12/16/24/34` (`GRAND-TEINT.md:70`) and as row padding "11–12" (`components.md:24`), neither an icon size. Per FORBIDDEN ("inventing a value no doc states → STOP instead") it is **flagged for the founder → designer**, NOT tokenised. Nothing 12-shaped entered `dimension.iconSizePx`.
- **PART B — the new gate + the fidelity amendment.** `scripts/check-design-dimensions.mjs` (wired into `run-gates.sh`, positive + negative): for each of the 9 tokens it asserts built value `===` the manifest value AND the byte-exact quote is present in its doc AND the quote contains the number (static non-vacuity), PLUS a **completeness** check — every key the built tokens add beyond `tokens.json` in {celebration, band, dimension} must be one of the nine (an undERIVED addition fails). **Non-vacuous BOTH WAYS proven** (`show-design-dimensions-negative.sh`): a bad token value (`haloPx 220→221`) is rejected (exit 1) AND a bad doc (`motion.md 220px→221px`) is rejected (exit 1). **Fidelity gate amended, recorded as STANDING LAW** in `check-token-fidelity.mjs`: `tokens.json` values stay immutable (every key present + deep-equal), the built package MAY be a superset for the SCOPED set `{celebration, band}` (every other group stays strict deep-equal, so no stray key hides elsewhere). `tokens.json` is **byte-UNALTERED**; fidelity gate green (17 groups).
- **PART C — D22 name fix.** The ops repo's real name is **`platform`** (github.com/beurni2/platform), corrected from `platform-ops` at both occurrences in `docs/ECOSYSTEM-MASTER-REFERENCE.md` (l.515 recommendation + l.712 open-decision row). This is a **name correction only** — D22's "where ops lives" stays **open** ("Undecided — ops functions are specified but homeless" preserved verbatim).
- **PART D — pack anatomy DERIVED, not STOP (`docs/derivations/PACK-ANATOMY.md`).** The B+9 rows **name** the BOM shape, so this derives (quoting spec): **B+I-16** (`Boutik-Plus-Build-Spec.md:60`) "A `PackProduct` is a bill-of-materials over shared `PackComponent`s. Pack availability = **min over components of floor(available / qty)**…" — the scarcest-component law verbatim; **B+I-17** (:61) honest BOM-min degradation; the §5-style pack shapes (:106–108). **NO VALUES**: no ceilings, no legal structure, no « Mon Enseigne »; **no contract code** (that stays behind the §12 B+9 gate). Doc-only; nothing invented.
- **Version 0.9.2** across root + 5 packages + intra-deps (certification→contracts/ui-tokens, contracts→kernel-types) + both docs manifests (11 docs, regenerated: Contract + MASTER-REFERENCE hashes updated) + 2 run-gates drift args; **lockfile regenerated**; **api-surface snapshot delta = `packageVersion 0.9.1→0.9.2` ONLY** (exports + schemas byte-identical, proven leaf-by-leaf — ui-tokens is not in the contracts snapshot, so the added tokens contribute zero, exactly as the DoD ceiling permits).
- **Evidence (my own runs, this session):** typecheck 0 · **`run-gates.sh` EXIT 0** (all positive suites; **every** negative fixture fires as required, incl. both new design-dimension negatives) · **reference world green — mocks 8/8** (payment-provider + eligibility CERTIFIED) **· 15-step chain complete** (corr_e1demo, 28 events) · **cold proof EXIT 0** (fresh `--local` clone @ `43529c0`, **frozen** install, build, ALL GATES GREEN, 0 GATE FAILED). **Assembly pins untouched · money/taxonomy/secrets/shape/event paths untouched · `tokens.json` unaltered.**
- **Fresh-context verifier (MANDATORY — canon docs + tokens + new gate + PackLab shape) — VERDICT: PASS, BLOCKING: none** (verbatim in `_review/WO-5.6/verifier-verdict.md`). All eight mandate items executed by the verifier's OWN hands: the 9 tokens byte-verified against their doc lines (rebuilt dist fresh from committed src); **the 12px STOP independently confirmed** (`grep` found no "icon 12" in `docs/design/`; only the spacing step + row-pad 11–12); **the new gate proven non-vacuous THREE ways** (bad value fires · bad doc fires · a planted stray undERIVED token `iconSizePx.badge 12` fires the completeness guard); the fidelity amendment confirmed SCOPED — `git diff` on `tokens.json` EMPTY, and immutability of existing keys inside the superset groups still enforced (planted `haloMs 700→701` and `themeStripPx 4→5` both fire); **snapshot delta = packageVersion ONLY** (zero added/removed/changed leaves in exports+schemas); Part D quotes byte-accurate with honest ellipses, no ceiling/legal/« Mon Enseigne »/contract-code; §3.1 + D22 (stays OPEN) confirmed; `run-gates.sh` EXIT 0, mocks 8/8, chain 15/15, all 11 negatives fire at exit exactly 1.
- **Verifier non-blocking notes (recorded, no action):** (1) Part D's shape rows are markdown-normalized inside the citation (fields/enum byte-identical — normalization, not invention); (2) B+I-17/B+9 quotes elide middle clauses with honest `…`; (3) **latent, pre-existing (NOT introduced here) — a future hardening flag:** a brand-new top-level ui-tokens export that is in *neither* the fidelity MAP *nor* design-dimensions `OWNED_GROUPS` would escape both gates. This WO's new `dimension` group is explicitly in `OWNED_GROUPS`, so it is fully covered — but a future export could slip. Flagged for a later gate-hardening slice (a "every ui-tokens export is owned by exactly one gate" meta-check); out of scope here.
- **Pending / next:** the founder's AMBER verdict. **Do NOT merge (AMBER — merge requires the founder's paste).**

## 2026-07-13 · WO-5.6 · done (founder review PASS — merged; canon v0.9.2)
- **Merged — sha `494748f`.** CTO reviewed the diff itself, not the relay: no money/taxonomy/contracts-src path touched; §3.1 verbatim incl. the self-assign-forbidden law; both `platform-ops` → `platform` with D22's "Undecided — ops functions are specified but homeless" preserved (**D22 STAYS OPEN** — a name fix is not a decision); `SUPERSET_OK = {celebration, band}` scoped; `OWNED_GROUPS` covers `dimension`; warm + cold `run-gates.sh` EXIT 0, every negative fixture firing; certification 4/4 domains, 8/8 behaviours; `tokens.json` byte-unaltered.
- **GUARD 4 tripped on `c5ac7ca` (a 3-line `.gitignore`) and the session STOPPED — correctly.** It reported the full delta verbatim, did not fix-and-retry, and did not self-assign a ruling. **CTO ruling: the CLOSING-COMMIT TEMPLATE was INCOMPLETE, not violated.** It was written for a commit that only records a review; it forgot that committing a packet requires the machinery governing what the packet sweeps in. **A rebase was REFUSED**: it would move `43529c0`, which the verifier verdict, the JOURNAL, and the CTO review all cite — the packet would stop describing the branch, the exact defect another lane is held on. **A naked waive was also refused** — a guard waived on first fire is not a guard.
- **TEMPLATE AMENDED (standing law).** The closing delta may carry `_review/` · `JOURNAL.md` · `.gitignore` — and `.gitignore` ONLY when every added line is scoped to `_review/` or `_evidence/`. A rule that hides source, docs, or packages still trips the guard (GUARD 4b).
- **NEW GUARD 4c — THE PACKET MUST BE POSITIVELY PRESENT.** `_review/*/` makes loose review DIRECTORIES invisible to git. A future lane committing a directory-shaped packet would commit NOTHING, and the old guard would have reported GREEN — it only asked "is anything forbidden here?", never "is the packet actually here?" **A guard that passes on an empty packet is a vacuous guard** (the §8 class). Caught only because the session stopped and asked.
- **PACKET DEFECT (recorded, not waived).** `logs/branch-log.txt` was ABSENT — the file PACKET-PRIMACY names explicitly. The CTO never saw a HEAD sha, a commit chain, or the closing-commit delta from the packet; those checks were pushed into the merge paste's guards, which ran and passed. Every future CANON packet carries `logs/branch-log.txt` BY THAT NAME.
- **NAMED DEBT ① — the gate-coverage meta-check. Executioner: the NEXT CANON slice.** `check-token-fidelity.mjs` iterates `Object.entries(MAP)`; `check-design-dimensions.mjs` iterates `OWNED_GROUPS`. Both are hand-maintained. Nothing asserts their union is the whole built ui-tokens surface. The verifier called this "latent, pre-existing" — right about the code, wrong about the risk. Before v0.9.2 the law was `built == tokens.json`, so MAP was the complete surface BY CONSTRUCTION and the hole was unreachable. v0.9.2 legitimises supersets and ships `dimension`, the first export ever to live outside `tokens.json`. The hole is now a paved road. Required: `leafPaths(built) ⊆ (MAP ∪ DERIVED)` — every export owned by exactly one gate, or the build fails. This is the vacuous-icon-test class (§8): a gate that passes green while proving nothing about what it never looks at.
- **NAMED DEBT ② — zero-hardcode is HALF-PAID.** WO-5.6 CREATES the nine tokens and consumes none. `boutik-plus` (WO-6.0, merged) still holds 220/132/118/56/50/44/30/17/28 as literals — the gap that caused this WO. Executioner: an APPS slice. Until then, literals and tokens can drift silently.
- **NAMED DEBT ③ — CANON WO-5.7 (with the gate-coverage meta-check).** `@platform/certification` still declares `@platform/ui-tokens: 0.6.0` and its `dist/chain/report-html.js` imports `neutralColors`, a symbol Grand Teint DELETED. This is a latent break inside canon; it bit shop-plus this week and forced a scoped v0.6.0 override there. WO-5.7 kills it at the source.
- **RULING — WO-7.0 (OZ) RE-PINS to canon v0.9.2.** WO-6.5 (APPS), WO-6.4 (BOSSY), WO-OPS-0 (PLATFORM) may ride at `4440ce0`: api-surface delta is `packageVersion` ONLY, ui-tokens additions purely additive, nothing they consume moved. OZ is the exception — WO-7.0 is a Grand Teint reskin; pinned at v0.9.1 it has no `dimension` tokens and WILL hardcode the same nine literals boutik-plus did. **We are not creating the same debt twice in one week.**
- **BLOCKED ON FOUNDER — the 12 px icon STOP.** The session refused to invent a value no doc states and was right to. To the designer: name a 12 px icon size, or re-point the kit's third icon prop at 17 / 28. Nothing 12-shaped enters `dimension.iconSizePx` until he rules.

## 2026-07-13 · WO-5.7 · in-review (canon v0.9.3 — TIER AMBER; do NOT merge; impl `c5251a9`)
- **Base:** branched `canon/v0.9.3` off `origin/main` (contains `494748f`, the v0.9.2 merge — GUARD satisfied). Parts A/C/D land as code; B is verified-already-clean; E is a STOP-flag. Warm + cold `run-gates.sh` **EXIT 0**.
- **PART A — the gate-coverage meta-check (named debt ①).** New `scripts/token-surface.data.mjs` is the **single source** of gate ownership (`FIDELITY_MAP` · `DERIVED` · `OWNED_GROUPS` · `STRUCTURAL_EXPORTS`); `check-token-fidelity.mjs` and `check-design-dimensions.mjs` now IMPORT it (no more hand-maintained twins). New meta-gate **`check-token-coverage.mjs`**: every built ui-tokens export is owned by **exactly one** gate — a `FIDELITY_MAP` export, the `dimension` group, or a declared `STRUCTURAL_EXPORT` (the 3 themes + `themes` map = compositions of already-checked colours; `grandTeintMeta` = `$meta` provenance) — AND every value leaf is a `tokens.json` leaf or a `DERIVED` path. Result: `23 exports each owned by exactly one gate (17 fidelity · dimension · 5 structural)`. **Non-vacuous BOTH branches** (`show-token-coverage-negative.sh`): a stray top-level export **— the designer's exact `icon` group —** fires (exit 1), and a stray leaf (`spacing.strayLeaf`) fires (exit 1). The two existing negative fixtures were updated to `cp` the shared data module into their tmp re-root; both still fire.
- **PART C — `dimension.iconSizePx.badge = 12`, the 12 px STOP RESOLVED.** NOT invented: the trust-strip badge glyph (« PAIEMENT PROTÉGÉ ») **fills the labelXS line box**, so its px = `type.scale.labelXS.size × lh = 10 × 1.2 = 12`. Anchored as a **COMPUTED** token to canon's OWN type scale — if labelXS moves, the badge must move or `check-design-dimensions.mjs` fails (proven: the gate multiplies the two built leaves and asserts `=== 12`). **VALUE 12 is the designer's and immutable; SHAPE is canon's** — it enters the existing `dimension.iconSizePx.badge`; the designer's proposed **top-level `icon` group was REFUSED** (two shapes for one concept is a canon-shape conflict), and the coverage gate now guards against exactly that stray. The three laws recorded in `DESIGN-DIMENSIONS.md` (icon+word never alone · not a hit target · do not upscale).
- **PART D — `dimension.iconSizePx.tab = 20`, DERIVED (not STOP).** `components.md:74` (TabBar) byte-states “icon 20 + word \`labelXS\` (icon+word law)”. Quote-anchored to the DOC — **not** the designer's README prose (a README is not a design doc). This unblocks OZ wiring the v0.9.1 nav glyphs (accueil/produits/vitrine) into the TabBar. The 11-token derivation table + enforcement are in `DESIGN-DIMENSIONS.md`.
- **PART B — the certification skew is ALREADY CLEAN in canon (verified; no change needed).** Named debt ③ said `@platform/certification` pins ui-tokens `0.6.0` and imports the deleted `neutralColors`. **At canon HEAD this is false:** `report-html.ts:1` imports `{ boutikPlusTheme, sharedColour }` (live symbols); the pin is `0.9.3`; `git log -S neutralColors` shows `neutralColors` was replaced by `sharedColour` back at **v0.8.0 (`036a04d`)**; a repo-wide sweep finds **zero `0.6.0`, zero `neutralColors`, all internal pins `0.9.3`**. No canon change; **typecheck is the standing regression guard** (an import of a non-exported symbol fails the build). The live skew survives only in **shop-plus's own old pin** (its scoped v0.6.0 override) — deletable when shop-plus re-pins to v0.9.3. *(Grounding a claim in the code, not the debt note — the debt was stale.)*
- **PART E — STOP-FLAG: the `command_id` mint rule (FOUNDER decision).** Verified the gap myself (not on APPS's word): `packages/contracts/src/events.ts:10` constrains `command_id: z.string().min(1)` — nothing more. Canon **requires** idempotency (Boutik §5.7 “Events … carry command_id …”; Contract §3/§4; SP-I13 “Payment retried — No duplicate charge. Idempotent.”) and names the envelope field, but **no spec sentence states the client-mint FORMAT or the ENTROPY SOURCE**. The only mint code is certification's **sandbox reference fixtures** (`cmd_${domain}_${seed}_${index}`), explicitly test-only — not a client rule. Two properties are **offline-forced** (Ten Laws #7), not judgement: (1) minted **client-side**; (2) minted **once, persisted, never recomputed** (a recomputed id collides with its persisted self across a reboot = a lost action shown as pending forever). But FORMAT + ENTROPY are **one ruling and it's the founder's** — it is the event envelope (§7 canon shape) with double-charge stakes. **No canon code shipped for E.** CTO/CDO recommendation for the founder: **UUIDv4 (or ULID) minted from a CSPRNG** (`expo-crypto` / OS entropy pool) — `Math.random()` carries only its SEED's entropy, and cold-boot Android-Go seeding is unproven; a CSPRNG is cold-boot-safe by construction, one line. **Founder ruling required.**
- **tokens.json DRIFT — journaled; canon byte-UNALTERED.** Canon's `docs/design/tokens.json` is unchanged at v0.9.3 (`git diff` empty). The design tool reportedly merged its `icon` block into **three OTHER `tokens.json` copies** (incl. the founder's bundle) — those copies are **external, not in this repo**; the bundle and canon have **drifted**. I did **not** import the tool's block. `badge`/`tab` are derived here and live in **family.ts `dimension`** (design-dimensions-owned), NOT in `tokens.json` — because Part A's own “**exactly one gate**” rule forbids the dual ownership that a `tokens.json` `dimension` block would create. **Which copy is the truth is the founder's ruling, not the tool's** — flagged.
- **Version 0.9.3** (6 package.json + intra-deps + both docs manifests + 2 run-gates drift args); **lockfile regenerated**; **api-surface snapshot delta = `packageVersion 0.9.2→0.9.3` ONLY** (exports + schemas byte-identical). **`tokens.json` BYTE-UNALTERED.**
- **Evidence (my own runs):** typecheck 0 · **`run-gates.sh` EXIT 0** (all positives; every negative fires, incl. the new coverage negative on BOTH branches) · **mocks 8/8** (4/4 domains) **· chain 15/15** (corr_e1demo, 28 events) · **cold proof EXIT 0** (fresh `--local` clone @ `c5251a9`, **frozen** install, build, ALL GATES GREEN, 0 GATE FAILED). Assembly pins untouched · money/taxonomy/secrets/shape/event paths untouched.
- **Fresh-context verifier (MANDATORY — canon docs + tokens + gates + event-envelope STOP) — VERDICT: PASS, BLOCKING: none** (verbatim in `_review/WO-5.7/verifier-verdict.md`). Every part re-checked by the verifier's OWN hands: **Part A non-vacuity proven by its own plants** — a stray top-level export (`strayGlyph`) fired `check-token-coverage.mjs` (exit 1) **while fidelity + design-dimensions stayed green**, i.e. it reproduced the exact silent-not-ship hole named debt ① targets, now caught; stray leaves in a fidelity group and in `dimension` fired the right gates; both value gates confirmed importing the shared lists (no divergent hand-copies); both prior negatives still fire. **Part C** confirmed a GENUINE computed anchor — tampering `labelXS.lh 1.2→1.3` in a tmp dist fired `canon computes … = 13 !== 12` (not a hardcoded 12); no top-level `icon` export exists. **Part D** tab quote byte-confirmed at `components.md:74`. **Part B** independently swept clean (zero 0.6.0 / neutralColors; `neutralColors` gone since `036a04d`) — verifier's own judgment: the debt-③ premise was **stale**, nothing to fix in canon. **Part E** independently confirmed canon states no mint format/entropy and no canon code shipped — STOP is correct. tokens.json empty diff; snapshot delta packageVersion-only; suite EXIT 0, mocks 8/8, chain 15/15; `logs/branch-log.txt` present by name.
- **Verifier non-blocking notes (recorded):** (1) themes verified as pure spreads of already-fidelity-checked colours (no raw value escapes); (2) it ran warm + a from-source rebuild, relying on my captured `cold-run-gates.log` for the cold run (which I did run — EXIT 0 @ `c5251a9`). **NAMED DEBT ④ — future hardening flag:** the coverage gate enforces "exactly one gate" as *no-unowned-export + no-stale-owner + leaf-completeness*, but does NOT separately assert the three ownership lists are **pairwise-disjoint** — an export deliberately listed in BOTH `FIDELITY_MAP` and `STRUCTURAL_EXPORTS` would skip leaf-checking silently. This is a *deliberate-mis-listing* case, not the *forgot-to-register* case named debt ① closes; low severity. A one-line disjointness assertion closes it — flagged for a later gate-hardening slice (with the meta-check family), out of scope here.
- **Pending / next:** the founder's AMBER verdict, plus **two founder rulings the session surfaced — (E) the `command_id` mint FORMAT + ENTROPY, and (drift) which `tokens.json` copy is truth.** **Do NOT merge (AMBER — merge requires the founder's paste).**

## 2026-07-13 · WO-5.7 · done (founder review PASS — merged; canon v0.9.3)
- **Merged — sha `9e1a527`.** Coverage meta-gate live: 23 exports, each owned by exactly one gate; negative fixture = the designer's own proposed top-level `icon` group (rejected exit 1) + a stray leaf. `badge = 12` enters as a **COMPUTED** token (`labelXS.size × lh = 10 × 1.2`) — if labelXS moves, the build fails. `tab = 20` **derived** from `components.md:74`, not the designer's README. `tokens.json` byte-unaltered; snapshot delta = packageVersion only.
- **CTO'S DEBT ③ WAS STALE — corrected on record.** Certification never imported `neutralColors` at canon HEAD; it was replaced by `sharedColour` at v0.8.0 (`036a04d`). The live skew exists only in shop-plus's ancient v0.6.0 pin and dies on its re-pin. The session verified the claim in the code instead of building on the debt note. THE LOOP REVIEWS THE REVIEWER.
- **TWO FOUNDER RULINGS OPEN:** (E) the command_id mint FORMAT + ENTROPY (canon says `z.string().min(1)` and nothing more — gap verified by this session's own hands); (drift) which `tokens.json` copy is truth — canon's is byte-unaltered; the design bundle's copies have drifted.
- **NAMED DEBT ④ (from the verifier, recorded).** The coverage gate does not yet assert the three ownership lists are pairwise-disjoint (a deliberate double-listing could skip leaf-checking); low severity, a one-line fix, folded into the meta-check family for a later gate-hardening slice.

## 2026-07-13 · WO-5.8 · in-review (canon v0.9.4 — TIER AMBER; do NOT merge; impl `b55b979`)
- **Base:** branched `canon/v0.9.4` off `origin/main` (contains `9e1a527`, the v0.9.3 merge). CTO authorized opening this off merged main; **Part G (the `command_id` mint rule) is NOT started — it becomes WO-5.9 the moment the founder rules.** This slice is **Part F only.**
- **PART F — the LOCKFILE URL-FORM GATE (standing law; every repo inherits).** New `scripts/check-lockfile-url-form.mjs`: a committed `pnpm-lock.yaml` may carry **zero** SSH-form git URLs (`git@host:` · `ssh://` · `git+ssh://`) or the build fails. Wired into `run-gates.sh` (positive + negative). **Non-vacuous** (`show-lockfile-url-form-negative.sh`): a planted `git@github.com:beurni2/private.git` resolution is rejected (exit 1). **platform-contracts' own lockfile is clean** — `grep -c 'git@github.com:' pnpm-lock.yaml` = 0, all resolutions are registry-integrity; the gate passes here (warm + cold), and the lockfile stayed SSH-clean through the v0.9.4 regeneration.
- **THE LAW (journaled verbatim, standing):** *"A regenerated lockfile carries its AUTHOR'S ENVIRONMENT. Installability is proven only by a cache-isolated cold clone IN A CLEAN HOME. A cold proof inside a patched container is a warm build wearing the word 'cold'."* An SSH-form URL resolves only against the author's ssh keys / `~/.gitconfig`, so the lockfile installs on their box and **nowhere else**.
- **WHY NOW:** **shop-plus carries 39 SSH-form URLs and has not been cold-installable since WO-5.3 MERGED** — invisible for weeks because agents patched `~/.gitconfig` around it. This gate makes that class visible at the source; every repo that inherits it (via its own `run-gates`) fails loudly instead of silently depending on a patched home. *(platform-contracts has no git deps, so its own cold-installability was never at risk from this class — it is the clean exemplar; the gate's teeth are for the app repos, shop-plus first.)*
- **Version 0.9.4** (6 package.json + intra-deps + both docs manifests + 2 run-gates drift args); **lockfile regenerated** (still 0 SSH URLs); **api-surface snapshot delta = `packageVersion 0.9.3→0.9.4` ONLY**; `tokens.json` byte-UNALTERED (untouched this slice). *(Version bump is CTO-cadence consistency for a CI-gate-only slice — no package API/token/tracked-doc changed; flagged for the founder to confirm the cadence.)*
- **Evidence (my own runs):** typecheck 0 · **`run-gates.sh` EXIT 0** warm (lockfile gate green; its negative fires exit 1) · **cold proof EXIT 0** (fresh `--local` clone @ `b55b979`, **frozen** install, build, ALL GATES GREEN, 0 GATE FAILED; the clone's lockfile SSH-clean) · mocks 8/8 · chain 15/15.
- **Fresh-context verifier (AMBER — CI-gate slice) — VERDICT: PASS, BLOCKING: none** (verbatim in `_review/WO-5.8/verifier-verdict.md`). By its OWN plants: `git@github.com:`, `ssh://`, `git+ssh`, AND `git@gitlab.com:` (host-agnostic) all fire the gate (exit 1); a clean control passes (not vacuously failing); the shipped negative fixture exits 1. Confirmed Part G NOT started (no `packages/contracts/src` change; the only `command_id` strings in the diff are journal prose / a pre-existing snapshot export name), snapshot delta packageVersion-only (exports 109/109 + schemas 71/71 identical), tokens.json 0-line diff, version uniform 0.9.4, `run-gates.sh` EXIT 0, mocks 8/8, chain 15/15.
- **CTO PRE-MERGE FIX APPLIED (commit `3885940`, source, precedes the packet).** The verifier's "non-blocking" finding is the vacuous-proof class, so the CTO ruled it does NOT wait for a hardening slice. `show-lockfile-url-form-negative.sh` re-roots the gate via `sed` on line 17's literal; a silent `sed` miss would leave the copy identical, resolve `root` to the mktemp parent, and exit 1 via "pnpm-lock.yaml not found" — a **false green** indistinguishable from a real catch. Two assertions close it: **(1)** `cmp -s` — the `sed` MUST have changed the gate's bytes (a miss is itself a fixture failure); **(2)** the failure OUTPUT must contain "SSH-form" — the plant was CAUGHT, not merely some exit 1. Re-run: still exit 1, now with the output assertion; a **simulated sed-miss now reports MISBEHAVED** instead of a false green (proven this session). Warm suite re-run EXIT 0.
- **NAMED DEBT ⑤ (rescoped — the lockfile fixture is now hardened here).** The same `sed`-reroot false-green pattern still lives in the OTHER three fixtures (`-token-fidelity-`, `-design-dimensions-`, `-token-coverage-`). They are correct today (their `sed` matches), but should get the same two assertions. Folded into the gate-hardening slice with named debt ① and ④ — apply the pattern uniformly across the family.
- **NOTE (cosmetic, pre-existing, out of scope):** the `run-gates.sh` icon-manifest gate label still reads "26 icons" though the set is 29 since WO-5.4 (the gate's OWN output correctly says 29). A label-accuracy fix for a future tidy; no behavior effect.
- **Pending / next:** the founder's AMBER verdict. **Do NOT merge (AMBER — merge requires the founder's paste).** Two founder rulings still open from WO-5.7: **(E→WO-5.9) the `command_id` mint rule**, and **(drift) which `tokens.json` copy is truth.**

## 2026-07-13 · WO-5.8 · done (founder review PASS — merged; canon v0.9.4)
- **Merged — sha `04af4b5`.** The lockfile URL-form gate is standing law: a committed `pnpm-lock.yaml` carries zero SSH-form git URLs (`git@host:` · `ssh://` · `git+ssh://`) or the build fails; verifier proved it host-agnostic (github + gitlab) and non-vacuous. **CTO pre-merge fix landed as source (`3885940`) preceding the packet — do NOT rebase `b55b979`:** the negative fixture now asserts the `sed` re-root changed the gate's bytes (`cmp -s`) AND the failure output names "SSH-form", closing the vacuous-proof (a silent sed-miss now reports MISBEHAVED, not a false green). Closing delta from the fix commit = `_review/` + `JOURNAL.md` only; `b55b979` preserved.
- **PRECEDENT — force-push of an IN-REVIEW branch (narrow; the four conditions are load-bearing).** Permitted ONLY when: (1) every evidence-cited commit is preserved byte-identical (`b55b979`); (2) only staging/docs commits are replaced (the in-review journal + packet); (3) the packet is regenerated from the new head; (4) all guards re-run on the final head. Outside those four it is the packet-divergence defect. **This slice met all four.**
- **RECORD — verifier coverage boundary.** The packet's fresh-context verifier verdict covers **`b55b979`**. The fix commit **`3885940`** was proven **in-session under CTO order** (re-run of the negative + the simulated-sed-miss MISBEHAVED meta-proof), **not** by a fresh verifier — proportionate for a fixture script, stated here so nobody later assumes coverage it does not have.
- **CONSUMER NOTE — AMENDED (CTO correction; my earlier line was wrong).** My prior entry said OZ and PLATFORM "stay on v0.9.3" — that rule is for consumers ALREADY pinned there. **OZ and PLATFORM have not yet re-pinned, and a FIRST pin takes the CURRENT release: they pin `04af4b5` (v0.9.4)** — its api surface is identical to v0.9.3 (snapshot delta = packageVersion only), zero skew. The standing rule: **consumers do not CHASE canon-internal bumps AFTER pinning; they do not pin BEHIND head on a first pin.** The next api-visible pin is WO-5.9 (the `command_id` ruling).
- **LOCAL-GATE ↔ CANON-LAW RELATIONSHIP (journal per CTO ruling — so nobody later deletes the wrong gate).** `sera` and `boutik` each **self-merged a GREEN lockfile-URL gate today** (convention-matched locally — correct under the GREEN tier). Canon's Part F (here, v0.9.4) is the **STANDING-LAW** version of that same gate. When consumers next re-pin, **their local gates REMAIN (defense in depth)** — but **the LAW lives in canon.** Do NOT delete a repo's local gate believing canon's covers their repo (canon gates canon's own lockfile, each app gates its own), and do NOT delete canon's believing the app copies suffice (canon is the law every repo inherits). Both stand.
- **QUEUED — WO-5.9 (AMBER), opens ONLY on the founder's `command_id` ruling arriving as a founder-labelled paste** (client-side · minted once · persisted · never recomputed · UUIDv4 · OS CSPRNG · `Math.random` forbidden on any mint path): schema/doc + a reference mint helper + a gate proving `Math.random` absent from mint paths (non-vacuous). APPS adopts at its isolated seam `src/offline/commandId.ts`. **Not started.**

## 2026-07-13 · WO-5.10 · done (founder review PASS — merged; canon v0.9.6 — PLATFORM's pin)
- **Merged — sha `ba6f16d` (PLATFORM's pin; PLATFORM was held at this gate).** `ModerationReasonCode` (the six ratified codes) + `ModerationDecisionSchema` — a silent/generic rejection unrepresentable, a reasonless rejection unrepresentable, `decided_by` ops:moderation:* only (no self-moderation). Snapshot delta = the 3 new exports + version; `tokens.json` byte-unaltered.
- **Verifier attacked the actor regex with prefix + newline tricks** (`xops:moderation:d`, trailing `\n`) — both refused; the `:`-in-suffix leniency accepted as harmless hierarchical sub-ids.
- **WO-7.1 EVIDENCE-LAW PRE-MERGE FIX (packet-refresh, no source change).** The cold-proof artifact must SHOW its own isolation: the first log captured only run-gates output (`export HOME=` 0, `insteadof` 0). Re-ran from the top on `f68cee4` — isolated HOME · explicit proxy `insteadOf` auth established in that HOME · fresh pnpm store · `frozen-install rc: 0` · ALL GATES GREEN — new log satisfies the law (`export HOME=` 1, `insteadOf` 2). One packet-refresh commit `b48f68a`; source byte-unchanged.
- **CONSUMER NOTES (journal per CTO ruling).**
  - **PLATFORM pins v0.9.6 (`ba6f16d`) at OPS-1a — its one re-pin.** Desk 3 issues `ModerationDecision`s naming these codes; boutik consumes them.
  - **boutik:** its local enum **byte-matches** canon's ratified set. The local→canon swap is a **small named slice AFTER its in-flight B-batch closes** — NOT now (no second writer in a RED lineage).
  - **OZ (shop-plus) stays on `04af4b5`** — moderation shapes are irrelevant to shop-plus; consumers don't chase.
- **Lane idle.** Next canon work opens only when a consumer surfaces a gap or the founder issues a ruling with canon work.

## 2026-07-13 · WO-5.10 · in-review (canon v0.9.6 — TIER AMBER; do NOT merge; impl `6537780`)
- **Base:** branched `canon/v0.9.6` off `origin/main` (contains `2153661`, the v0.9.5 merge). Encodes the Boutik **A1 RATIFIED v1 set** (founder-ratified 2026-07-13) so **PLATFORM Desk 3** can issue moderation decisions boutik consumes. **Next CONSUMER-VISIBLE pin.**
- **`ModerationReasonCode` (enums.ts): the six ratified codes verbatim** — `facts_incomplete` · `no_public_safe_proof` · `price_or_contact_in_image` · `not_neutral_packaging` · `prohibited_or_unlaunched_category` · `authenticity_concern`. Cross-checked against canon (derive-never-invent): B+I-01 (approved facts + public-safe proof), the price-free/contact-free hero, the neutral-packaging rule (B+3), category rules, "no unresolved moderation/authenticity concern". The codes are A1's ratified set entering canon now.
- **`ModerationDecisionSchema` (shapes/moderation.ts): three properties enforced AT THE SCHEMA, not by discipline.** (1) exactly two outcomes `approved` | `changes_requested` — **a silent/generic rejection is unrepresentable** (mirrors the no-generic-`failed` order-status law); (2) `changes_requested` requires `reasons: min 1` of the enum — **a reasonless rejection is unrepresentable**; (3) `decided_by` MUST match `ops:moderation:*` — **no self-moderation** ("verification/moderation operator Ops only", Boutik-Plus-Build-Spec §Roles); a supplier actor never validates.
- **The gate (`show-moderation-decision-negative.mjs`, wired in `run-gates.sh`) is NON-VACUOUS:** two VALID decisions PARSE (approved by ops; changes_requested with a reason by ops), while the two the WO names — a **reasonless changes_requested** and a **supplier-actor decision** — plus a generic `rejected` are all REFUSED at parse. Unit tests `moderation.test.ts` **5/5** (incl. off-enum reason rejected, `ops:moderation:` empty-suffix rejected, `ops:finance:x` rejected).
- **Version 0.9.6** (6 package.json + intra-deps + both docs manifests + 2 drift args); **lockfile regenerated** (SSH-clean); **api-surface snapshot delta = `packageVersion 0.9.5→0.9.6` + the THREE new exports (`MODERATION_REASON_CODES`, `ModerationReasonCodeSchema`, `ModerationDecisionSchema`) + two registered schemas ONLY** (no existing export/schema removed or changed — structural diff). **`tokens.json` byte-UNALTERED.**
- **Evidence (my own runs):** typecheck 0 · **`run-gates.sh` EXIT 0** warm (moderation negative fires exit 1) · **mocks 8/8** (4/4 domains) **· chain 15/15** · **COLD PROOF BOTH LINES** — warm EXIT 0 AND **clean-HOME cold EXIT 0** (fresh `--local` clone @ `6537780`, isolated `HOME` with no `~/.gitconfig`, **frozen** install, build, ALL GATES GREEN).
- **Build note:** first `tsc` failed — my enum JSDoc contained `**price-free**/contact-free`, whose `**/` prematurely closed the block comment. Reworded (markdown emphasis has no place in a code comment); rebuilt clean. *(Recorded per ground-every-claim.)*
- **Fresh-context verifier (MANDATORY — contracts shape path) — VERDICT: PASS, BLOCKING: none** (verbatim in `_review/WO-5.10/verifier-verdict.md`). Built the package fresh and ran **33/33 adversarial parse checks against the built dist**: the six codes exact; the generic `rejected`/`denied`/missing-decision all FAIL (silent rejection unrepresentable); reasonless/missing-reasons/off-enum-reason all FAIL; the actor regex is **anchored** — `supplier:acme`, `ops:finance:x`, `moderation:x`, empty-suffix `ops:moderation:`, the prefix-trick `xops:moderation:d`, and a trailing-newline all FAIL, while `ops:moderation:desk3` passes; `.strict()` blocks a stray `reasons` on `approved` (no smuggling); the valids parse (non-vacuous). Independently mapped every code to canon (B+I-01/B+I-02 :45–46, B+3 :176, B+4 :177, authenticity :32, Ops-only actor :28) — derive-never-invent holds. Snapshot delta = the 3 new exports + 2 schemas + version; tokens.json byte-unaltered; `run-gates.sh` EXIT 0, mocks 8/8, chain 15/15, 92 contracts tests.
- **Verifier non-blocking note (recorded):** the actor regex char class permits `:` in the suffix, so `ops:moderation:desk:3` parses — harmless (still an ops:moderation actor; supports hierarchical sub-ids). No change.
- **CTO pre-merge evidence-law fix (WO-7.1; packet-refresh only, NO source change).** The first `cold-clean-home-run-gates.log` captured only the `run-gates` output — the RUN was isolated but the ARTIFACT did not SHOW it (`grep -c 'export HOME='` = 0, `grep -ci insteadof` = 0). Re-ran the cold proof on `f68cee4` **from the top**, capturing the isolation into one log: `export HOME=<clean>` · the explicit proxy `insteadOf` auth established in the isolated HOME (not inherited) · a fresh pnpm store · `frozen-install rc: 0` · build · `ALL GATES GREEN` (0 GATE FAILED). New log now satisfies the law (`export HOME=` ×1, `insteadOf` ×2). One packet-refresh commit; source byte-unchanged.
- **Pending / next:** the CTO's AMBER verdict. **Do NOT merge (AMBER — merge requires the CTO's paste).** On merge this is the next CONSUMER-VISIBLE pin. **Consumer note:** boutik re-pins and swaps its local enum for canon's on its NEXT slice; PLATFORM consumes at OPS-1a.

## 2026-07-13 · WO-5.9 · in-review (canon v0.9.5 — TIER AMBER; do NOT merge; impl `e9f529d`)
- **Base:** branched `canon/v0.9.5` off `origin/main` (contains `04af4b5`, the v0.9.4 merge). Encodes founder ruling ① (`command_id` mint rule). **This is the next CONSUMER-VISIBLE pin.**
- **The schema + reference helper (`packages/contracts/src/command-id.ts`, re-exported from the RN-safe root).** `CommandIdSchema` = a **branded UUIDv4** (`z.string().regex(UUID_V4).brand<'CommandId'>()`) — a raw string cannot stand in for a validated minted id. `mintCommandId(): CommandId` is the reference mint: **RN-safe** — it reads the ambient Web Crypto global `globalThis.crypto.randomUUID`, **never an `import` of the `crypto` builtin** (which the RN-safe scanner forbids in the root graph; scanner re-run GREEN); it **THROWS** with no CSPRNG and **never** falls back to `Math.random`; it validates its own output so it cannot emit a non-UUIDv4. Node/web have `globalThis.crypto` natively (verified Node v22); RN apps provide it via expo-crypto.
- **The gate (`scripts/check-mint-path-entropy.mjs`, every repo inherits).** Scans every mint-path file (`command-id*` / `commandId*`) and fails on a `Math.random(` **CALL** — comments/strings that NAME `Math.random` (stating the rule) are stripped/ignored, so the rule's own documentation doesn't false-positive — and requires each mint path to draw from a CSPRNG (`randomUUID`/`getRandomValues`) so an empty file can't pass vacuously. **Non-vacuous** (`show-mint-path-entropy-negative.sh`, hardened per the WO-5.8 lesson): a planted `Math.random()` in `command-id.ts` is CAUGHT (exit 1; output names `Math.random`; the `sed` re-root is asserted to have changed the gate).
- **Tests (`packages/contracts/test/command-id.test.ts`, 3/3).** `mintCommandId` returns a UUIDv4 `CommandIdSchema` accepts · 1000 mints are DISTINCT (CSPRNG entropy, not a constant) · `CommandIdSchema` REJECTS `cmd_1` / empty / nil-uuid / version-1 forms.
- **DELIBERATE non-change (flagged for the CTO):** `EventEnvelopeSchema.command_id` stays `z.string().min(1)` — NOT tightened to `CommandIdSchema`. The certification **reference chain** mints deterministic, readable ids (`cmd_chain_${seed}_s${step}_${n}`) so its 15-step run + HTML report stay byte-stable and inspectable (and `events-and-gates.test.ts` uses `cmd_1`); forcing UUIDv4 there would break reproducibility. The envelope is the transport; `CommandIdSchema` is the mint shape a production client validates against. If the CTO wants the envelope to enforce it, the reference chain needs a test-only id strategy first — a follow-up slice.
- **Version 0.9.5** (6 package.json + intra-deps + both docs manifests + 2 drift args); **lockfile regenerated** (SSH-clean — the v0.9.4 gate passes); **api-surface snapshot delta = `packageVersion 0.9.4→0.9.5` + the TWO new exports `CommandIdSchema` (schema) + `mintCommandId` (function) ONLY** (named + justified; no existing export/schema removed or changed — proven by structural diff). **`tokens.json` byte-UNALTERED.**
- **Evidence (my own runs):** typecheck 0 · **`run-gates.sh` EXIT 0** warm (mint gate green; its negative fires exit 1) · **mocks 8/8** (4/4 domains) **· chain 15/15** · **COLD PROOF in a CLEAN HOME EXIT 0** (fresh `--local` clone @ `e9f529d`, isolated `HOME` with no `~/.gitconfig`, **frozen** install, build, ALL GATES GREEN — the lockfile is genuinely cold-installable, honoring the v0.9.4 law).
- **Fresh-context verifier (MANDATORY — contracts path) — VERDICT: PASS, BLOCKING: none** (verbatim in `_review/WO-5.9/verifier-verdict.md`). Verified against the built dist by its OWN hands: `CommandIdSchema` accepts a real v4 and **rejects** `cmd_1`/empty/nil/v1/v3/bad-variant; `mintCommandId` returns accepted values, **1000 distinct**; the mint draws from `globalThis.crypto` with **no node `crypto` import** (RN-safe scanner exit 0); **the gate is non-vacuous AND robust** — proven by the verifier's own plants: a string that NAMES `Math.random` (no paren) PASSES, a real `Math.random()` CALL FAILS naming it, a comment-with-paren PASSES, a no-CSPRNG mint FAILS, and a mint file elsewhere (`packages/app/src/offline/commandId.ts`) is CAUGHT by naming convention; the envelope non-change rationale confirmed sound (tightening would break the deterministic reference chain + `cmd_1` tests); snapshot delta = exactly the 2 new exports + version; tokens.json byte-unaltered; full suite EXIT 0, mocks 8/8, chain 15/15.
- **Verifier non-blocking notes (recorded):** (1) it reproduced the WARM suite and honestly flagged that it did NOT re-run the clean-HOME cold proof (which I ran — `cold-clean-home-run-gates.log` in the packet, EXIT 0 @ `e9f529d`); (2) `CommandIdSchema` is case-insensitive (accepts uppercase v4) — RFC-4122-consistent input leniency; `mintCommandId`/`crypto.randomUUID` emit lowercase; deliberate, no change.
- **Pending / next:** the CTO's AMBER verdict. **Do NOT merge (AMBER — merge requires the CTO's paste).** On merge this is the next CONSUMER-VISIBLE pin; APPS adopts `mintCommandId` at `src/offline/commandId.ts`.

## 2026-07-13 · WO-5.9 · done (founder review PASS — merged; canon v0.9.5 — the next CONSUMER pin)
- **Merged — sha `2153661` (THE NEW CONSUMER PIN; APPS adopts `mintCommandId` at `src/offline/commandId.ts` on its next slice).** `CommandIdSchema` (branded UUIDv4) + `mintCommandId()` (OS CSPRNG via `globalThis.crypto`, RN-safe, throws with no CSPRNG, never `Math.random`, self-validating) + the `check-mint-path-entropy.mjs` gate (every repo inherits; fails a `Math.random(` call, requires a CSPRNG draw, non-vacuous). Snapshot delta = the two new exports + packageVersion only; `tokens.json` byte-unaltered.
- **The verifier's gate proof was the sharpest of the project** (CTO): the comment-plant (no false positive), the no-CSPRNG-draw plant (no vacuous pass), and the cross-package plant at APPS' exact future seam (`packages/app/src/offline/commandId.ts`).
- **OPEN QUESTION (named, no executioner, nothing broken) — tighten `EventEnvelopeSchema.command_id` to `CommandIdSchema`?** Deliberately NOT done in v0.9.5 and correctly FLAGGED, not smuggled. **Precondition:** a test-only id strategy for the DETERMINISTIC reference chain (which mints `cmd_chain_${seed}_s${step}_${n}` so its 15-step run + HTML report stay byte-stable; `events-and-gates.test.ts` uses `cmd_1`). Until that exists, the envelope stays `z.string().min(1)` (transport); `CommandIdSchema` is the mint shape a production client validates against. A named question, not a debt.
- **Both founder rulings (2026-07-13) now fully discharged:** ① the mint rule is encoded here; ② tokens.json source-of-truth needed no code — it ratifies the shipped v0.9.3 architecture (canon's `tokens.json` authoritative; badge at `dimension.iconSizePx.badge`; the `icon` group refused and `check-token-coverage.mjs`-guarded; bundle copies regenerate FROM canon).
- **Canon queue is EMPTY.** Next canon work opens only when the founder's tokens.json ruling produces designer-bundle regeneration work, or a consumer surfaces a canon gap.

## 2026-07-13 · Founder Decisions (Beurni) — command_id mint rule + tokens.json source of truth · CLOSED ✅
- **RULING ① — the `command_id` mint rule (CLOSED).** *"command_id is minted CLIENT-SIDE, exactly ONCE at command creation, PERSISTED with the command, and NEVER recomputed. FORMAT: UUIDv4. ENTROPY: the OS CSPRNG (expo-crypto on React Native; `crypto.randomUUID` elsewhere). `Math.random()` is FORBIDDEN as an idempotency-key source. Canon encodes this; consumers adopt on their next slice."* → closes the WO-5.7 Part E STOP-flag. **Opens WO-5.9** (below).
- **RULING ② — tokens.json source of truth (CLOSED).** *"The single source of truth for design tokens is platform-contracts `docs/design/tokens.json` at the current canon pin. Every bundle copy is a DOWNSTREAM artifact, regenerated FROM canon, never edited independently. The designer's proposed top-level `icon` group does not enter canon — the value lives at `dimension.iconSizePx.badge`."* → closes the WO-5.7 tokens.json-drift item. **Confirms the v0.9.3 architecture already shipped:** canon's `tokens.json` is byte-authoritative; the badge lives at `dimension.iconSizePx.badge`; the `icon` top-level group is refused (and `check-token-coverage.mjs` guards the stray). No code change owed — the ruling ratifies what canon already enforces; the bundle copies are the ones that must regenerate from canon, not the reverse.

## 2026-07-13 · WO-5.11 · in-review (canon v0.9.7 — TIER AMBER; do NOT merge; impl `63491c5`)
- **Base:** branched `canon/v0.9.7` off `origin/main` (contains `ba6f16d`, the v0.9.6 merge). Encodes the **QR-vitrine dimension primitives** the designer stated in the QRVitrine spec sheet (WO-7.2 handoff), so the Shop+ QR-vitrine component (WO-7.2b) has canon dimensions to render against. **This gates WO-7.2b — NOT a general consumer pin.**
- **The four primitives (`dimension.qr` in `docs/design/tokens.json`, mirrored in the built `dimension` export) — each DERIVED and byte-QUOTED from the sheet (derive-never-invent):** `quietZoneModules = 4` (« zone de silence de 4 modules en papier pur », ISO 18004) · `moduleMinPx = 4` (« module 4 px (= spacing.xs, jamais de sous-pixel) ») · `printModuleMinMm = 1.0` (« plancher d'impression 1,0 mm/module ») · `printSideMm = 48` (« 37 × 1,3 = 48 mm — scannable à bout de bras sur un mur de boutique »). Full quote-per-value record in `docs/derivations/QR-DIMENSIONS.md`. No value was absent from the sheet → **no STOP owed.**
- **CTO AMENDMENT (2026-07-13, from `handoff_rulings_0713`) folded in BEFORE any packet shipped — the token set is PRIMITIVES ONLY.** The earlier draft carried a fifth token `minSideDp = 148`; **dropped.** 148 was the **V3 worked example**, and the encoder is ruled **versions 1–5** — the real URL against canon's origin may force **V4+**, and a frozen 148 would put a V4 QR **under its own floor**. The on-screen side is now a **DERIVATION, not a token**: `minSidePx(version) = (modules(version) + 2×quietZoneModules) × moduleMinPx` — worked examples **V3 → 148 · V4 → 164** — recorded in `QR-DIMENSIONS.md` with the print check (`48 mm / (modules(V4)+8) = 48/41 ≥ 1.17 mm ≥ the 1.0 floor`, holds through V4; the component asserts the floor at render, consuming the primitives). `qr.ecc = "M"` is on the sheet but **deliberately OUT of scope** (encoder concern per questions.md Q4) — recorded, not encoded.
- **Gate wiring.** `dimension` joins `FIDELITY_MAP` (`token-surface.data.mjs`) so the four `qr` leaves are checked `tokens.json ⊆ built`, and joins `SUPERSET_OK` (`check-token-fidelity.mjs`) so the pre-existing **doc-derived** `controlHeightPx`/`iconSizePx` keys keep coexisting (still pinned by `check-design-dimensions.mjs`). `check-token-coverage.mjs` owns **exactly the four `dimension.qr` leaves**; its negative (`show-token-coverage-negative.sh` tamper B) now plants a stray on `dimension.qr` (was `spacing`) and confirms rejection.
- **Version 0.9.7** (6 package.json + intra-deps + both docs manifests + 2 drift args); **lockfile regenerated** (URL-form gate green — zero SSH-form git URLs); **api-surface snapshot delta = `packageVersion 0.9.6→0.9.7` ONLY** — the `exports` map is **byte-identical** (a ui-tokens-only change touches no `@platform/contracts` shape; proof in `_review/WO-5.11/logs/snapshot-delta.txt`). **`tokens.json` deliberately DOES change here** (gains `dimension.qr`) — the "tokens.json byte-equal to main" merge anchor of shape-only WOs does NOT apply to this token WO.
- **Evidence (my own runs):** three token gates green (fidelity 18 groups · design-dimensions 11 tokens · coverage 23 exports) · coverage negative fires **exit 1** on `dimension.qr.strayLeaf` · **`run-gates.sh` EXIT 0** warm · **COLD PROOF SHOWING ITS OWN ISOLATION** (`_review/WO-5.11/logs/cold-clean-home-run-gates.log`): fresh `export HOME=` (×1), explicit proxy `insteadOf` in the isolated HOME (×2), fresh pnpm store, `git clone --local` @ `63491c5`, `frozen-install rc: 0`, ALL GATES GREEN — satisfies the WO-7.1 evidence law.
- **Fresh-context verifier (MANDATORY — tokens/contracts path) — VERDICT: PASS, BLOCKING: none** (verbatim in `_review/WO-5.11/logs/verifier-verdict.md`). Given only the DoD, the derive-never-invent rule, the sheet path, and the sha — no build memory. Independently: confirmed exactly four `qr` keys (no `minSideDp`, no bare `148` as a token — 148 survives only as derivation prose); quoted all four French source lines from the sheet itself; re-derived the side math (V3=29, V4=33; 148, 164; V4 floor 1.17 ≥ 1.0); built the package and confirmed `dimension.qr` deep-equals tokens.json AND `controlHeightPx`/`iconSizePx` un-clobbered; **ran its OWN tamper** (`bogusFifth:999` on `dimension.qr`) and watched coverage fail with "leaf 'dimension.qr.bogusFifth' is checked by NO gate", then rebuilt to green (non-vacuous by its own hand); snapshot delta = packageVersion only, exports byte-identical; all 6 package.json + 2 manifests + run-gates args at 0.9.7; no retired names, no scope creep.
- **Verifier non-blocking note (recorded):** `printModuleMinMm` serializes as `1` in JSON (`1.0 === 1`) — deep-equal holds; not a defect.
- **Pending / next:** the CTO's AMBER verdict. **Do NOT merge (AMBER — merge requires the CTO's paste).** **Consumer note:** this is NOT a general consumer pin — **OZ (shop-plus) re-pins to this sha AT WO-7.2b's start** (the QR-vitrine component build); **WO-7.2a stays on `04af4b5`** (the QR primitives are irrelevant to it). No other consumer chases v0.9.7.

## 2026-07-13 · WO-5.11 · done (CTO review PASS — merged; canon v0.9.7 — gates WO-7.2b)
- **Merged — release-merge sha `508e94b` (THE v0.9.7 CONSUMER PIN); impl `63491c5` byte-preserved in history as provenance only.** `dimension.qr` carries the four designer primitives (`quietZoneModules=4`, `moduleMinPx=4`, `printModuleMinMm=1.0`, `printSideMm=48`), each byte-quoted from the QRVitrine spec sheet; the on-screen side is a **derivation, not a token** (`minSidePx=(modules+2·quietZone)·moduleMinPx`; V3→148, V4→164; component asserts the print floor at render).
- **CTO CORRECTION (pin designation — STANDING RULE):** **pins ride RELEASE MERGES, never impl commits.** The v0.9.7 consumer pin is **`508e94b` (the merge)**, not `63491c5`. My first report named the impl sha as OZ's pin — corrected here: OZ pins the release merge. The impl commit stays a provenance fact (byte-preserved), not a pin target.
- **The CTO amendment landed BEFORE any packet shipped** (`handoff_rulings_0713`): `minSideDp=148` dropped because the encoder is ruled versions 1–5 and a frozen 148 would sink a V4 QR under its own floor. Folded into the impl commit, not a follow-up — the packet never carried the fifth token.
- **Merge guards (my runs, all green):** heads agree · `ba6f16d` ancestor · main ⊆ candidate (not behind) · closing delta beyond `63491c5` = JOURNAL + packet zip only · `dimension.qr` EXACTLY four leaves in tokens.json AND the built export · `minSideDp` grep = 0 outside JOURNAL/derivation prose · coverage negative fires (exit 1) on a planted `dimension.qr.strayLeaf` · snapshot delta = packageVersion only (exports byte-identical) · tokens.json qr block = the four primitives · cold log greps its own isolation (`export HOME=`×1, `insteadof`×2, ALL GATES GREEN×1). Post-merge coverage gate green on main.
- **CONSUMER NOTE (journal per convention).** **NOT a general consumer pin.** **OZ (shop-plus) re-pins the release merge `508e94b` at WO-7.2b's start** (the QR-vitrine component build) — the merge is the pin, not the impl. **WO-7.2a stays on `04af4b5`** — the QR primitives are irrelevant to it. No other consumer chases v0.9.7.
- **Pending / next:** lane idle. **WO-5.12 (constrain `HandoffAuthorization.authorizedBy`) is QUEUED behind a founder ruling (section D)** — NOT started until it arrives founder-labelled. `contracts/` shape work = a §7 hard trigger; it does not open on CTO enthusiasm.

## 2026-07-13 · Founder Ruling (Beurni) — the payment-operator identity boundary · CLOSED ✅
- **RULING.** *"The payment-operator actor pattern is `ops:payment:*` (extending canon's `ops:<domain>:*` convention). Break-glass issuance validates by ALLOW-LIST: at BOTH halves, only an actor matching `^ops:payment:[A-Za-z0-9._:-]+$` validates; every other actor — supplier, dispatcher, `ops:moderation:*`, anything — is refused by non-match."* → opens **WO-5.12** (below).

## 2026-07-13 · WO-5.12 · in-review (canon v0.9.8 — TIER AMBER; do NOT merge; impl `cfa6760`)
- **Base:** branched `canon/v0.9.8` off `origin/main` (contains `508e94b`, the v0.9.7 merge). Encodes the payment-operator identity boundary. **On merge this is consumer-visible for PLATFORM** (its named interim's executioner).
- **Read-before-write.** `HandoffAuthorization` (`shapes/custody.ts`) carried ONE operator-identity field: `authorizedBy: IdSchema`. Canon (`ECOSYSTEM-MASTER-REFERENCE §5.494`; `Sera §115`; `components.md:161`) is explicit — the **authorized payment operator ISSUES** the break-glass authorization (« ÉMIS PAR »); the **dispatcher** does GROUND verification (« VÉRIFIÉ PAR ») and **may not issue** ("nobody holds both halves"). So `authorizedBy` = the issuer/payment-operator half; the dispatcher half is a SEPARATE act, not a field on this shape.
- **Built.** `PaymentOperatorActorSchema = z.string().regex(/^ops:payment:[A-Za-z0-9._:-]+$/, …)` (exported; re-exported by `index.ts` `export *`); `authorizedBy` constrained to it (was the generic `IdSchema`). Sibling of WO-5.10's `ops:moderation:*` actor, extending canon's `ops:<domain>:*`.
- **Audit of other operator-identity fields (STOP-AND-FLAG ambiguous ones — none were ambiguous):** `ModerationDecision.decided_by` = `ops:moderation:*` (different domain, already constrained — leave); `EventEnvelope.actor` = generic all-domain `z.string().min(1)` (not payment-specific — leave). No other payment-operator field exists.
- **The « both halves » flag (canon-grounded, no invention).** The ruling says "at BOTH halves"; the canon shape carries only the ISSUER identity. The dispatcher's VÉRIFIÉ PAR ground-verification is not a field on `HandoffAuthorization` (Sera §6.3 binds it to `{order,rider,buyer,amount,providerRef,expiry}` — no dispatcher identity), and the dispatcher is NOT an `ops:payment:*` actor (the ruling itself lists "dispatcher" as refused). So this slice constrains the one operator-identity field that exists; **inventing a dispatcher field would be an unauthorized canon-shape addition** — flagged, not done. The maker-checker "two halves" are two people/acts, not two fields.
- **Non-vacuous both directions.** `shapes.test.ts`: a valid `ops:payment:*` issuer parses; the four named actors (`supplier:aicha`, `logistics-service:dispatch` (sera's real dispatcher literal), `ops:moderation:x`, empty-suffix `ops:payment:`) all refuse, **failing only on `authorizedBy`**. Gate `show-payment-operator-negative.mjs` (wired in `run-gates.sh`) proves the same against the built dist (exit 1). Full contracts suite **97/97**.
- **Downstream fixture fix (YOUR OWN fixtures).** The one non-conforming operator value in the repo — `shapes.test.ts` base `authorizedBy: 'op_001'` — is fixed to `'ops:payment:op1'`. The certification 15-step chain builds **no** `HandoffAuthorization` (grep empty) — nothing to fix there.
- **Version 0.9.8** (6 package.json + intra-deps + both docs manifests + 2 drift args); **lockfile regenerated** (URL-form gate green); **`docs/design/tokens.json` UNTOUCHED** (byte-equal to main). **Snapshot delta:** `packageVersion 0.9.7→0.9.8` + one new export/schema `PaymentOperatorActorSchema` + `HandoffAuthorizationSchema.properties.authorizedBy` tightened `{minLength:1}` → `{pattern:"^ops:payment:[A-Za-z0-9._:-]+$"}` (the snapshot's `schemas` block is NON-opaque and now **drift-locks the regex**). No export removed; no other schema field changed.
- **CORRECTION (ground-every-claim — recorded).** My first snapshot-delta artifact + impl commit message claimed "registeredSchemas identical / the tightened field invisible in the snapshot." **That was WRONG** — my check compared a non-existent key (`registeredSchemas`), so two `undefined`s reported "identical"; the real key is `schemas` and it correctly DID change (the drift-lock above). The **fresh-context verifier caught it** (Finding 10). Fixed: `snapshot-delta.txt` corrected, impl commit message amended (message-only, tree byte-identical: `ecb40e5` → `cfa6760`), cold proof re-run against `cfa6760`. No code changed — the behavior was always the stronger, correct one.
- **Evidence (my own runs):** `run-gates.sh` **EXIT 0** warm (payment-operator negative fires exit 1); **COLD PROOF SHOWING ITS OWN ISOLATION** (`_review/WO-5.12/logs/cold-clean-home-run-gates.log`): `export HOME=` ×1, proxy `insteadOf` ×2, `git clone --local` @ `cfa6760`, `frozen-install rc: 0`, ALL GATES GREEN.
- **Fresh-context verifier (MANDATORY — custody/money/contracts path) — VERDICT: PASS, BLOCKING: none** (verbatim in `_review/WO-5.12/logs/verifier-verdict.md`). No build memory. Independently: regex exact (anchored, `+` not `*`); `authorizedBy` constrained, no other payment field missed; **9/9 adversarial refusals** against the built dist (incl. trailing-`\n`, leading-space, prefix-trick `xops:payment:d`, multiline injection — JS `$` without `m` is multiline-safe); 3/3 valids parse; gate non-vacuous + wired; fixture fixed; suite 97/97; chain builds no HandoffAuthorization; tokens untouched; versions consistent; **the « both halves » scoping judged canon-faithful** (constraining the issuer field is correct; a dispatcher field would be canon-invention). Its one flag (Finding 10, the snapshot wording) is the correction recorded above — a doc fix, not a code defect.
- **Pending / next:** the CTO's AMBER verdict. **Do NOT merge (AMBER — merge requires the CTO's paste).** On merge, consumer-visible for PLATFORM.

## 2026-07-13 · WO-5.12 · done (CTO review PASS — merged; canon v0.9.8 — PLATFORM's pin)
- **Merged — release-merge sha `67bda02` (THE v0.9.8 CONSUMER PIN — PLATFORM's, the executioner of its named interim; pins ride release merges, not impl commits); impl `cfa6760` byte-preserved in history as provenance.** `PaymentOperatorActorSchema` (`^ops:payment:[A-Za-z0-9._:-]+$`) constrains `HandoffAuthorization.authorizedBy` — the ISSUER (« ÉMIS PAR ») half of the break-glass maker-checker; allow-list, non-vacuous both ways; the four named non-payment actors refused.
- **Merge guards (my runs, all green):** heads agree · `508e94b` ancestor · main ⊆ candidate (not behind) · impl `cfa6760` byte-preserved (`git diff ecb40e5 cfa6760` empty — the message-only amend) · closing delta beyond `cfa6760` = JOURNAL + packet zip only · `PaymentOperatorActorSchema` exported in the built dist (parses `ops:payment:x`, refuses `supplier:x`) · snapshot `authorizedBy` carries the pattern `^ops:payment:[A-Za-z0-9._:-]+$` · payment-operator negative exits 1 refusing all four · contracts suite 97/97 · `tokens.json` byte-equal to main · cold log greps its own isolation. Post-merge on main: negative exits 1, snapshot pattern present.
- **Finding 10 → the record (CTO ruling): the artifact-class vacuous check.** A comparison against a NON-EXISTENT key (`registeredSchemas`) reported "identical" while the real `schemas` block had correctly changed — the drift-lock it revealed (the snapshot byte-pins the `ops:payment` regex on `authorizedBy`) is the stronger, desired behavior. Disposition ruled standard: artifact corrected · message-only amend disclosed with trees byte-identical (`ecb40e5`→`cfa6760`) · the verifier's verdict left untouched. **Lesson to carry: a delta check must assert the key it compares EXISTS, or an absent key silently passes as "identical."**
- **CONSUMER NOTE.** **PLATFORM pins `67bda02` (v0.9.8)** — the executioner of its named interim (payment-operator identity). Other consumers do not chase this canon-internal bump.
- **Pending / next:** lane idle. **Canon's E4 queue is EMPTY.** Next canon work opens only on a founder ruling with canon work or a consumer-surfaced gap.

## 2026-07-13 · GP-CANON · grounding pass (🟢 — no product code; report only, no version/merge)
- **1. Localization readiness (mo/dyu).** `@platform/i18n` models mo/dyu as **optional per-entry fields** `moore?`/`dioula?` on `CatalogEntrySchema` (`packages/i18n/src/catalog.ts:28-29`); `fr` is the required base (`:26`). **No locale-keyed structure, no runtime resolver/`t()`/fallback** — `index.ts` exports only the catalog schema + pure lint fns (`packages/i18n/src/index.ts:6-7`); consumers pick the field, "fallback" is structural (fr required, mo/dyu optional). **Register-tags:** `register` (money|selling|neutral, `catalog.ts:8`) + `screenClass` are REQUIRED on every entry; the lint applies banned-token + register-mismatch to **every** text variant incl. moore/dioula (`lint.ts:79-126`), while reading-budgets bind **`fr` only** (`lint.ts:137`). **§10.5 condition (d):** a moore/dioula variant on a `register:money` OR `class:instruction` entry is a VIOLATION (`lint.ts:170-176`) — money/instruction stay French. **Today:** catalog has 8 entries, ALL money/checkout, **0 moore, 0 dioula**, 2 audioScriptRef. **Layout-consequence tokens:** `type.scale` + `variableAxes.wdth[75,125]` (tokens.json:64), `money.abbreviation/truncation: "forbidden"` (:154-155), `touch.minTargetPx 48` (:85), the `type.note` French-long-text caveat (:78); `label` screenClass is budget-exempt (D18). **Governing doctrine (quoted):** Contract §10.5 "Whisper of Level C — Mooré/Dioula touches ONLY in genuinely universal moments … never in instructions or money copy … a garnish, not the dish" (`docs/Ecosystem-Engineering-Execution-Contract.md:210`), condition (d) (:225), 6th-grade (:213). **Translations + recordings are FOUNDER-GATED** — plumbing is ready (optional fields + lint coverage), content is not present and not mine to propose.
- **2. Open-question ledger (consolidated, with sources).**
  - **(Q1) IdSchema accepts whitespace-only ids.** `IdSchema = z.string().min(1)` (`packages/contracts/src/shapes/common.ts:11`); empirically `" "`, `"\t"`, `"\n"`, `"x "` all PARSE (min(1) is length, not trimmed-length). Same pattern in `IsoTimestampSchema` (`common.ts:9`) and `providerTransactionReference` (`custody.ts:236`). Attributed to **B1's observation** (a boutik slice; I could not locate a journaled line in `boutik-plus` — its JOURNAL is the CI/PAT saga — so sourced by the canon code + empirical behavior, not a boutik quote). **A ruling would unblock:** whether canon Id/timestamp/reference strings must reject blank/untrimmed values (e.g. `.trim().min(1)` or a format regex) — a broad, cross-shape `contracts/` tightening (§7). Recommendation available on request; not mine to decide.
  - **(Q2) Tighten `EventEnvelopeSchema.command_id` to `CommandIdSchema`.** `command_id: z.string().min(1)` (`packages/contracts/src/events.ts`); `CommandIdSchema` (branded UUIDv4) shipped at v0.9.5 but the envelope was deliberately NOT tightened (JOURNAL 2026-07-13 WO-5.9, "OPEN QUESTION … named, no executioner"). **Named precondition:** a test-only id strategy for the DETERMINISTIC reference chain (mints `cmd_chain_${seed}_s${step}_${n}`, byte-stable; `events-and-gates.test.ts` uses `cmd_1`). **A ruling would unblock:** the transport enforcing the mint shape at the boundary.
  - **(Q3, minor) `EvidenceBundle.coarseLocation` doc-consistency (D19).** Now present only in `docs/Sera-Build-Spec.md:80` as `coarseLocation?`; absent from Boutik/Shop §5.6. Schema keeps the optional-field bridge (`shapes/custody.ts` EvidenceBundle). Séra OWNS Evidence&Validation, the field is optional, and the founder already touched it (WO-2.8 edit A5) — **low-stakes, effectively settled**; listed for completeness. A ruling would unblock nothing functional (doc tidiness only).
- **3. S8 (SP8 « Earnings & reputation ») — reputation-defining-sentence ABSENCE: CONFIRMED, both grep directions.** **Presence:** canon NAMES reputation but **defines no score/formula/rating mechanic** — it is **Deferred-to-after-E1** (`Contract:85`), an E4/M6 surface (`Shop-Plus-Building-Plan §M6`), shown as "**reputation with sample size**" (`Shop-Plus-Build-Spec:180`). **Absence (the defining sentence):** canon constrains reputation only NEGATIVELY — "**no black-box score**" (`Shop-Plus-Building-Plan:76` SP6.2), Law 5 deterministic (no ML ranking), "ratings only ≥ min verified reviews" + "no fake counts" (`Building-Plan:92`), "proof only from validated deliveries" (SP-I17 `Build-Spec:57`), single-level + related-party excluded — and the ACTUAL mechanic ("**min review count before ratings**", drivers, moderation SLA) is an explicitly **⏳ OPEN Cercle question** (`Shop-Plus-Build-Spec:217`). Contracts code carries **no** reputation shape — only the event NAME `reputation.updated.v1` (`packages/contracts/src/events.ts:115`). **Precondition discharged:** no reputation-defining sentence exists; any S8 build needs a founder ruling on the mechanic.
- **Deliverable:** the report (delivered to the founder). No code, no version bump, no merge.

## 2026-07-13 · WO-5.13 · in-review (canon v0.9.9 — TIER AMBER; do NOT merge; impl `63e9783`)
- **The STOP (OZ, SP#001-A):** `StorefrontSchema` lacked `name`/`zone`/`category`/`createdAt`/`updatedAt` — the Seller #001 aggregate's fields. Extended **additively, by derivation**.
- **Read-before-write (verbatim in the derivation record).** The prior shape was `{ id, resellerId, slug, discoverable(bool), curatedItems }` `.strict()`. Every one of those fields is **byte-unchanged**; the five new keys are appended after `curatedItems`. `discoverable` was ALREADY a required boolean — the WO's "discoverable if absent" is conditional; it is not absent, so untouched (not re-added). Ownership: §5.2 "owns Storefront & Attribution" (`Shop-Plus-Build-Spec:80-81`).
- **The added fields (derived from SP0.2, `Shop-Plus-Building-Plan:34` "reseller activation (store name/zone/category focus)").** `name: z.string().min(1).max(120)` · `zone: z.string().min(1)` · `category: z.string().min(1)` · `createdAt`/`updatedAt: IsoTimestampSchema`. **`.max(120)` is a boundary guard, NOT a canon value** (no spec bounds a store name; no `.max()` precedent in the codebase) — marked founder-overridable in code + doc.
- **STOP-honored (not invented):** `zone` stays a free **display string** — « Rood Woko, Ouagadougou » precedent (`docs/design/copy.md:157`); a zone enum/gazetteer is a **founder decision**, NOT added. `category` stays a free string — the category-floor taxonomy is an **open founder decision**, NOT added. **Per-event payload shapes:** NONE exist in canon (events = `EVENT_NAMES` union + the single `EventEnvelopeSchema`); `storefront.created/published.v1` are names (`events.ts:98-99`). Introducing a storefront-only payload shape would be a new architecture — per the WO's "or quote what exists," none added.
- **Additive is safe:** **nothing constructs a `Storefront` anywhere in the repo** (grep → only a doc-comment `quote.ts:28`; the 15-step chain builds none), so required fields break no fixture. OZ is the one consumer and needs these fields.
- **Non-vacuous, both directions (`shapes.test.ts`).** A full storefront parses; **empty name, over-max name (121), missing `createdAt`/`updatedAt`/`zone`/`category`, a stray field under `.strict()`, AND a missing `discoverable`** (additive-integrity check) all refuse. Full contracts suite **106/106**.
- **Version 0.9.9** (6 package.json + intra-deps + both docs manifests + 2 drift args); **lockfile regenerated** (URL-form gate green); **`docs/design/tokens.json` UNTOUCHED**. **Snapshot delta:** `packageVersion 0.9.8→0.9.9` + `StorefrontSchema.properties` gains exactly `name/zone/category/createdAt/updatedAt` (5 added, 0 removed, **pre-existing 5 byte-unchanged**, all 5 appended to `required`); **no export added/removed/changed**; no other schema changed.
- **Evidence (my own runs):** `run-gates.sh` **EXIT 0** warm; **COLD PROOF SHOWING ITS OWN ISOLATION** (`_review/WO-5.13/logs/cold-clean-home-run-gates.log`): `export HOME=` ×1, proxy `insteadOf` ×2, `git clone --local` @ `63e9783`, `frozen-install rc: 0`, ALL GATES GREEN.
- **Fresh-context verifier (MANDATORY — contracts shape path) — VERDICT: PASS, DEFECTS: 0** (verbatim in `_review/WO-5.13/logs/verifier-verdict.md`). No build memory. Independently: additive confirmed (pre-existing 5 byte-unchanged, `.strict()` retained, `discoverable` still required boolean); field schemas exact, **no enum near zone/category**; its own dist parse-script — full valid parses, empty/over-max/missing-each/stray/missing-discoverable all refuse, 120-char accepts; nothing constructs a Storefront elsewhere; suite 106/106; the no-payload-shape judgment concurred (payload shapes exist only in the cert harness, untouched); snapshot delta to the property; tokens untouched; versions consistent; no feature creep.
- **Verifier NON-BLOCKING notes (recorded, flagged to the founder):** **N1** — `name: " "` parses (min(1) accepts a space) — the **same class as open question Q1** (IdSchema whitespace); a trim ruling would close it globally, not mine to change under an open convention. **N2** — `IsoTimestampSchema = z.string().min(1)` (`common.ts:9`) does NOT validate ISO-8601 format, so `createdAt`/`updatedAt` are non-empty strings, not format-checked; pre-existing canon primitive used by every dated shape — worth the founder knowing. **N3** — JOURNAL lands as a separate commit (this one); standard pattern.
- **Pending / next:** the CTO's AMBER verdict. **Do NOT merge (AMBER — merge requires the CTO's paste).** **Consumer note:** OZ re-pins at SP#001-A's resume (the resume takes the current release); nobody else chases.

## 2026-07-13 · WO-5.13 · done (CTO review PASS — merged; canon v0.9.9 — OZ's resume pin; the spine held on it)
- **Merged — release-merge sha `1c98ad1` (THE v0.9.9 CONSUMER PIN; pins ride release merges); impl `63e9783` byte-preserved in history.** `StorefrontSchema` gains `name`/`zone`/`category`/`createdAt`/`updatedAt` additively; zone+category stay free display strings by founder STOP; no per-event payload shapes invented.
- **Merge guards (my runs, all green):** heads agree · `67bda02` ancestor · main ⊆ candidate (not behind) · impl `63e9783` byte-preserved · closing delta = JOURNAL + packet zip only · **parse script:** full storefront parses AND empty-name / 121-char / each missing field (name/zone/category/createdAt/updatedAt/discoverable) / a stray key all refuse · snapshot delta = the five properties added to `StorefrontSchema` (pre-existing byte-unchanged) + packageVersion, no export change · `tokens.json` byte-equal to main · contracts **106/106** · cold log greps its own isolation. Post-merge on main: snapshot carries the new `name` property.
- **The additive property was proven three independent ways** (CTO): the source diff (existing lines byte-unchanged), the parsed snapshot (`properties` +5, pre-existing 5 identical), and the runtime parse script (old required fields still enforced). Both no-enum STOPs held **in the code itself**; `.max(120)` recorded as a founder-overridable boundary guard, not a canon value.
- **NON-BLOCKING notes carried to the founder (from the verifier):** **N1** `name: " "` parses (min(1)) — the **open Q1 whitespace class**; a trim ruling closes it globally. **N2** `IsoTimestampSchema` is not ISO-format-validated (pre-existing primitive across all dated shapes). Both are the standing `z.string().min(1)` convention, not this slice's defect.
- **CONSUMER NOTE.** **OZ (shop-plus) re-pins `1c98ad1` at SP#001-A's resume** (the resume takes the current release). Nobody else chases v0.9.9.
- **Pending / next:** lane idle. **Canon's queue is EMPTY.** Two live founder questions remain open (Q1 IdSchema-whitespace — now with a second witness in the storefront `name`; Q2 command_id envelope tightening); nothing is blocked on me.

## 2026-07-15 · Founder Ruling (Beurni) — the whitespace tightening · CLOSED ✅
- **RULING.** *"whitespace-only strings are invalid on Id-class and name-class fields: canon enforces trimmed non-empty."* → closes open question **Q1** (IdSchema whitespace) and its second witness (storefront `name`, WO-5.13 note N1). Opens **WO-5.14** (below).

## 2026-07-15 · WO-5.14 · in-review (canon v0.9.10 — TIER AMBER; do NOT merge; impl `55c7aef`)
- **The primitive.** New canonical **`TrimmedNonEmptyString`** — a non-empty string with no leading/trailing whitespace (internal whitespace preserved), encoded as a **regex `^\S([\s\S]*\S)?$` (NOT a `.refine`)** so it is JSON-Schema-representable and **drift-locks in the shape-freeze snapshot**. Lives in **`@platform/kernel-types`** (`src/strings.ts`) — the base package both kernel-types shapes and contracts shapes consume — and is re-exported from `@platform/contracts` (`shapes/common.ts`, `IdSchema = TrimmedNonEmptyString`).
- **Reading (flagged, §7).** "Trimmed non-empty" encoded as the STRICTER "equal to its trimmed form AND non-empty" — refuses both whitespace-only (`" "`/`"\t"`/`""`) AND surrounding-whitespace (`" x"`/`"x "`); internal spaces preserved. Chosen because "trimmed" implies no surrounding whitespace and an untrimmed id is a latent key bug; relaxable to contains-a-non-space if the founder meant only whitespace-only.
- **BREAKAGE AUDIT (done FIRST, per the WO) — PROVED ZERO both directions.** Grepped every consumer repo's fixtures/tests for whitespace-only AND surrounding-whitespace values on id/name/zone/category/landmark/alias fields → **0** and **0**. No STOP; no fixture breaks (suite green, mocks 8/8, chain 15/15).
- **Disposition (every `min(1)` string field, BOTH packages).** TIGHTENED: id-class (`IdSchema` → all id fields; kernel `UserIdSchema`, `PhoneAliasSchema`), name-class (`ProductVersion.name`, `Storefront.name` keeps `.max(120)`), display strings (`zone`/`category`/`zoneFrom`/`zoneTo`/`zones[]` + kernel `Location.zone`/`landmark`). EXEMPT-with-reason: timestamps (`IsoTimestampSchema` — N2 governs), machine refs (`slug`/`productCode`/`signature`/`collectRef`/`humanReasonRef`/`MediaRef.ref`/`mimeType`/`Location.maskedRelay`), free-text (`Location.directions`, states/reasons, `handlingClass`), policy versions, event envelope (`command_id` = Q2). Full table in `docs/derivations/WHITESPACE-TIGHTENING.md`.
- **⚠ CAUGHT-IN-REVIEW & FIXED — the kernel-types miss (this is the value of the fresh-context verifier).** My FIRST cut audited only `contracts/src` and **silently skipped `packages/kernel-types`** — leaving `UserId` (id-class) and `Location.zone`/`landmark` (delivery-route display) untightened while their `Storefront`/`ProductVersion` twins were tightened: **canon internally inconsistent** (`Storefront.id` refused `" "` but `User.id` accepted it; the derivation doc's "every field" claim was false). Fresh-context verifier PASS 1 = **FAIL**, correctly. **Fix:** relocated the primitive INTO `@platform/kernel-types` (correct layering — the base package, single source of truth), re-exported from contracts, tightened `UserId`/`PhoneAlias`/`Location.zone`/`landmark`; the audit table now covers BOTH packages. Impl commit **amended** (`e4f94e4` → `55c7aef`; never pushed); cold proof re-run at the new sha.
- **Non-vacuous both directions.** `shapes.test.ts`: accept clean / refuse whitespace, reaching real fields — `StorefrontSchema` id/resellerId/name/zone/category AND `User.id`/`phoneAlias.alias` AND `Order.dropoff.zone`/`landmark` each refuse `" "`/`"\t"`/`""`/`" x"`/`"x "` on-field. Gate `show-trimmed-string-negative.mjs` (wired in `run-gates.sh`): clean storefront+order parse; **35 plants** (25 storefront + 10 order.dropoff kernel Location) refuse on-field. Suite **126/126**.
- **Version 0.9.10**; **`docs/design/tokens.json` UNTOUCHED**; lockfile regenerated. **Snapshot delta:** `packageVersion 0.9.9→0.9.10` + **1 new export** `TrimmedNonEmptyString` + **115 PURE `pattern` additions** of `^\S([\s\S]*\S)?$` (contracts id/name/display + kernel `Location.zone`/`landmark` everywhere used); **0 non-additive changes** (`Storefront.name` keeps `maxLength:120`; timestamps + `Location.directions` keep no pattern). **`UserId`/`PhoneAlias` stay `{}`** (brand `.transform` → opaque JSON-Schema; behaviour-locked by the tests/fixture, not the snapshot — named).
- **Evidence (my runs):** `run-gates.sh` **EXIT 0** warm; **COLD PROOF SHOWING ITS OWN ISOLATION** @ `55c7aef` (`export HOME=` ×1, proxy `insteadOf` ×2, `git clone --local`, `frozen-install rc: 0`, ALL GATES GREEN).
- **Fresh-context verifier (MANDATORY — contracts+kernel path) — PASS 1 FAIL (the miss above), PASS 2 (re-verify @ `55c7aef`): PASS, DEFECTS: 0** (both verbatim in `_review/WO-5.14/logs/verifier-verdict.md`). PASS 2, by its own dist parse-script, confirmed **no** id/name/display field where a `Storefront` twin refuses `" "` but a `User`/`Order` twin accepts it (inconsistency gone); single primitive definition, no dep cycle; every `min(1)` field dispositioned; breakage ZERO; 126/126 + cert 8/8 + chain 15/15; snapshot delta = 115 pure pattern-adds, 0 non-additive. One NON-BLOCKING note: the storefront on-field test uses `JSON.stringify(issues).contains` (the gate fixture already does strict `path.join()` — coverage solid).
- **Pending / next:** the CTO's AMBER verdict. **Do NOT merge (AMBER — merge requires the CTO's paste).** **Consumer note:** nobody chases; consumers inherit the tightening at their next natural re-pin.

## 2026-07-15 · WO-5.14 · done (CTO review PASS — merged; canon v0.9.10 — Q1 CLOSED)
- **Merged — release-merge sha `0f2fce8` (the v0.9.10 consumer pin; pins ride release merges); impl `55c7aef` byte-preserved.** `TrimmedNonEmptyString` (regex `^\S([\s\S]*\S)?$`, drift-locked) in `@platform/kernel-types`, re-exported by contracts; id-class + name/zone/category (contracts) + `UserId`/`PhoneAlias`/`Location.zone`/`landmark` (kernel-types) are trimmed non-empty. **Q1 (IdSchema whitespace) CLOSED.**
- **DOCTRINE journaled (CTO ruling).** **(1) THE VACUOUS-AUDIT CLASS — a cross-package ruling is audited across the WORKSPACE.** A one-package audit of a workspace law is vacuous: my first cut audited only `contracts/src`, leaving `User.id` and `Storefront.id` under different law (one refused `" "`, the other accepted it). The fresh-context verifier's PASS-1 FAIL caught it; the fix relocated the primitive to the base package (kernel-types) and audited both. *(Sibling of WO-5.12's Finding-10 artifact-class vacuous check: a check that doesn't cover its whole subject silently passes.)* **(2) The stricter "equal-to-trimmed" reading is RATIFIED as the ruling's own text** — an untrimmed id is a latent key bug; the audit proved zero breakage; the regex form drift-locks. **(3) Branded-schema opacity stands as named** — `UserId`/`PhoneAlias` are behaviour-locked by the suite, not the snapshot (their `.transform` erases the JSON-Schema projection).
- **Merge guards (my runs, all green):** heads agree · `1c98ad1` ancestor · main ⊆ candidate · impl `55c7aef` byte-preserved · closing delta = JOURNAL + packet zip only · `TrimmedNonEmptyString` defined ONCE in kernel-types + re-exported · independent parse: `User.id`/`Order.dropoff.zone`/`Storefront.name` each refuse `" "`/`"\t"`/`" x"`/`"x "` and accept internal whitespace · snapshot delta = +1 export + 115 pure pattern-adds, 0 non-additive · trimmed-string negative exit 1 · contracts **126/126** · chain **15/15** · `tokens.json` byte-equal · cold log greps its own isolation. Post-merge on main: negative exit 1, `Order.dropoff.zone` carries the pattern.
- **CONSUMER NOTE.** Nobody chases. **OZ stays on 0.9.9 through the spine and inherits at its next natural re-pin** — at which point any dirty value in a then-existing aggregate fails parse LOUDLY, which is the tightening doing its job. **Q2** (command_id envelope) stays parked-open (exempted here by name).
- **Pending / next:** lane idle. **WO-5.15 (the réputation law)** is the queued slice — opens on the founder ruling that defines the mechanic (GP-CANON §3 confirmed canon carries no reputation-defining sentence; "no black-box score"; "min review count before ratings" is the open Cercle question). Not started until it arrives founder-labelled.

## 2026-07-15 · Founder Ruling (Beurni) — S8, la réputation · CLOSED ✅
- **RULING.** *"La réputation d'une revendeuse est LE NOMBRE DE VENTES LIVRÉES. An exact count — never a rank, never a score, never a comparison. Source: delivery-validated events attributed to her storefront. Rendered verbatim (« N ventes livrées »), shown from the first delivered sale (floor = 1, founder-overridable), deterministic and explainable in that one sentence."* → **closes S8** (the last GP-CANON open item — canon had carried no reputation-defining sentence). Opens **WO-5.15** (below).

## 2026-07-15 · WO-5.15 · in-review (canon docs — TIER AMBER; do NOT merge; impl `9fd2ccf`; NO version bump)
- **Encoded** the réputation law in the canonical Shop+ doc set — `Shop-Plus-Build-Spec.md` **SP8**: *réputation = the exact count of delivered sales, « N ventes livrées », never a rank/score/comparison, sourced via the locked `Order.resellerId` (SP-I01), shown from the first delivered sale (floor 1, founder-overridable), deterministic + explainable* — reinforces Ten-Laws §5 and the "no black-box score" guardrail.
- **No self-contradiction (cross-doc completeness doctrine, WO-5.14).** The two pre-ruling "sampled" references were reconciled in the same commit: `Shop-Plus-Building-Plan.md` SP6.2 ("Reputation (sampled)" → "delivered-sales count") and `ECOSYSTEM-MASTER-REFERENCE.md` SP8. Verifier grep confirmed no surviving "sample/sampled" reputation contradiction.
- **DERIVE-OR-STOP — DERIVABLE from existing shapes; NO STOP; NO payload invented.** Linkage verified in the bytes: `delivery.validated.v1` (events.ts:61) → `ValidationDecision.taskId` (custody.ts:280) → `DeliveryTask.orderId` (logisticsTaskBase, custody.ts:51) → **`Order.resellerId` (commerce.ts:181, LOCKED — SP-I01)** → `Storefront.resellerId`. So réputation(R) = |{ Order : Order.resellerId=R ∧ its delivery task reached `delivery.validated.v1` }|. **Honest flag recorded:** the canonical `EventEnvelopeSchema` is generic (no subject id; canon has NO per-event payload — the `*PayloadSchema` shapes live only in the certification harness), so the counter derives from the Order/ValidationDecision projections; the harness `EligibilityEventPayload{order_id,task_id,validation_id}` is the E1 transport, not canon. No canon payload proposed.
- **Derive-never-invent held:** only the founder's own values (floor 1, marked founder-overridable); no fraud-decrement, time-window, or related-party mechanic invented.
- **Scope / version.** **Docs only — no `contracts/` shape moved**, so the api-surface snapshot AND all package versions are **UNCHANGED** (packageVersion stays `0.9.10`, no bump — pins are by commit sha, so OZ reads this encoding at the merge sha). Only the doc-hash manifest is re-synced (3 doc hashes; both copies). `docs/design/tokens.json` untouched.
- **Evidence (my runs):** `run-gates.sh` **EXIT 0** warm — **drift-check OK: 11 canonical docs match manifest (packageVersion 0.9.10)** (pristine passes, tampered fails); **COLD PROOF SHOWING ITS OWN ISOLATION** @ `9fd2ccf` (`export HOME=` ×1, proxy `insteadOf` ×2, `git clone --local`, `frozen-install rc: 0`, drift-check OK, ALL GATES GREEN).
- **Fresh-context verifier (MANDATORY — canon-docs / DERIVE-OR-STOP path) — VERDICT: PASS** (verbatim in `_review/WO-5.15/logs/verifier-verdict.md`). Independently re-walked every linkage hop in the files (agreed DERIVABLE, STOP correctly not taken, no payload invented); confirmed the envelope is subject-less + canon has zero `*PayloadSchema`; confirmed all three "sampled" references reconciled with no surviving contradiction; ruling transcribed verbatim; derive-never-invent held; docs-only, manifest re-synced with `git status` clean and packageVersion 0.9.10; French Voice clean.
- **Verifier observations (acted on / recorded):** **(2, acted on)** the old SP6.2 phrase "related-party exclusion" — I added a completeness paragraph to the derivation record confirming it is NOT lost (survives at SP6.3, SP-I17, §6, Risk/Moderation; structurally moot for the count since only delivery-VALIDATED sales count); impl amended `ea06d26`→`9fd2ccf`, cold proof re-run. **(1, recorded for the founder)** gated Cercle carries a distinct per-delivery verified-review/ratings object (`Build-Spec:217` open ⏳ "min review count before ratings") — a DIFFERENT object from the headline réputation count; no collision today, but a future Cercle screen must not surface a review-rating AS "réputation."
- **Pending / next:** the CTO's AMBER verdict. **Do NOT merge (AMBER — merge requires the CTO's paste).** **Consumer note:** OZ's S8 build order is written against this encoding, queued behind its spine; OZ reads it at the merge sha.

## 2026-07-15 · WO-5.15 · done (CTO review PASS — merged; canon docs; S8 CLOSED; no version bump)
- **Merged — release-merge sha `006e2f5` (OZ's S8 build opens on it — docs-only, so consumers pin by commit sha, not a version string; packageVersion stays `0.9.10`); impl `9fd2ccf` byte-preserved.** Réputation = the exact count of delivered sales (« N ventes livrées ») is now canon in Shop-Plus-Build-Spec SP8; the two pre-ruling "sampled" references reconciled. **S8 CLOSED.**
- **Merge guards (my runs, all green):** heads agree · `0f2fce8` ancestor · main ⊆ candidate · impl `9fd2ccf` byte-preserved · closing delta = JOURNAL + packet zip only · drift-check "11 canonical docs match manifest (0.9.10)" on the candidate · `git diff` on `contracts/src` + snapshot + `tokens.json` + package.json vs main **EMPTY** (docs-only, no shape/version move) · the derivation record quotes the ruling verbatim · grep "sampl" leaves only the two unrelated quality-card hits (« fulfillment sample (with n) ») · cold log greps its own isolation. Post-merge on main: manifest clean (no drift), SP8 carries the law.
- **The five-hop linkage proof + cross-doc reconciliation is the standard** (CTO): `delivery.validated.v1` → `ValidationDecision.taskId` → `DeliveryTask.orderId` → `Order.resellerId` (LOCKED, SP-I01) → `Storefront.resellerId`; derivable from existing shapes, nothing invented; the subject-less-envelope flag is honest.
- **CANON LEDGER IS CLEAN for the first time** (CTO): **Q1 closed** (whitespace tightening, WO-5.14), **S8 closed** (this WO), **Q2 parked by choice** (command_id envelope tightening — named, precondition documented, not owed).
- **Pending / next:** lane idle. Canon queue is EMPTY; no open question is owed. Next canon work opens only on a founder ruling carrying canon work or a consumer-surfaced gap.

## 2026-07-15 · WO-FP-0 · in-review (canon v1.0.0 — TIER 🟠 AMBER; do NOT merge; impl `ef2b565`)
- **Faso Premium enters the token pipeline.** `docs/design/tokens.json` is now **Faso Premium v2** (`$meta.version 2.0.0`): the shared palette + four per-app accent sets (boutik · shop · pwa · sera) + the type scale & two family names + radii + the four geometry constants + the seven fp\* motions — **every value transcribed verbatim from `handoff_redesign/README.md` § The shared system.** Byte-mirror `packages/ui-tokens/src/faso-premium.ts`; warrant `docs/derivations/FASO-PREMIUM-TOKENS.md`.
- **The STOP I opened, resolved by CTO ruling (all four, journaled in the derivation record):** ① the set is **SEVEN** fp\* (README § Motion canonical); fpFade/fpToast documented-out. ② timing encodes **verbatim whatever its form** — cubic-bezier (fpIn, fpUp) or keyword (fpPulse `ease` · fpBar `ease-in-out` · fpShimmer `linear` · fpShake `ease`); inventing a bezier for a keyword-timed animation would be the violation. ③ **fpPop** = duration `{300,450}` + `cubic-bezier(.2,.8,.2,1)` (the only explicit curve; usage split `.3`×8/`.45`×5/`.5`×3 journaled verbatim, founder-overridable). ④ **ranges → `{min,max}` verbatim** (view/heroMoney/body/caps/art/buttonSecondary/fpPop), never collapsed.
- **THE HIERARCHY LAW (CTO, journaled — governs the four app lanes):** the README defines the system; tokens encode it; prototypes are pixel-source for app-local detail. Where a prototype strays from a token (the 19× `fpIn` `.3s` no-bezier stragglers vs the 50× `.32s`+bezier token), **the token wins** and the app normalizes; where a value exists only in a prototype (fpFade, fpToast, per-usage variance), the app derives locally.
- **Architecture — v2 primary, v1 under a legacy path (WO item 2).** Grand Teint moved **byte-verbatim** (git renames, zero content change): `tokens.json → tokens.grand-teint.json`; `src/{family,themes,index}.ts → src/legacy/`. Root `@platform/ui-tokens` = Faso Premium; **`@platform/ui-tokens/legacy` = Grand Teint** (frozen for the transition window; the Séra dispatch console stays on it this wave). Both surfaces **stay fully gated**: `token-surface.data.mjs` now lists two SURFACES and the three token gates loop — FP is fidelity+coverage-owned (no doc-derived this wave); GT keeps its 11 design-dimension warrants. **Coverage meta-gate owns the new groups; the planted stray (stray export + stray leaf on `motion.fpIn`) fires.**
- **Transition-window consumer repoints → `./legacy`** (their reskin is a later named slice): `certification/src/chain/report-html.ts` (E1 dashboard), `ui-tokens/test/tokens.test.ts` (GT structural test), `scripts/check-icon-manifest.mjs` (icon set = GT `landmark.iconNames`, read from `tokens.grand-teint.json`).
- **Version — platform 1.0.0 (lockstep, MAJOR), machine-forced.** `check-export-maps.mjs` asserts every package version = root version, so a ui-tokens MAJOR is a **platform** MAJOR; from 0.9.10 the only MAJOR is **1.0.0**. All six package.json + intra-deps + `run-gates.sh --pinned-version` + `docs.manifest.json` (both) + api-surface snapshot packageVersion → 1.0.0; lockfile re-resolved. Design-system version (`$meta`) is the separate line: 1.0.0 Grand Teint → 2.0.0 Faso Premium.
- **Snapshot delta (named):** api-surface snapshot = `packageVersion 0.9.10→1.0.0` **ONLY** (`exports` + `schemas` byte-identical, compared structurally on the real keys — no vacuous key-miss; no contracts shape moved). docs.manifest = `packageVersion` only (11 doc hashes unchanged). ui-tokens surface: ADDED the ten FP root exports; MOVED the GT set to `./legacy` (unchanged bytes).
- **Evidence (my runs):** `ALL GATES GREEN` (full `run-gates.sh`) — token-fidelity 27 groups × 2 surfaces deep-equal; token-coverage 33 exports × 2 surfaces each owned by one gate; design-dimensions 11 warranted; icon-manifest 29 icons; every negative fires (fidelity FP `#A31D4E→#A31D4F`; coverage stray export `icon` + stray leaf `motion.fpIn.strayLeaf`; design-dimensions bad-value + bad-doc). Suites: ui-tokens 19, contracts 126, certification 20, kernel 5, i18n 15.
- **Cold proof (isolated HOME, `git clone --local` @ `ef2b565`, frozen-install rc 0):** ALL GATES GREEN; log greps its own isolation markers (`_review/WO-FP-0/logs/cold-clean-home-run-gates.log`). Independent value-fidelity cross-check (json ↔ README, not the json==ts gate): **37/37 colours + every numeric + every bezier present in the README, 0 missing.**
- **Fresh-context verifier — VERDICT: PASS, BLOCKING: none** (`_review/WO-FP-0/logs/verifier-verdict.md`). No build memory. Independently: every leaf traced to the README (derive-never-invent holds; `body` correctly weightless; two warn tones both encoded); rulings ②③④ confirmed against the prototype grep (keyword timings NOT fabricated beziers; fpPop {300,450}+curve; 7 fp*, fpFade/fpToast out); `.ts` deep-equals json; **GT byte-identical to `f912613`**; gates non-vacuous (coverage negative plants the stray on the **FP** surface, both caught); version lockstep + snapshot delta packageVersion-only; the one runtime consumer repoint is load-bearing. Four flags, all correct-not-defect (letter-spacing transcription documented · weights `[400,700]` endpoint array · header vs `$meta` version-line juxtaposition · shared `#5F4403` hex under two README-stated names).
- **One flag for the CTO (reversible pre-merge):** `titleLetterSpacing` transcribes the README's typographic U+2212 minus to the functional ASCII `-.02em` — value preserved, prose glyph normalized. Override if byte-strict.
- **Pending / next:** the CTO's AMBER verdict. **Do NOT merge (AMBER — merge requires the CTO's paste).** **Four app lanes (boutik-plus · shop-plus · sera · the PWA cliente surface) pin the release-merge sha to adopt Faso Premium; the Séra dispatch console stays on v1 via `@platform/ui-tokens/legacy` this wave.**

## 2026-07-15 · WO-FP-0 · done (CTO review PASS — merged; canon v1.0.0 · Faso Premium 2.0.0)
- **Merged — release-merge sha `f23407c` (THE PIN the FP app lanes fire on; pins ride release merges); impl `ef2b565` byte-preserved in history, closing `0d9522b`.** Faso Premium is the v2 root of `@platform/ui-tokens`; Grand Teint v1 preserved byte-verbatim at `@platform/ui-tokens/legacy` for the transition window.
- **Sha correction (journaled):** the impl is **`ef2b565`**, not `a5333e9`. `a5333e9` was the pre-push commit that bundled JOURNAL into the impl; I split it into impl `ef2b565` + closing `0d9522b` per the A/B law before pushing (stated in the in-review report). The CTO's guard list carried the earlier `a5333e9` from my first status note; all guards were re-run against the real candidate.
- **Merge guards (my runs, all green):** heads agree (`0d9522b`) · `f912613` ancestor · 2 ahead / **0 behind** main · closing delta = JOURNAL + packet zip only · four GT files **IDENTICAL** to `f912613`. Anchors on the candidate: `check-token-fidelity` exit 0 (27 groups × 2 surfaces) · coverage negative fires on **both** planted strays (`icon` export + `motion.fpIn.strayLeaf`, on the **FP** surface) · all six packages 1.0.0 · tokens.json `$meta` 2.0.0 · `@platform/ui-tokens/legacy` importable (dist import + the 15-step chain dashboard renders through `report-html → /legacy`, rc 0) · api-surface delta = **packageVersion-only** on the real keys (exports+schemas identical) · 0 ssh-form. **Post-merge on main: ALL GATES GREEN.**
- **THE PLATFORM CROSSED 1.0.0** (lockstep MAJOR, machine-forced by `check-export-maps`): all six packages + intra-deps + manifests + pinned drift version + api-surface packageVersion. Design-system version is the separate line: Grand Teint 1.0.0 → **Faso Premium 2.0.0**.
- **Consumer note / pins:** `f23407c` is the pin for **WO-FP-BOUTIK · WO-FP-SHOP/PWA · WO-FP-SERA** (the three FP app lanes adopt Faso Premium on it). The Séra dispatch console stays on Grand Teint via `/legacy` this wave; its reskin is a later named slice unless the Séra HANDOFF covers it.
- **Pending / next:** lane idle. Canon's redesign work is **done** unless an app lane surfaces a token gap.
