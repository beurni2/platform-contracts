# JOURNAL — platform-contracts
Continuity ledger per CTO charter §6/§6bis. Every entry is evidence-grounded.

Format per entry:
## <date> · <slice/WO id> · <status: in-progress | in-review | done | blocked-on-founder>
- What was done (with the tool result / test output that proves it)
- Decisions made · safest-defaults applied on open ⏳ (flagged) · founder overrides
- Pending / next

---

## 2026-07-09 · E0 bootstrap · done
- Arranged the founder-supplied kickoff set: `e0-kickoff/docs/` → `/docs` (7 canon documents), `CLAUDE.md`/`AGENTS.md`/`JOURNAL.md` → repo root, four work orders → `/WORK-ORDERS/`, `e0-kickoff/` (incl. its README) deleted. Commit `f72dba5` "chore(e0): bootstrap canon, charter, work orders", pushed to `main`.
- Canon completeness check: Execution Contract + 3 Build Specs + 3 Building Plans all present in `/docs`. North Stars + prototype not yet supplied — expected per WO-0 §B6 ("…when the founder supplies them"); the manifest covers the seven present documents.

## 2026-07-09 · Founder Decision — RoundingLaw v1 · CLOSED ✅
- **Ruling (founder, 2026-07-09):** RoundingLaw v1 CONFIRMED; the WO-0 §B2 ⏳ and its PENDING_FOUNDER_DECISION block are lifted. The law: `sellerPlatformFee = floor(0.05 × B)` · `resellerPlatformFee = floor(0.20 × (C + M))` · `sellerNet = B − C − sellerPlatformFee` · `resellerNet = (C + M) − resellerPlatformFee` · `platformProductFeeRevenue = sellerPlatformFee + resellerPlatformFee`. All Quote money fields integer FCFA; rounding exists ONLY on the two fees; nets are defined by subtraction, never independent multiplications.
- Applied as the single named module `packages/contracts/src/money/rounding-law.ts` (`ROUNDING_LAW_VERSION = 'v1'`) — no marker, no skipped tests. Fees computed in exact integer arithmetic (no floating-point money).
- Suite contains, all passing: the reconciliation property tests (fast-check, 2,000 runs each, both identities exact ∀ integer FCFA inputs in realistic ranges) · the §5.4 worked baseline asserted literally (10,000/1,000/1,500/1,000 → 11,500 · 12,500 · 8,500 · 2,000 · 1,000) · the founder's fixed non-divisible regression asserted exactly (B=10,001 · C=333 · M=778 · D=600 → fees 500 · 222, sellerNet 9,168, resellerNet 889, platform 722, productSubtotal 10,779, buyerTotal 11,379).

## 2026-07-09 · WO-0 · in-review (built + verified; tag/merge reserved to the founder's reviewer)
- **Built on branch `e0/wo-0`**, small conventional commits:
  - B1: pnpm + Turborepo + TypeScript-strict workspace; packages `@platform/contracts` · `@platform/kernel-types` · `@platform/i18n` · `@platform/ui-tokens`, all versioned 0.1.0.
  - B2: E1-critical §5.6 shapes as TS types + zod strict schemas (frozen Quote verbatim — `supplyMode`/`handlingClass`/`kittingSealId` are a tested parse failure on the Quote); four secrets as branded, non-assignable types; pure money functions (`computeWaterfall`, `assertQuoteReconciles`, RoundingLaw v1); byte-stable canonical JSON; event envelope + E1 event-name union with all `packlab.*`/`cercle.*`/`campaign.*`/`referral.*`/`review.*` names excluded.
  - B3: kernel types (phone-alias identity, no-address Location — rejects `streetAddress`, offline queued-=-pending semantics, media refs). Types + schemas only, no services.
  - B4: i18n catalog schema (`register` + `screenClass` on every entry), copy-lint CLI implementing exactly the four §10.5 failure conditions with token lists/budgets as maintained data files; seed catalog = the eight canonical Shop+ §6.1 checkout strings (register: money); negative fixture catalog.
  - B5: ui-tokens — color/type/spacing/radius/elevation/motion, one family DNA, three themes (`boutik-plus` supply-green · `shop-plus` warm commerce · `sera` road-and-custody), shared verified-badge color; tokens only, no components.
  - B6: `docs.manifest.json` (sha256 × 7 docs + packageVersion 0.1.0, root + package copy) + `drift-check` CLI shipped in `@platform/contracts`.
  - B7: CI workflow `.github/workflows/ci.yml` + `scripts/run-gates.sh`.
- **Evidence (tool outputs in this session; copies in `_review/WO-0-review.zip` → `logs/`):**
  - `pnpm build` 4/4 green · `pnpm typecheck` 5/5 green (incl. secret-separation type tests) · **60/60 tests passing** with assertions visible (verbose output captured).
  - Every gate shown failing once on its negative fixture: copy-lint fixture with « Veuillez patienter » + « séquestre » fails with all four conditions reported (exit 1) · tampered consumer doc fails drift-check (exit 1) · pristine copy passes · secret-substitution fixture fails tsc (TS2322 both ways) · independent-multiplication quote throws QuoteReconciliationError (10,778 ≠ 10,779) · tampered snapshot (kittingSealId on Quote) fails shape-freeze.
- **Safest defaults applied (maintained data, not canon):** copy-lint token lists (administrative-formal set beyond the canonical six; marketing/urgency; finance jargon; Mooré/Dioula) and reading-level budgets are data files seeded so the canonical §6.1 strings pass; screen-class list is a lint parameter. All tunable without code changes.
- **Canon nit surfaced to the founder (no action taken):** `EvidenceBundle` carries `coarseLocation` in the Séra spec §5.6 but not in Boutik+/Shop+ §5.6 — modeled as optional here; the specs should be reconciled at source.
- **Founder overrides recorded:** WO-0 §B8/DoD calls for the `v0.1.0` git tag, but the founder's session instruction reserves tagging and merging to his reviewer — versions are set to 0.1.0 and the tag is intentionally NOT created. Ecosystem-Engineering session ruling: RoundingLaw v1 confirmed (see Decision entry above), so the §B2 PENDING_FOUNDER_DECISION path was never entered.
- **Fresh-context verifier (mandatory for money/contracts paths, charter §6bis): VERDICT PASS, zero blocking findings.** The verifier — given only the SPEC AUTHORITY quotes, the founder RoundingLaw ruling, the full `main...e0/wo-0` diff, and the DoD — independently re-ran build/typecheck/tests/gates with caches bypassed (60/60 at the time, 0 skipped), read the actual assertions, confirmed the frozen Quote is field-exact §5.6 with gated fields a tested parse failure, confirmed no float money and no independent-multiplication nets anywhere in src, confirmed the event union carries zero gated names, and confirmed nothing OUT-OF-SCOPE was built. Findings: 1 NON-BLOCKING (a same-version snapshot regeneration would pass CI — freeze-with-version-bump was convention, not machine) + 7 NOTEs.
- **Fixes applied after the verdict (commit `5a5c191`), re-verified:** (1) new CI gate `scripts/check-snapshot-version-bump.sh` — a changed API-surface snapshot without a `@platform/contracts` version bump now fails CI (initial addition allowed); demonstrated failing once on a tampered snapshot (exit 1, output in evidence). (2) copy-lint condition (b) now scans `moore`/`dioula` variant texts, not only `fr` (+ regression test). (3) gate runner uses `mktemp` for the drift-consumer fixture (no fixed /tmp path). Post-fix full re-run: build green, gates ALL GREEN, **61/61 tests passing** (verbose output in evidence).
- **Verifier re-check of the delta: RE-VERDICT PASS.** All four branches of the new snapshot-version gate exercised live in a scratch clone (tamper-without-bump fails exit 1, tamper-with-bump passes, missing base skips, unchanged passes); lint variant coverage confirmed with the regression test; 61/61 tests and ALL GATES GREEN reproduced with caches bypassed; JOURNAL wording checked for overclaim — none found. Remaining findings: 3 cosmetic NOTEs (working-tree version read in dirty local trees, un-cleaned mktemp dir, evidence archive location).
- **Review packet:** `_review/WO-0-review.zip` — full `main...e0/wo-0` diff as `diff.txt`, all test/CI/gate outputs incl. every negative-fixture failure and both verifier verdicts under `logs/`, complete source tree under `source/` (node_modules and .git excluded).
- **Pending / next:** founder's reviewer verdict on `e0/wo-0` (merge + tag `v0.1.0`); then WO-B0.1 / WO-SP0.1 / WO-SE0.1 pin the tag.

## 2026-07-09 · Conduct (founder correction, binding)
- Every number in any report to the founder must be copyable from a log in the evidence set; adopted permanently — reports are now written against the logs, not from memory.

## 2026-07-09 · WO-0 · done (founder review PASS) + WO-0B consumability · done
- **Founder review verdict on `e0/wo-0`: PASS.** Merged into `main` with merge commit `24d400a` ("merge(e0): WO-0 platform-contracts v0.1.0 — founder review passed"), pushed.
- **Consumability gap closed (blocked the tag):** `dist/` was gitignored and no outside consumer had ever installed the packages. Mechanism chosen: **per-package `prepare` builds + pnpm git-dependency `path:` syntax** (commits `41d7bfd`, `5bf4a4a`) — each package builds from source on install; `@platform/contracts`'s dep on kernel-types changed `workspace:*` → `0.1.0` (workspace protocol does not resolve for outside consumers — verified failing: `ERR_PNPM_WORKSPACE_PKG_NOT_FOUND`) with `linkWorkspacePackages: true` keeping local sibling links (lockfile: `specifier: 0.1.0, version: link:../kernel-types`). Consumers need two `pnpm-workspace.yaml` blocks: `onlyBuiltDependencies` (pnpm 10 blocks dependency build scripts by default) and an `overrides` entry routing transitive `@platform/kernel-types@0.1.0` to the same git ref. Exact install line (app repos): `pnpm add "git+https://github.com/beurni2/platform-contracts.git#v0.1.0&path:packages/contracts"` (same pattern for the other three) — full recipe in `/CONSUMING.md`.
- **Acceptance evidence (`consumer-proof.txt` in the WO-0B packet):** from a scratch directory outside the repo, all four packages installed pinned to main sha `5bf4a4a`; `baseline-check.mjs` imported `computeWaterfall` + `assertQuoteReconciles` and asserted the §5.4 baseline — output: productSubtotal 11500 · buyerTotal 12500 · sellerNet 8500 · resellerNet 2000 · platformProductFeeRevenue 1000 · "assertQuoteReconciles: no throw" · OK; `imports-check.mjs` resolved and imported kernel-types, i18n, and ui-tokens from the same ref. Workspace re-verified after the change: 61/61 tests passing.
- **Tag `v0.1.0`: created, push BLOCKED by the environment (blocked-on-founder/infra).** Annotated tag cut on the final main commit (`git rev-parse v0.1.0` → `5a6813f`, `v0.1.0^{commit}` → `b10f482`). Pushing it fails deterministically — `error: RPC failed; HTTP 403` from the sandbox git proxy on every tag-push variant (3 forms tried with 2s/4s/8s backoff); branch pushes succeed, so this is a tag-ref permission of the proxy, not a network fault. `git ls-remote --tags origin` is empty (full evidence: `tag-status.txt` in the WO-0B packet). The documented `#v0.1.0` install line was proven verbatim against the tag from a scratch consumer (`consumer-tag-proof.txt`: §5.4 baseline OK). **Unblock (either):** (a) from any authorized clone: `git fetch origin main && git tag -a v0.1.0 -m "platform-contracts v0.1.0 — E0 canon package set (WO-0, founder review passed)" b10f482 && git push origin v0.1.0`; or (b) grant tag-push on the proxy and I push the existing tag object. Until then app repos pin sha `b10f482` (interim note in `/CONSUMING.md`).

## 2026-07-09 · WO-0C · platform-contracts v0.2.0 — in-review (built + verified on `canon/v0.2.0`; merge and tag are the founder's, not mine)
- No failures to report: every suite, every gate, and the scratch-consumer E2E are green; the fresh-context verifier's verdict is attached below the evidence bullets.
- **1. RN-safe entrypoints (the one structural change).** Each package's `exports` `"."` entry now exposes only shapes, schemas, pure functions, tokens, and catalog data. `@platform/contracts` root no longer re-exports the drift-check module (node:crypto/fs/path); it moved to subpaths `@platform/contracts/drift-check` (lib) and `@platform/contracts/drift-cli` (CLI module), bin `drift-check` unchanged. `@platform/i18n` root no longer re-exports the data loader (node:fs/path/url); it moved to `@platform/i18n/data-loader` and `@platform/i18n/lint-cli`, bin `copy-lint` unchanged, plus `@platform/i18n/catalog.json` exposing the seed catalog as importable JSON data. Additive for existing consumers: all three app repos import only root entries or type-only imports (verified by grep over their `origin/main`; the CLIs are consumed via `pnpm exec` bins only), and every package carries a `./dist/*` passthrough so any legacy deep import keeps resolving.
- **2. Gate.** New scanner `scripts/scan-rn-safe-entry.mjs` walks each `"."` static import graph, follows `@platform/*` siblings, and fails on ANY node builtin (full `builtinModules` set, `node:`-prefixed or bare) or unvetted external (allowlist: `zod`). Pass output (log-copied): contracts 19 files · kernel-types 5 · i18n 3 · ui-tokens 3, all "no node builtins", externals `[zod]` or none. Negative fixture `scripts/show-rn-safe-negative.sh`: hardlinked copy of the built contracts package (`cp -al`), planted `import { readFileSync } from "node:fs"` into `dist/enums.js` on the copy (rm-before-write so the shared inode is untouched, verified pristine afterwards) — scanner output "✗ @platform/contracts: root entry graph is NOT RN-safe … node builtin 'node:fs' imported", exit rc=1. Both wired into `scripts/run-gates.sh` alongside a new export-map gate (`scripts/check-export-maps.mjs`: every subpath target exists post-build, node-only subpaths present, versions 0.2.0, `./dist/*` present).
- **3. CONSUMING.md.** Added the CI-ONLY auth step verbatim from the deployed app-repo workflows (read from `boutik-plus` `origin/main:.github/workflows/ci.yml` lines 19–20, identical in shop-plus): the `git config --global url."https://x-access-token:${{ secrets.PLATFORM_CONTRACTS_READ_TOKEN }}@github.com/beurni2/platform-contracts".insteadOf` rewrite before `pnpm install`, requiring the fine-grained read-only PAT secret per app repo, ephemeral runner config only. All install lines now pin `#v0.2.0`; the interim-sha note is replaced by the tag process (founder cuts tags in the GitHub Releases UI at the reviewed final sha — sandbox tag pushes are 403, known). New section documents the RN-safe roots + node-only subpaths and that RN apps may now import canon directly (type-only workaround optional).
- **4. Versions.** All four packages + root → 0.2.0; `@platform/contracts` dep on kernel-types → `0.2.0`; `docs.manifest.json` regenerated (packageVersion 0.2.0, 7 docs, root + package copies identical); run-gates `--pinned-version` → 0.2.0. **Shape-freeze snapshot updated deliberately in this PR** — snapshot packageVersion 0.2.0, 91 exports (was 93: `checkDocsDrift` and `sha256Hex` left the root surface), and the version-bump gate confirms: "check-snapshot-version-bump: snapshot changed with version bump 0.1.0 -> 0.2.0 — OK". Untouched suites stayed green untouched: reconciliation, secrets, copy-lint, no-gated-shapes — full run 42+9+5+5 = 61/61 tests, 0 skipped; `run-gates.sh` → "ALL GATES GREEN (positive suites pass; every negative fixture demonstrably fails)".
- **Scratch-consumer E2E (outside the repo, pinned to sha `d1bc628`):** all four packages installed at 0.2.0; root imports asserted the §5.4 baseline (11500 · 12500 · 8500 · 2000 · 1000, "assertQuoteReconciles" no throw); subpaths `@platform/contracts/drift-check`, `@platform/i18n/data-loader`, and `@platform/i18n/catalog.json` resolved and worked (seed catalog: 8 entries, 0 violations); output in the packet (`logs/consumer-e2e-proof.txt`).
- **5. Deferred, with reasons (OUT OF SCOPE this release):** (a) North Stars + prototype into `/docs` — founder has not supplied the files; they join the manifest at v0.3.0. (b) `PERF-BUDGETS.md` — Decision D17 silent; budgets are named at E0 by founder numbers, not invented ones. (c) copy-lint label-class rule — Decision D18 silent; the lint's screen-class set stays as maintained data until ruled. (d) `EvidenceBundle.coarseLocation` spec-text alignment — D19: founder edits the spec at source; the schema keeps the optional-field bridge until the canon text changes.
- **Fresh-context verifier: VERDICT PASS** (report attached verbatim in the packet, `logs/verifier-verdict.txt`). It independently reproduced every figure (61/61 tests · scanner counts 19/5/3/3 · snapshot 93→91 exports, removed set exactly `{checkDocsDrift, sha256Hex}` · consumer install at 0.2.0 with the §5.4 baseline exact) and confirmed the CONSUMING.md auth step byte-identical to the deployed boutik-plus workflow. Findings: 1 NON-BLOCKING (the scanner mapped `@platform/pkg/subpath` imports to the sibling's ROOT entry — a future node-only subpath import could have evaded the gate; no such import exists today) + 3 NOTEs (packet must be committed before merge — done in the packet commit that closes this entry, after both verifier passes; a missing dist could have made the fixture "fail" vacuously; app repos still pin `#v0.1.0`, which is expected — they move in their own PR). Transparency: the verifier's first run was terminated mid-verification by a platform credit limit and was resumed to completion; both the partial and final outputs are preserved in the verdict file.
- **Post-verdict hardening (commit `9e855b6`), re-verified:** scanner now resolves `@platform/*` subpaths through the sibling's real exports map (wildcards included); the negative fixture gains scenario 2 — planted `import "@platform/i18n/data-loader"` in a hardlinked root graph → scanner surfaces `node:fs/promises`, `node:path`, `node:url` from `data-loader.js`, rc=1 — plus a missing-dist guard (exit 0 = undemonstrated, never counted as the expected failure). Full re-run at final sha `9e855b6`: "ALL GATES GREEN", 61/61 tests, snapshot-version-bump gate "0.1.0 -> 0.2.0 — OK", scratch-consumer E2E re-pinned to `9e855b6` green (all root entries + subpaths + catalog.json).
- **Verifier delta re-check: RE-VERDICT PASS** (verbatim in the packet, `logs/verifier-re-verdict.txt`). It re-ran everything at the delta tip (scanner 19/5/3/3 rc=0 · 61/61 tests · all 12 gates green · both fixture scenarios rc=1 with the real dist pristine · missing-dist guard exercised by parking dist: "BUG: … build before running the fixture", exit 0 · its own wildcard probe caught with the same three builtins · consumer E2E re-pinned green). Findings: 1 NON-BLOCKING — a JOURNAL clause here claimed the packet was already committed while it was still anticipatory; the wording is corrected above and the packet commit closes this entry — plus 2 NOTEs (a CI-side wildcard fixture scenario is optional, the verifier probed that branch functional itself; the credit-limit interruption disclosure is consistent but not independently verifiable by it).
- **Conduct:** every figure above is copied from a log in `_review/WO-0C-review.zip`; the verifier verdict is attached verbatim in the packet and below.
- **Founder review verdict: PASS.** Guard re-issued after the first guard's self-contradiction (it demanded both the pre-packet head `ad6253c` and the packet in-tree, which cannot both hold) was caught and reported rather than forced through. Re-issued guard verified empirically before merge: `canon/v0.2.0` head `933aa0d`, log contains `ad6253c`/`9e855b6`/`d1bc628` · `git diff --stat ad6253c..933aa0d` touches only `_review/WO-0C-review.zip` (Bin 0 -> 52970 bytes, the packet commit and nothing else) · `WO-0C-review.zip` tracked at head · scanner exit 0 (19/5/3/3 files, no node builtins). **Merged into `main`: `558e007..efabb77`** ("merge(canon): v0.2.0 — RN-safe entrypoints, builtin scanner, CONSUMING v0.2.0 — founder review passed"), pushed. `git ls-remote --heads origin`: `main` → `efabb7726e8c58df6730d3fe8d7fbc82a31adbe9`. WO-0C status: **done**.
- **Tag `v0.2.0`:** founder creates it at the merge commit via GitHub Releases (sandbox tag pushes 403, known — see the WO-0/WO-0B precedent above). Consumers bump per `CONSUMING.md`: shop-plus's WO-1.1 pre-flight self-adjusts; sera and boutik-plus bump inside their first E1 slices.

## 2026-07-10 · WO-1.0 · platform-contracts v0.3.0 — in-review (built + verified on `e1/wo-1.0`; do NOT merge — founder's reviewer decides)
- No failures to report: 87/87 tests (48 contracts + 9 i18n + 5 kernel-types + 5 ui-tokens + 20 certification, 0 skipped), all gates green, every negative fixture demonstrably fails; the verifier verdict is attached below, verbatim, and ships in the packet committed by the closing commit of this entry.
- **1. `@platform/certification` (new package, node tooling — README states it must never enter an app's runtime graph; exempt from the RN-safe roots by design, tracked by the export-map gate).** (a) The §3 suite: a standard `MockAdapter` interface per E1 domain (payment-provider · eligibility · supply-projection · readiness) and `certifyAdapter` running the eight §3 behaviors as executable checks with a per-behavior scorecard — certification is 8/8, no partial passes. (b) Envelope conformance: every event must parse via the pinned `PlatformEventSchema` and carry `{command_id, correlation_id, aggregateVersion, actor, serverTime, version}`. (c) The §2.3 fifteen-step chain runner over pluggable `ChainAdapters`, validating ONE correlation chain across the nine §6 ids, the step-6 Quote against `QuoteSchema` + `assertQuoteReconciles`, and the step-8 Order against the five-state enum; emits text + static-HTML chain report (the E1 dashboard seed). (d) Four bundled §3-complete reference adapters self-test the suite and runner end-to-end; the chain reference adapters compose them.
- **2. Canon addition (the deliberate part of this bump):** `ORDER_STATUSES = quote_issued | reserved | payment_pending | paid | confirmed` and `OrderSchema.status` tightened from bare string to the enum (WO-1.1 verifier NB-6; §2.2 single-definition). Terminal/failure states are E2's and were NOT added. Snapshot updated deliberately: packageVersion 0.3.0, 93 exports (was 91: + `ORDER_STATUSES`, `OrderStatusSchema`); gate output: "check-snapshot-version-bump: snapshot changed with version bump 0.2.0 -> 0.3.0 — OK". Versions 0.3.0 across root + all five packages; docs manifest regenerated (7 docs, packageVersion 0.3.0).
- **Evidence (all in `_review/WO-1.0-review.zip` → `logs/`, committed by the closing commit of this entry):** reference adapters certify **4/4 domains, 8/8 behaviors each** (scorecards in `certification-positive.txt`) · deficient mock (duplicates behavior removed) **REFUSED, 7/8, exit 1** · envelope negative (missing correlation_id) fails exit 1 · chain runner: **"15-STEP CHAIN COMPLETE — correlation corr_e1demo — 27 events"**, all nine ids minted and distinct (`quote_e1demo → rsv_e1demo → payatt_e1demo → order_e1demo → pkg_e1demo → dtask_e1demo → val_e1demo → sob_supplier_e1demo → payout_e1demo`), `chain-report.html` generated · broken-link negative (validation_id dropped) fails exit 1 · order-status negative: `'shipped'` refused with `expected one of "quote_issued"|"reserved"|"payment_pending"|"paid"|"confirmed"`, exit 1 · all pre-existing suites green untouched (money, secrets, copy-lint, RN-safe 19/5/3/3, shape-freeze, drift) · typecheck 8/8 tasks successful (uncached; the 6/6 figure first written here was stale — corrected per the verifier's finding 2).
- **Flake caught and fixed during build (evidence-grounded):** the delay-behavior check intermittently failed under parallel turbo load — Node timers measured with `Date.now()` can quantize ~1ms "early" (observed 39ms for a 40ms sleep). Fixed with a documented 2ms clock-quantization tolerance (a deficient mock observes ~0ms, so the tolerance cannot mask a missing behavior); full suite then ran **6/6 consecutive uncached passes**.
- **3. Deferred founder items, re-recorded at v0.3.0 (unchanged, awaiting founder inputs):** (a) North Stars + prototype into `/docs` — files still not supplied; join the manifest at their arrival. (b) `PERF-BUDGETS.md` — D17 silent; device budgets are founder numbers, not invented ones. (c) copy-lint label-class rule — D18 silent; screen-class set remains maintained lint data. (d) `EvidenceBundle.coarseLocation` spec-text alignment — D19: founder edits the canon at source; the optional-field bridge stays until then.
- Not merged. After founder review, main advances and the founder cuts `v0.3.0` via GitHub Releases at the final sha (sandbox tag pushes 403, known).
- **Fresh-context verifier: VERDICT PASS** (report verbatim in the packet, `logs/verifier-verdict.txt`). It re-ran every DoD item itself, uncached: 4/4 domains at 8/8 with scorecards observed · deficient mock "NOT CERTIFIED — payment-provider — 7/8", only `emits_duplicates` ✗, exit 1 · chain "15-STEP CHAIN COMPLETE — corr_e1demo — 27 events" with the nine ids pairwise distinct and the HTML verdict "CHAÎNE COMPLÈTE" · snapshot delta independently confirmed (91→93, added exactly `ORDER_STATUSES` + `OrderStatusSchema`) · its own probes beyond the shipped tests: a 1-FCFA buyerTotal tamper at step 6 → `QuoteReconciliationError` flagged; `status:'shipped'` at step 8 → parse failure flagged; deficient delay observes 0ms vs the 38ms threshold, so the 2ms tolerance cannot mask · no network calls in the package · 87/87 tests, "Tasks: 8 successful, 8 total" typecheck. Findings, titles quoted verbatim: 1 **NON-BLOCKING (fix before merge)** — "JOURNAL.md:71,74 claims 'the verifier verdict is attached below and in the packet, verbatim' … but `_review/` contains only WO-0/WO-0B/WO-0C zips — no WO-1.0-review.zip exists at HEAD"; 2 **NON-BLOCKING** — "JOURNAL.md:74 'typecheck 6/6 clean' is stale … 'Tasks: 8 successful, 8 total'"; 3 **NOTE** — "the delay check measures the adapter's self-reported `deliveredAt`… acceptable for cooperative mocks/live siblings under §3"; 4 **NOTE** — "without `--out`, the runner writes `chain-report.html` into the cwd… Cosmetic"; 5 **NOTE** — "pairwise-distinctness covers exactly the nine §6 chain ids (what the Contract requires)".
- **Findings resolved before merge (docs-only delta, no code touched):** finding 1 — the two anticipatory clauses are reworded above to name the closing packet commit, and the packet (with this verdict inside) is committed by that closing commit, per the WO-0C precedent; finding 2 — the stale "6/6" corrected to the log-copyable "8/8 tasks successful". NOTEs 3–5 acknowledged, no action (cooperative-adapter measurement is the §3 certification model; the gate always passes `--out`; distinctness scope is the Contract's).

## 2026-07-10 · Conduct (founder correction #2, binding, all repos)
- Identity headers are command-generated: the final message shows `git rev-parse --short HEAD` and its output together — a typed sha is prose, and prose is not evidence. (Root cause of the 4c1f2ae guard failure: a typed header sha, not command output. The self-anchoring guard pattern replaces named-sha guards.)

## 2026-07-10 · WO-1.0 · done (founder review PASS — merged)
- Self-anchoring guards verified empirically before merge: local `e1/wo-1.0` head equal to origin's (`2f4e9e657a616cc997c70a45dd7675f932a0003b` both sides, ls-remote log-copied) · log contains `50e1bc5`/`3c64cd8`/`506ba7a`/`47406e1` · `git diff --stat 50e1bc5..HEAD` touches only `_review/WO-1.0-review.zip` (Bin 0 -> 71455 bytes) · `run-gates.sh` at head exit 0, "ALL GATES GREEN".
- **Merged into `main`: `b076ff3..9308c64`** ("merge(e1): WO-1.0 certification suite + 15-step chain runner + order-status canon (v0.3.0) — founder review passed"), pushed; `git rev-parse HEAD` → `9308c641c368ea56d141867d667b122e10db682d`.
- **Release ref = the merge-commit sha `9308c64`** (pins use the sha, per the founder tag ruling). The founder re-cuts lowercase `v0.3.0` at the NEW main after this merge — the prior `V0.3.0`/`BOSSY`/`boss` tags are founder artifacts being deleted (report-only). Observed during the guard fetch, report-only: a lowercase `v0.3.0` also appeared on origin pointing at the PRE-merge main `b076ff3` (v0.2.0-era content) — it needs the same re-cut at `9308c64`.
- App repos converge on this sha at their next E1 slices; certification of shop-plus's in-repo WO-1.1 mocks happens at E1 assembly, per the work order.

## 2026-07-10 · Founder Decisions D17 + D18 · CLOSED ✅ (founder-signed 2026-07-10)
- **D17 (performance & device budgets):** reference profile = Android Go class, 2GB RAM (Tecno Spark Go / Itel A-series, Android 12–13 Go), 3G-class network (1.5 Mbps, 300ms RTT), 360×800. Signed budgets: cold start < 5s · touch-to-feedback < 100ms · touch-to-result < 250ms · no frozen scroll > 200ms · initial PWA payload < 300KB compressed · queued offline actions survive app-kill and reboot · French long-text never truncates meaning. Landed as `/docs/PERF-BUDGETS.md` with the Contract §7.3 named list completed by conservative values, each marked "CTO-derived — tune at E4 telemetry".
- **D18 (copy-lint label rule):** reading-level budgets apply only to sentences of four or more words; label screen-classes are checked for banned tokens + register only. Landed as lint rule + data (`minWordsForBudget: 4`, `labelClasses: ["label"]`, new `label` screen class).

## 2026-07-10 · WO-0D · platform-contracts v0.4.0 — in-review (built + verified on `canon/v0.4.0`; do NOT merge — founder's reviewer decides)
- No failures to report: 97/97 tests (53 contracts + 14 i18n + 5 kernel-types + 5 ui-tokens + 20 certification, 0 skipped), "ALL GATES GREEN (positive suites pass; every negative fixture demonstrably fails)", typecheck clean; the verifier verdict is attached below and ships in the packet committed by the closing commit of this entry.
- **1. PERF-BUDGETS.md** committed to `/docs`; manifest regenerated — 8 docs, packageVersion 0.4.0, `PERF-BUDGETS.md` sha256 `bffcb5328f994540c97a400c7494d2947c8304f287db8c8809fa94be8546b358` present in `docs.manifest.json`.
- **2. D18 lint rule** implemented as rule + data; tests log-copied: « Espace revendeur » PASSES as label · « Repère » PASSES as label (the collision instance) · a 5+-word administrative sentence still FAILS the budget on a non-label class · « séquestre » in a label still FAILS the banned-token check · a sub-4-word high-syllable sentence is budget-exempt even outside labels. The pre-existing negative fixture still fails with all four §10.5 conditions.
- **3. Supply-projection contract promoted** into `@platform/contracts` as `SupplyProjectionSchema` (strict; §2.2 single definition; B4.2/SP-I03 — no supplier identity/contact/pickup fields); `@platform/certification` re-imports it (`SupplyProjectionEventPayloadSchema` aliased for compatibility; the behavior-8 identity check now anchors on the canon schema). Negative gate log-copied: `supplierName`/`supplierPhone`/`pickupAddress` → `unrecognized_keys`, exit 1. Positive parses in tests (4 leak variants refused).
- **4. CONSUMING.md**: overrides section now the ≥0.3.0 full block — `@platform/contracts`, `@platform/kernel-types`, AND `@platform/ui-tokens` all routed to the pinned ref (certification's version-deps); install lines reference `<RELEASE_SHA>` with the current worked example (v0.3.0 release = `9308c641c368ea56d141867d667b122e10db682d`); `@platform/certification` added to the consumer build-script allowlist.
- **5. Versions 0.4.0** across root + all five packages; deliberate snapshot bump — 94 exports (was 93: + `SupplyProjectionSchema`), gate output: "check-snapshot-version-bump: snapshot changed with version bump 0.3.0 -> 0.4.0 — OK".
- **Deferred, re-recorded at v0.4.0:** (a) North Stars + prototype into `/docs` — founder files still not supplied. (b) D19 `EvidenceBundle.coarseLocation` spec-text alignment — founder edits the canon at source; the optional-field bridge stays until then. (D17 and D18 leave the deferral list — closed above.)
- Not merged. After founder review, main advances; the release ref is the merge-commit sha per the standing tag ruling.

### WO-0D verifier round (continues the in-review entry above)
- **Fresh-context verifier: VERDICT PASS at `a968d0b`** (brief = the WO-0D order verbatim + §2.2/§7.3/§3 quotes + the full diff; no build-thread context). Finding titles quoted verbatim (standing rule): 1 "DIFF AUTHENTIC AND FULLY IN SCOPE" (NOTE) · 2 "FORBIDDEN LIST CLEAN" (NOTE) · 3 "D18 IMPLEMENTED EXACTLY" (NOTE — verifier probed boundaries beyond the shipped tests: 4-word boundary binds, 3-word exempt everywhere, conditions a/b/d still fire on labels) · 4 "§2.2 SINGLE DEFINITION HOLDS" (NOTE — alias is reference-identical; behavior-8 anchors on the canon object) · 5 "SNAPSHOT DELTA IS EXACTLY THE PROMOTION" (NOTE) · 6 "LABEL EXEMPTION FOR ≥4-WORD LABEL STRINGS HAS NO TEST" (**MINOR**) · 7 "JOURNAL FORWARD-REFERENCES A VERDICT NOT YET IN THE DIFF" (NOTE) · 8 "STALE ILLUSTRATIVE EXAMPLE IN CONSUMING.md" (NOTE) · 9 "VERSION COHERENCE COMPLETE" (NOTE).
- **Finding 6 fixed in `804b65b`** — one committed test: « Coordonnées géolocalisées vérification communautaire » (4 words, avg 4.50 syllables/word by the shipped estimators — the min-words clause cannot exempt it) PASSES as `label` AND FAILS `reading_level` on `status`. i18n suite now 15 tests; workspace total **98/98** (53 contracts + 15 i18n + 5 kernel-types + 5 ui-tokens + 20 certification — `tests-after-finding6.txt`, log-copied). **Verifier delta re-check: "DELTA VERDICT — CONFIRMED FIXED"** at `804b65b`; its stat check confirms the commit touches only `packages/i18n/test/copy-lint.test.ts` (+13 lines).
- **Finding 7 resolved by this entry's closing commit** — the packet committed there contains the verdict file (initial + delta, verbatim); the forward-looking wording above becomes true at that commit, not before. **Finding 8 deferred, recorded:** the illustrative `@platform/kernel-types@0.2.0` pin in CONSUMING.md's Mechanism section is stale (actual pins 0.4.0) — outside WO-0D scope per the verifier's own reading; queued for the next doc pass.
- The count line in the opening paragraph above ("97/97 tests … 14 i18n") was true when written (pre-`804b65b`); the of-record count after the verifier round is **98/98**, per this addendum.

## 2026-07-10 · WO-0D · done (founder review PASS — merged)
- Self-anchoring guards verified empirically before merge: local `canon/v0.4.0` head equal to origin's (`c487d16a49b870cded579205f1f55614d0bd2936` both sides, ls-remote log-copied) · log contains `965b566`/`804b65b`/`a968d0b`/`b4301d7`/`67591c3`/`efb92c1`/`c9569ef` (7/7) · `git diff --stat 965b566..HEAD` touches only `_review/WO-0D-review.zip` (Bin 0 -> 58272 bytes) · `run-gates.sh` at head exit 0, "ALL GATES GREEN (positive suites pass; every negative fixture demonstrably fails)".
- **Merged into `main`: `4e751c3..be1df7a`** ("merge(canon): v0.4.0 — PERF-BUDGETS (D17), copy-lint label rule (D18), supply-projection promotion — founder review passed"), pushed; `git rev-parse HEAD` → `be1df7ad79a09d43182a638cc8acdd0a9081302e`.
- **Release ref = the merge-commit sha `be1df7a`**; apps converge on it at E1 assembly (sera may re-add « Repère » to its catalogs at that bump — the D18 label rule now admits it). Founder noted the finding-6 catch (label exemption previously unpinned by any committed test) with approval.
- **D17 and D18 leave the open-decisions board.** Remaining founder items: North Star files + prototype into `/docs` · D19 `coarseLocation` spec edit at source · stale CONSUMING.md `@0.2.0` mechanism example at the next doc pass.

## 2026-07-10 · E1 ASSEMBLY PHASE 2 · the fifteen steps LIVE — in-review (built + verified on `e1/assembly`; do NOT merge — founder's reviewer decides)
- No failures to report. The Contract E1 exit is met on live wiring, every figure below log-copied: **`15-STEP CHAIN COMPLETE — correlation corr_e1live — 31 events`, exit 0** (`logs/e1-run.txt`) · nine ids minted and distinct (`quote_e1live → rsv_e1live → payatt_e1live → order_e1live → pkg_e1live → dtask_e1live → val-order_e1live → sob_supplier_e1live → payout_e1live`) · **all four §3 pairs 8/8 mock AND live side by side** (`logs/conformance-pairs.txt`) · dashboard `assembly/chain-report.html` + exit doc `assembly/E1-EXIT.md` committed · live-wiring negative (validation_id dropped) **exit 1** · **cold proof green from committed bytes** at `340026e` (`logs/cold-proof.txt`) · root suites **98/98**, root gates "ALL GATES GREEN", typecheck 8/8 root + assembly clean.
- **Pinned world verified on origin before anything** (`logs/pinned-world.txt`): canon `be1df7a` (ancestor of main, asserted) · sera `77f12bc` · boutik-plus `76ef6aa` · shop-plus `0a8068f` — all three app repos had already converged their canon pins to `be1df7a` at their "assembly phase 1" heads.
- **Consumption mechanism (WO item 1):** `assembly/` is a standalone pnpm workspace consuming 16 app service packages as `git+https…#<pinned-main>&path:` deps. Two app-repo gaps closed ASSEMBLY-SIDE (app repos untouched, per FORBIDDEN): (a) `workspace:*` intra-repo leaf deps → pinned to the same repo+sha by `.pnpmfile.cjs`; (b) no `prepare` scripts + gitignored `dist/` → `scripts/build-consumed.mjs` compiles each consumed package in the pnpm store against `consumed-base-tsconfig.json`, whose sha256 equals all three repos' `tsconfig.base.json` at the pinned shas (`472ba1c6…777d`, all four identical — `logs/consumed-base-tsconfig-provenance.txt`). Lockfile regenerated then normalized to https-form URLs (standing guard; 96 scp-form entries → 0) and frozen-install-proven. Cold-proof round 1 caught the `file:`-linked certification packing only `dist/` → bootstrap order documented + fail-fast check (`340026e`); round 2 green.
- **Live adapters (WO item 2), imports only:** boutik SupplierRegistry/ProductCatalog/buildPremiumFrameAssets/OfferBook/buildSupplyProjection/FulfillmentBook · shop issueQuote (kill-switch consulted FIRST, live)/ImmutableQuoteStore/OrderSpine/LedgerRecords/attribution sign+verify · sera RiderRegistry/ReadyQueue/AssignmentBook/CustodySpine/acceptSellerReadinessEvidence. Both app DOs run under Miniflare/workerd from their COMMITTED bundles (boutik stock, shop reservation) — step 7 is atomic on both sides. Séra's intake projections read REAL cross-repo state (funding ← shop escrow record; readiness ← boutik FulfillmentBook). Payment provider = certified sandbox mock (no real provider, no credentials). Where boutik services return plain outcomes, the adapter wraps REAL outcome data in envelopes — plumbing, not domain logic. Step 4 composes the canon-owned `ResellerListingSchema` document (no listing CRUD exists in any app at E1 — §2.2: the shape's single definition lives in contracts). ADR-002 reading, journaled: "Séra/Boutik+ never import commerce-core" binds the APPS; this harness is the E1 assembly rig, not an app.
- **⚠ FLAGGED FOR FOUNDER — two §3 payload-registry alignments in `@platform/certification` (`78a88c0`), certification-package scaffolding only, `contracts/` and the money waterfall untouched (root snapshot/drift gates unchanged):** (1) **eligibility**: the WO-1.0 scaffold payload carried a SettlementObligation WITH an amount inside a Séra-produced event — the founder-reviewed live producer (WO-1.3 CustodySpine) emits the amount-free SE-I09 signal `{order_id, task_id, validation_id, result, settlement_eligibility}`; certifying live eligibility (WO item 3) against the amount-bearing scaffold was impossible without violating SE-I09/Ten-Laws-2, so the registry now pins the LIVE shape. (2) **payment-provider**: the scaffold's nested `{orderId, paymentAttemptId, leg}` shape is parsed by NO deployed consumer — the founder-reviewed OrderSpine (WO-1.1) parses top-level `amount`/`status`, the shape shop's provider mock emits; the registry now pins the deployed contract. Both reference mocks re-certified 8/8 after alignment; certification tests 20/20; reference chain still 15/15. This is the §3 discipline working as designed — the mocks had drifted from the deployed truth; easy revert if the founder rules otherwise.
- **§3 pairs (WO item 3), each 8/8, log-copied side by side:** supply-projection (LIVE boutik offer-service) · readiness (LIVE boutik fulfillment-service) · eligibility (LIVE sera custody-service — three real spines; evidence resubmission, a real at-least-once behavior, gives the signals distinct aggregate versions) · payment-provider (sandbox composition over shop's MockPaymentProvider — §3 misbehaviors driven through the mock's OWN knobs: webhookCopies, timeoutFirstNInitiates, staleStatusReads, and its real idempotency-key amount-mismatch refusal). Live invalid-transition refusals are REAL service refusals (expired offer, expired readiness challenge, drop-without-validation, amount-mismatch idempotency).
- **Flag service (WO item 4), live:** `assembly/worker/flag-worker.ts` — DO-backed workerd worker, remotely togglable (`GET /flags`, `POST /kill/:switch`), snapshot parsed by shop's own `parseSnapshot`. In-run demo log-copied: kill ON → `issueQuote` → `reason=checkout_killed` (refused closed) → kill OFF → the fifteen steps complete. **Remaining E0 deferral re-recorded: staging-environment isolation — enters at E2.**
- **The run (WO item 5) extras, all in-run and log-copied:** exactly-once eligibility proven live (drop-code replay absorbs with zero events; duplicate injection into shop's OrderSpine absorbs as `duplicate:true`) · obligations recorded COPIED `supplier:supplier-1 8500` / `reseller:rs_e1live 2000` · step-15 reconciliation to the franc (escrow leg 12500 = amountPaidAtCheckout; payouts 8500+2000 = sellerNet+resellerNet) · step-6 Quote validated live by `assertQuoteReconciles`, step-8 Order on the canon five-state enum · drop code LAST (step 13).
- The fresh-context verifier verdict is attached below and ships in the packet committed by the closing commit of this entry. Not merged; after founder review, main advances and the three app repos take up E2.

### E1 assembly verifier round (continues the in-review entry above)
- **Fresh-context verifier: VERDICT PASS at `cf0dd3f`** — it re-established the cold world itself (wiped node_modules/dist/caches, README bootstrap), re-ran every DoD item, and executed the founder's tamper mandate. Finding titles quoted verbatim (standing rule): 1 "PINNED WORLD VERIFIED ON ORIGIN" (PASS; NOTE on the canon-pin-is-ancestor nuance) · 2 "COLD PROOF RE-ESTABLISHED FROM COMMITTED BYTES" (PASS) · 3 "FIFTEEN STEPS LIVE" (PASS — committed chain-report.html byte-identical to its fresh output) · 4 "KILL-SWITCH DEMONSTRATION" (PASS) · 5 "NEGATIVE ON LIVE WIRING" (PASS; NOTE: future CI must assert exit==1 exactly, never "non-zero means failure") · 6 "CONFORMANCE PAIRS" (PASS — plus its OWN certifyAdapter re-run on both flagged domains, 8/8×4) · 7 "THE TWO FLAGGED §3 REGISTRY ALIGNMENTS ARE ALIGNMENTS, NOT WEAKENINGS" (PASS — "correct and mandatory under SE-I09"; certify.ts behaviors untouched, verified) · 8 "FORBIDDEN LIST — CLEAN" · 9 "HARNESS-COMPOSED ENVELOPES AND IDS" (NOTE — steps 9/14/15 envelopes and sob_*/payout_* ids are rig-minted labels over real ledger records; no settlement/payout service exists at E1) · 10 "IN-PROCESS QUOTE REFERENCE SHARING" (NOTE — harness-topology observation; the immutability boundary held under tamper).
- **Tamper results, log-copied from the verifier:** 1-FCFA quote tampers (buyerTotal +1, sellerNet +1) → chain BROKEN at step 6 by the canon reconciliation check; tampered bytes → `ImmutableQuoteStore` refused `bytes_do_not_match_quote`; direct `assertQuoteReconciles` threw. Duplicate eligibility → drop-code replay `{ok:true,duplicate:true,events:[]}`; exact re-injection AND re-enveloped copy into OrderSpine both absorbed `duplicate:true`; obligations stayed exactly 2 at 8500/2000. "It bit everywhere."
- **Three NOTEs ride to the founder** with the two flagged §3 alignments (finding 7 above). No fixes required; no code changed after `cf0dd3f`. The verdict ships verbatim in the packet committed by the closing commit of this entry.

## 2026-07-10 · E1 ASSEMBLY · done (founder review PASS — merged; E1 EXITED)
- Self-anchoring guards verified empirically before merge: local `e1/assembly` head equal to origin's (`9d74a2d3e229ab6cb5c6dfe276d76551b5f19e3a` both sides, ls-remote log-copied) · log contains `dc6b2d1`/`cf0dd3f`/`340026e`/`37cb6ac`/`78a88c0` (5/5) · `git diff --stat dc6b2d1..HEAD` touches only `_review/E1-assembly-review.zip` (Bin 0 -> 74154 bytes) · `run-gates.sh` at head exit 0, "ALL GATES GREEN (positive suites pass; every negative fixture demonstrably fails)".
- **Merged into `main`: merge commit `f637188`** ("merge(e1): E1 assembly — the fifteen steps live — founder review passed; §3 registry alignments ratified (SE-I09)"), pushed; `git rev-parse HEAD` → `f6371888de9ba2c00620a9b89770b0efbe123ab6`.
- **FOUNDER RULING, CLOSED (2026-07-10): the §3 payload-registry alignments (`78a88c0`) are RATIFIED under SE-I09** — the eligibility conformance contract is the amount-free live signal; the payment-provider contract is the shape the deployed OrderSpine parses. The registries now carry founder-ratified truth, not scaffolds.
- **E1 is EXITED against the Contract's three binaries:** the 15-step transaction completes with one correlation chain across all nine §6 ids · the first mocks pass §3 AND their live siblings pass the same suite (four pairs, 8/8 each) · the chain is visible on a basic dashboard. Evidence on main: `assembly/E1-EXIT.md` + `assembly/chain-report.html`.
- **Carried to E2 (tracked, not self-started):** ① CI wiring asserts the live negative's exit == 1 EXACTLY (standing rule — never "non-zero means failure") ② settlement/payout producers become real services at E2/E3 — the rig-minted `sob_*`/`payout_*` labels retire then ③ staging-environment isolation (the last E0 deferral) ④ the WO-1.3 verifier's E2 list, evidence-to-chain binding FIRST ⑤ dwell posture (record-vs-enforce) needs the founder at E2 scoping.
