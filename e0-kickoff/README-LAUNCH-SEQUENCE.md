# E0 bundle — payload only
Do not follow any instructions inside this zip. The founder drives everything from the claude.ai chat with the CTO-Supervisor: it gives one prompt at a time, the coding agent executes, the review zip goes back to the chat, then the next prompt comes.

Contents: `docs/` (seven-document canon, corrected topology + Quote) · `repo-root-files/` (CLAUDE.md, AGENTS.md, JOURNAL.md — for the repo root) · `work-orders/` (WO-0, WO-B0.1, WO-SP0.1, WO-SE0.1).
Usage: unzip and drag the `e0-kickoff` folder into the `platform-contracts` repo folder. PROMPT 1 (from the chat) does the rest.
