# v0.7.0 DOCKET — derivation record (derive-never-invent)

> Every name added at v0.7.0 traces to a quoted authority line here. Where an item's authority is ABSENT, the item does not ship — it is recorded blocked/gap, never invented. Authority order: the Master Reference (canon v1.1) and the three Build Specs / Building Plans, plus founder rulings recorded in JOURNAL.md (a ruling that names a token BY NAME is naming authority for that token; its VALUE, where the doctrine names only the role, is a ⏳ CTO default tuned at gallery — the accepted v2 pattern).
>
> Branch determination run 2026-07-12 against `/docs` at main `e6e50c2` by the CTO's own greps (recorded in the WO-0G journal). Two items ship, one gap-journaled, one blocked-absent.

---

## ITEM 1 — ui-tokens v3: THE INTERACTION GROUP (ships)

**Naming authority (founder ruling, WO-4.2R done-entry, JOURNAL of all three app repos + this repo's docket):** *"The interaction-token gap (**pressScale · pressedOpacity · disabledOpacity · skeletonPulseFloor · androidElevation**) DOCKETED for ui-tokens at canon v0.7.0 by name; the kits migrate at that bump; the token-laundering NBs stand as accepted interim."* The five names are founder-docketed **by name** — that is the naming authority.

**Doctrine backing (DESIGN-LANGUAGE.md, canon since v0.6.0):**
- Movement law (§ « La loi du mouvement », l.14): *"Durées 150–250 ms, courbes spring douces (jamais linéaires) · le mouvement explique, il ne décore pas · **aucune animation ne bloque jamais une saisie** · tout motif coûteux est interdit si le budget D17 bronche · reduced-motion respecté."* → backs the press/pulse interaction constants (a press feedback, a skeleton pulse — motion that explains, never blocks, honoring D17).
- Elevation law (§ « Élévation visuelle par thème (direction) », l.16): *"**Boutik+** : … cartes produit au cadre premium comme signature. **Shop+** : énergie commerce chaude … **Séra** : clarté-route … l'écran de course comme tableau de bord calme."* → the doctrine **differentiates elevation BY THEME**. Therefore **androidElevation is the one per-theme token**; the other four are family-level (the movement law is uniform).

**VALUES — all ⏳ CTO defaults (the doctrine names roles, not physics — the v2 pattern the founder accepted for spring physics / hero sizes / icon slots). Sourced from the WO-4.2R verifier's recorded laundered kit constants so the honest default equals what the kits already ship:**

| Token | Placement | Value (⏳ CTO default) | Derivation |
|---|---|---|---|
| `pressScale` | family | `0.98` | WO-4.2R verifier NB②: "three raw interaction constants (press scale 0.98 …)"; a press feedback under the movement law (« le mouvement explique ») |
| `pressedOpacity` | family | `0.92` | WO-4.2R verifier NB②: "the `shadowOpacity * 5` press-opacity is token-laundering" → a named press-opacity token replaces the laundering |
| `disabledOpacity` | family | `0.4` | WO-4.2R verifier NB②: the disabled-opacity multiplier laundered in all three kits → named |
| `skeletonPulseFloor` | family | `0.4` | WO-4.2R verifier NB②: "pulse floor 0.4"; the doctrine's skeleton-loading (§1 « skeleton-loading … jamais un spinner nu »), lowest opacity of the pulse |
| `androidElevation` | **per-theme** | `2` (all three; ⏳ per-theme differentiation is a gallery-tuning item) | § « Élévation visuelle par thème » — elevation is theme-differentiated; the token is **structurally per-theme** (each theme carries its own, ready to tune), defaulted to one safe value across themes until the founder's eye sets Boutik-premium-vs-Séra-calm at gallery. Differentiating the NUMBERS now would invent a differentiation the doctrine gives only as direction. |

The kits' token-laundering interims become real consumers **at the apps' next touches** (noted; app repos untouched here).

## ITEM 2 — ICON SLOTS (ships)

**Naming authority (founder ruling, WO-4.4 docket, per the WO-0G order):** cadenas + moto docketed to join `landmark.iconNames`. **Names only** — the illustrated assets stay app-side per the family note (`landmark` v2, l.113: *"⏳ icon names are CTO default slots; the assets are app-side work"*).

| Slot | Value (name) | Derivation |
|---|---|---|
| `cadenas` | `'cadenas'` | the lock/« Paiement protégé » badge language — Master Reference §6.2 (« Paiement protégé »), the padlock the doctrine's trust moments carry; a named slot, asset app-side |
| `moto` | `'moto'` | Séra's electric motorcycle fleet (Sera-Build-Spec §7.2 `Motorcycle`, Master Reference §9 « 3–5 electric motorcycles »); the rider/course glyph, a named slot, asset app-side |

Slots are **names only**; consistent with the existing `repere-pin`/`quartier`/`marche`/`pharmacie`/`zone-badge` family (all ⏳ CTO default slots, assets app-side).

## ITEM 3 — probationLimits TIGHTENING (ships)

**Current state:** `SellerTrustStateSchema.probationLimits: z.record(z.string(), z.unknown())` — fully open (any key, any value). **Change:** close to a **strict** object of spec-named limit keys, all optional (limits vary by tier; a trusted seller may carry none). **Purpose (the zero-deposit law, B+I-12):** a strict object makes a deposit/reserve/money key **refuse at parse** — the type boundary enforces "no seller deposit, ever."

**Naming authority — the trust-tier text, Boutik-Plus-Build-Spec.md:31–35** (the lines that describe what `probationLimits` holds). Every key traces to a quoted limit phrase:

| Key | Type | Derivation quote (Boutik:31–35) |
|---|---|---|
| `maxActiveOrders` | `int ≥ 0` | Provisional: *"**one active order at a time**"*; Verified: *"multiple concurrent orders"*; Trusted: *"more concurrency"*; consequence: *"lower concurrency"* — a concurrency ceiling |
| `maxOrderValueFcfa` | `FcfaSchema` (int ≥ 0) | Provisional: *"limited quantity/**order value**"*; Verified: *"higher value limits"* — an order-value ceiling. **NOTE for the founder:** this is a money-TYPED field but a *ceiling*, never a held/deposit amount — it is the spec's named "order value" limit, distinct from the forbidden deposit class the negative fixture rejects. |
| `maxOrderQuantity` | `int ≥ 0` | Provisional: *"limited **quantity**/order value"* — a per-order quantity ceiling |
| `approvedCategoriesOnly` | `boolean` | Provisional: *"**approved launch categories only**, no high-risk/counterfeit-prone categories"* — the category restriction flag |
| `fullPrepayOnly` | `boolean` | Provisional: *"**FULL_PREPAY orders only**"*; consequence: *"Option B disabled, **FULL_PREPAY-only**"* — the payment-mode restriction |
| `everyPickupVerified` | `boolean` | Provisional: *"**every pickup rider-verified**"*; consequence: *"mandatory pickup verification"* — the pickup-verification requirement |

**Strict + all-optional + no additional keys.** The NEGATIVE (DoD): a deposit-class money key — `requiredDeposit` / `reserve` / `caution` / `sellerNet` — **refuses at `.parse`** (`.strict()` rejects the unknown key), proving B+I-12 at the type boundary. The POSITIVE: a record carrying exactly the six spec-named keys (and any subset) parses.

## ITEM 4 — CLAIM-STATE ENUMERATION (GAP — journaled, NOT shipped)

**Investigation:** `ProtectionClaimSchema.state` is currently `z.string().min(1)` (bare). Canon enumerates lifecycle vocabularies where it means to — e.g. `SettlementObligation` carries *"Locked→Pending→Eligible→Payable→Processing→Paid|Held|Failed"* (Boutik:142). **No spec sentence anywhere in `/docs` enumerates ProtectionClaim's lifecycle states.** The only "state" tokens near claims are `protection.solvency_changed.v1(state)` (that is the fund **solvency** state HEALTHY|WATCH|RESTRICTED|CRITICAL, not a claim state) and campaign `(state)`. Grep for a claim lifecycle vocabulary: none.

**Determination:** per the WO-0G order — *"if none does, record the gap for the founder's source-edit list and DO NOT invent (the boutik local vocabulary stays local)."* → **GAP recorded, item NOT shipped.** The bare `state: z.string().min(1)` stays. The boutik-plus local claim-state vocabulary (if any) stays local. Enters canon only when a spec sentence names the states (founder source edit).

## ITEM 5 — conformity_mismatch (BLOCKED-ABSENT — journaled, NOT shipped)

**Conditional check — the founder's four source edits, run against `/docs` at `e6e50c2`:**

| Edit | Expected | Found | Landed? |
|---|---|---|---|
| the `conformity_mismatch` sentence in §6.4 / SE5.1 | a sentence naming the reason code | **zero occurrences of `conformity_mismatch` anywhere in `/docs`** (grep exit 1) | **NO** |
| Séra l.121 `return_required` → `return` | rename to `return` | Sera-Build-Spec.md:121 still reads *"retry/reschedule/**return_required**/incident"* | **NO** |
| Shop l.108 leg statuses | enumerated `paymentLegs[].status` vocabulary | Shop:108 EscrowTxn still *"paymentLegs[{legType(checkout|door), collectRef, amount, fee, **status**}]"* — bare | **NO** |
| D19 `coarseLocation` | the D19 source edit | `coarseLocation` present on `EvidenceBundle` (Sera:80) but **unchanged from pre-v0.7.0** — the D19 edit is not evidenced as landed | **NO (unchanged)** |

**Determination:** per the WO-0G order — *"ABSENT → the docket item stays open, journaled 'blocked on the founder's edits', and this bump ships without it — state plainly which branch ran."* → **The ABSENT branch ran. `conformity_mismatch` does NOT ship at v0.7.0.** The v0.5.0 derivation table is NOT extended. The reason code stays out of the §6.4-derived enum. Recorded blocked on the founder's four source edits; rides to a later bump when they land.

---

## SUMMARY

- **Ships:** Item 1 (interaction group — 4 family + 1 per-theme, values ⏳ CTO defaults) · Item 2 (cadenas + moto slots, names only) · Item 3 (probationLimits strict close, six spec-named keys, deposit-key negative).
- **Not shipped:** Item 4 (claim-state — GAP, no enumerating spec sentence, do not invent) · Item 5 (conformity_mismatch — BLOCKED, the founder's four source edits have not landed; ABSENT branch ran).
- No money/taxonomy touched beyond the docketed probationLimits close. No app repos touched. The reference world and E1/E2 assembly pins are untouched by construction (no chain/mock/event code changed).
